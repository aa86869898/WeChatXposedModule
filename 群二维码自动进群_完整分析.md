# 纯后台自动扫码进群 — 完整逆向分析

## 一、当前进展

```
✅ 图片检测(type=3) → e9.y0() 获取路径
✅ RecogQBarOfImageFileEvent 触发QR识别
✅ RecogQBarOfImageFileResultEvent 拿到结果
✅ URL含/g/ → 确认群邀请
✅ CreateOrJoinChatroomEvent published
❌ 实际未加入 — 因为这是OpenSDK入口，不是内部API
```

## 二、CreateOrJoinChatroomEvent 为何不行

gt.m.callback() 源码:
```java
Context context = q3Var.d;
if (context == null) {
    Log.e("...", "context is null");
    return false;  // ← 传null直接拒绝
}
// 然后启动 WXBizEntryActivity — 必须在前台
```

## 三、微信内部真正的加群链路

```
二维码结果 → ScanCodeSheetItemLogic.i(type=22, f2)
  │
  ├─ z0.e(22, url) → 校验通过
  │
  ├─ y.a(22, f2Url)
  │   └─ new wr5.g()         ← Pipeline构建器
  │       .N([type, f2Url])   ← 输入参数
  │       .G(new x(this))     ← 步骤1: 获取URL信息
  │       返回 rn5.q
  │
  ├─ .b(callback)            ← 设置完成回调
  │
  └─ Pipeline.d() 执行:
      │
      └─ x.call():
          ├─ 判断URL类型: z0.o()→类型4(WX_CODE=群邀请)
          │
          ├─ CGI请求: /cgi-bin/micromsg-bin/batchgeturlinfo
          │   参数: lr3.d=url, lr3.e=urlType(4), ...
          │
          ├─ 回调 w.a():
          │   └─ 成功 → mr3响应
          │       mr3.d = 返回码
          │       mr3.i = 群名
          │       mr3.e = URL
          │       mr3.g = URL类型
          │
          └─ Pipeline把mr3传给callback
              │
              └─ callback(c1)处理:
                  ├─ 需要验证 → 打开RoomAccessVerifyApplicationUI
                  └─ 直接可加 → 发起加入CGI
```

## 四、纯后台方案

### 方案 A：自己走 Pipeline（推荐）

```java
// 1. 构造 f2 包装URL
Class<?> f2Clz = cl.loadClass("jd0.f2");
Object f2 = XposedHelpers.newInstance(f2Clz);
XposedHelpers.setObjectField(f2, "a", inviteUrl);

// 2. 通过 y.a() 创建Pipeline
Class<?> yClz = cl.loadClass("com.tencent.mm.plugin.scanner.y");
Object y = XposedHelpers.newInstance(yClz);
Object pipeline = XposedHelpers.callMethod(y, "a", 22, f2);

// 3. Pipeline会:
//    → batchgeturlinfo CGI获取群信息
//    → 结果交给callback

// 4. 设置自定义callback处理mr3结果
// (需要实现rn5.f接口)

// 5. 执行
XposedHelpers.callMethod(pipeline, "d");
```

**优点**: 完全走微信内部路径，参数正确
**难点**: 需要设置callback处理mr3响应，并在callback中发起加入CGI

### 方案 B：直接调 batchgeturlinfo + 自己解析

```java
// 直接构造CGI请求
// POST /cgi-bin/micromsg-bin/batchgeturlinfo (cmd=3964)
// 请求体: lr3{ d:url, e:4, f:0, g:0, h:0, i:0 }
// 响应: mr3{ d:retCode, i:groupName, e:url, g:urlType }
// 
// 拿到群名等信息后 → 调用加入CGI
```

## 五、待确认

1. **加入CGI是什么**: batchgeturlinfo拿到群信息后，用什么CGI执行加入？
2. **mr3回调处理**: c1回调的具体逻辑是什么？
3. **是否需要验证**: 有些群加了验证，mr3.d会指示是否需要验证

## 六、下一步

**最快验证方式**: 用方案A创建Pipeline，hook batchgeturlinfo的响应，
先看mr3返回了什么数据，再决定如何构造加入请求。

代码框架:
```java
// Hook batchgeturlinfo响应 → 打印所有字段
// y.a(22, f2).b(customCallback).d()
// → 看customCallback收到的Object是什么
// → 根据收到的群信息构造加入CGI
```
