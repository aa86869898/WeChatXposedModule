# 纯后台加群 - 深挖分析

## 已确认的完整调用链

```
QR识别结果 (type=22, url=weixin.qq.com/g/XXXX)
  │
  ├─ ScanCodeSheetItemLogic.i(type=22, f2={a:url})
  │   └─ if z0.e(type=22, url) → 通过校验
  │       └─ y.a(type, f2)
  │           │
  │           ├─ new wr5.g()           ← Pipeline 构建器
  │           ├─ .N([type, f2Url])     ← 设置输入参数
  │           ├─ .G(new x(this))       ← 添加步骤1: x.call()
  │           └─ 返回 rn5.q (Pipeline)
  │
  ├─ .b(new c1(this, url))            ← 设置最终回调
  │
  └─ Pipeline.d() 执行:
      │
      └─ x.call(input):
          │
          ├─ 根据 z0 判断 URL 类型
          │   z0.o(type,url) → 类型4 (WX_CODE, 群邀请)
          │   z0.d(type,url) → 类型3
          │   z0.l(type,url) → 类型1
          │   ...
          │
          ├─ 构造 batchgeturlinfo CGI 请求
          │   CGI: /cgi-bin/micromsg-bin/batchgeturlinfo (cmd=3964)
          │   请求参数:
          │     lr3.d = url        ← 群邀请URL
          │     lr3.e = urlType    ← 4 (WX_CODE)
          │     lr3.f = f2Var.b    ← 
          │     lr3.g = f2Var.c    ← 
          │     lr3.h = f2Var.d    ← 
          │     lr3.i = f2Var.e    ← 
          │
          ├─ h0.a(request, new w(this, c))
          │   w.a(errType, errCode, errMsg, response):
          │     └─ 成功: bVar.c([mr3])
          │         mr3.d = 返回码
          │         mr3.i = 群名
          │         mr3.e = URL
          │         mr3.g = URL类型
          │
          └─ Pipeline 将 mr3 传给最终回调 c1
              │
              └─ c1 处理:
                  ├─ 判断 mr3.d (是否需要验证)
                  ├─ 如果需要验证 → 打开 RoomAccessVerifyApplicationUI
                  ├─ 如果直接可加 → ??? (发起加入CGI)
                  └─ ???
```

## 加群CGI

batchgeturlinfo 拿到群信息后，需要调用第二个 CGI 执行加入。

待确认的CGI:
- /cgi-bin/micromsg-bin/verifychatroom (验证群)
- /cgi-bin/micromsg-bin/joinchatroom (加入群)

## 深挖方向

1. 找到 ScanCodeSheetItemLogic$c1 回调 → 看它如何处理 mr3 结果
2. 从 c1 找到实际的 join CGI 调用
3. 模拟整个 Pipeline 在后台执行，用自定义回调替代 c1
