微信联系人 / 群聊数据库 调取链路 —— 地毯式分析报告
目标 APK： com.tencent.mm （WCDB/SQLCipher 加密库  EnMicroMsg.db ）结论均已通过 Smali 交叉验证（静态方法签名、invoke-static/invoke-virtual、字段归属），下文每条都给出证据行号。
一、总体架构（三张表 + 三层入口）
微信数据存储是  MMKernel → Storage 插件 → Storage 类 → DB Helper(qf5.k0) → WCDB(SQLCipher)  五层结构：
内核
 gp0.j1 
MMKernel，静态方法  v(Class):lp0.a  按接口取插件
存储插件
 com.tencent.mm.plugin.messenger.foundation.h2 
实现接口  tn3.c4 ，持有所有 storage 实例
群聊服务
 p02.a 
实现接口  q02.f ， a()  返回群存储
联系人存储
 com.tencent.mm.storage.j4 
表  rcontact 
群聊存储
 com.tencent.mm.storage.a3 
表  chatroom 
DB Helper
 qf5.k0 （实现类  kh5.b0 ）
真正的 SQL 执行器，持有 WCDB 连接
联系人模型
 com.tencent.mm.storage.y3  →  com.tencent.mm.contact.s  →  im.f2 
字段  field_*  + lvbuff
群模型
 com.tencent.mm.storage.z2  →  im.y1 
字段  field_chatroom* 
关键证据（ a80.p0.W7  的 Smali）：
const-class v2, Ltn3/c4;
invoke-static {v2}, Lgp0/j1;->v(Ljava/lang/Class;)Llp0/a;   # 内核取插件
check-cast v2, Ltn3/c4;
check-cast v2, Lcom/tencent/mm/plugin/messenger/foundation/h2;
invoke-virtual {v2}, L.../h2;->cj()Lcom/tencent/mm/storage/j4;   # 取 ContactStorage
invoke-virtual {v2, v1}, Lcom/tencent/mm/storage/j4;->q(Ljava/lang/String;)Lcom/tencent/mm/storage/y3;
群聊证据（ b41.d2.E  的 Smali）：
const-class v2, Lq02/f;
invoke-static {v2}, Lgp0/j1;->v(Ljava/lang/Class;)Llp0/a;
check-cast v2, Lp02/a;
invoke-virtual {v2}, Lp02/a;->a()Lcom/tencent/mm/storage/a3;
invoke-virtual {v2, p0}, Lcom/tencent/mm/storage/a3;->t1(Ljava/lang/String;)Lcom/tencent/mm/storage/z2;
二、核心调用链（完整、逐跳）
1）打开数据库（拿到真正的 SQL 执行器  qf5.k0 ）
text
gp0.j1.v(tn3.c4.class)              // 静态，返回存储插件 h2
   └─ h2.cj()                       // 返回 ContactStorage (com.tencent.mm.storage.j4)
        └─ j4.d                     // 字段 d 类型 Lqf5/k0;  ← DB Helper
             └─ qf5.k0.B(sql, args) // 返回 android.database.Cursor（= rawQuery）
•  
 j4  字段确认： d: Lqf5/k0; （decompile_class_fields）
•  
 a3  字段确认： d: Lqf5/k0; （同上）
•  
 qf5.k0  是 interface， B(String,String[])  为抽象方法（get_class_smali 第 10 行）
•  
实现类  kh5.b0 ： B(sql,args)  →  f(sql,args,0)  →  this.b.z(...)  即 WCDB rawQuery（decompile_method 已验证）
•  
另有  qf5.k0.f(sql,args,flag) 、 qf5.k0.getPath() （返回 DB 文件路径）
不依赖 hook，纯反射即可在登录态下直接执行任意 SQL。
2）取单个联系人对象（自动解析 lvbuff）
text
j4.q(String username) → com.tencent.mm.storage.y3 (Contact)
 j4.q  内部：先查内存缓存  L(username) ，未命中则执行 select username,alias,conRemark,...,lvbuff,... from rcontact where username='...' ，再  y3.convertFrom(cursor) （ im.f2.convertFrom(Cursor)  按列名 hash 匹配 → 写  field_* ，并  parseBuff()  解析  lvbuff ）。
3）取群聊
text
gp0.j1.v(q02.f.class).a()  → com.tencent.mm.storage.a3 (ChatroomStorage)
a3.t1(String chatroomname) → com.tencent.mm.storage.z2 (ChatroomInfo)
4）取当前登录者信息（配置存储）
text
gp0.j1.x()          // 静态 → gp0.b0 (CoreStorage)
   └─ .c()          // → com.tencent.mm.storage.q3 (ConfigStorage)
        └─ .m(int key, Object def)  // 取值
•  
key  2  = 当前用户 wxid（证据： b41.y1.u() ）
•  
key  12290  = 当前用户性别（1=男, 2=女）（证据： SelfQRCodeUtil.initView ）
三、字段映射（联系人「全套」）
rcontact 真实列（证据： j4.D()  的 SQL）
username, alias, conRemark, domainList, nickname, pyInitial, quanPin,
showHead, type, uiType, weiboFlag, weiboNickname, conRemarkPYFull,
conRemarkPYShort, lvbuff, verifyFlag, encryptUsername, chatroomFlag,
deleteFlag, contactLabelIds, descWordingId, openImAppid, sourceExtInfo,
rowid, contactExtra
注意：rcontact 没有  sex  列！ 性别存在  lvbuff （二进制），由  Contact.parseBuff()  解出。
Contact 对象 getter ↔ 字段（全部经 decompile_method 验证）
wxid
 b1() 
 field_username 
昵称
 M0() 
 field_nickname 
备注
 r0() 
 field_conRemark 
微信号
 l0() （ im.f2.l0()  为原始 alias）
 field_alias 
性别
public int 字段  I （1=男,2=女）
lvbuff 第 2 个 int
类型
 getType() 
 field_type 
加密用户名
 G0() 
 field_encryptUsername 
验证标志
 f1() 
 field_verifyFlag 
拼音首字母
 Q0() 
 field_pyInitial 
全拼
 T0() 
 field_quanPin 
备注全拼
 s0() 
 field_conRemarkPYFull 
备注简拼
 x0() 
 field_conRemarkPYShort 
域名列表
 F0() 
 field_domainList 
描述词ID
 E0() 
 field_descWordingId 
OpenIM AppId
 N0() 
 field_openImAppid 
标签ID
 A0() 
 field_contactLabelIds 
ticket
 Y0() 
 field_ticket 
删除标志
 D0() 
 field_deleteFlag 
头像显示
 V0() 
 field_showHead 
群标志
 p0() 
 field_chatroomFlag 
微博标志/昵称
 j1() / k1() 
 field_weiboFlag / field_weiboNickname 
扩展
 z0() 
 field_contactExtra 
性别铁证（ NormalProfileHeaderPreference.B  的 Smali 2765-2841 行）：
iget v2, v2, Lim/f2;->I:I          # 取 Contact.I
const-string v3, "contact_profile_sex_icon"
if-ne v2, v9(1)  → 0x7f1104c9 (男图标)
if-ne v2, v10(2) → 0x7f1104c8 (女图标)
四、字段映射（群聊）
chatroom 真实列（证据： im.y1.initAutoDBInfo ）
chatroomname, addtime, memberlist, displayname, chatroomnick, roomflag,
roomowner, roomdata, isShowname, selfDisplayName, style, chatroomdataflag,
modifytime, chatroomnotice, xmlChatroomnotice, chatroomVersion,
chatroomnoticeEditor, chatroomnoticePublishTime, chatroomNoticeNew,
chatroomLocalVersion, chatroomStatus, memberCount,
chatroomfamilystatusmodifytime, associateOpenIMRoomName,
openIMRoomMigrateStatus, saveByteVersion, handleByteVersion,
roomInfoDetailResByte, oldChatroomVersion, localChatRoomWatchMembers,
spamStatus, compactFlag, qrCodeAccessType, rowid
ChatroomInfo（ z2 / im.y1 ）字段
群ID（xxx@chatroom）
 field_chatroomname 
成员列表（ ; 分隔）
 field_memberlist 
群名称
 field_displayname 
群昵称
 field_chatroomnick 
群主
 field_roomowner 
成员数
 field_memberCount 
群状态
 field_chatroomStatus 
群公告
 field_chatroomnotice 
我在群昵称
 field_selfDisplayName 
修改时间
 field_modifytime 
群列表有两种取法：① 查  rcontact ： SELECT username,nickname,conRemark FROM rcontact WHERE username LIKE '%@chatroom' （群名在 nickname/conRemark）；② 查  chatroom ： SELECT chatroomname,displayname,memberCount,roomowner FROM chatroom 。
五、可直接用的 Kotlin 验证代码
kotlin
package com.example.wxdump

import android.database.Cursor
import de.robv.android.xposed.IXposedHookLoadPackage
import de.robv.android.xposed.XposedBridge
import de.robv.android.xposed.callbacks.XC_LoadPackage

object WxDb {

    /* ---------- 基础反射 ---------- */
    private fun plugin(cl: ClassLoader, ifaceName: String): Any {
        val kernel = Class.forName("gp0.j1", false, cl)
        val v = kernel.getDeclaredMethod("v", Class::class.java)   // 静态
        v.isAccessible = true
        return v.invoke(null, Class.forName(ifaceName, false, cl))!!
    }

    /** 联系人存储 com.tencent.mm.storage.j4 */
    fun contactStorage(cl: ClassLoader): Any =
        plugin(cl, "tn3.c4").javaClass.getMethod("cj").invoke(plugin(cl, "tn3.c4"))!!

    /** 群存储 com.tencent.mm.storage.a3 */
    fun chatroomStorage(cl: ClassLoader): Any {
        val p = plugin(cl, "q02.f")
        return p.javaClass.getMethod("a").invoke(p)!!
    }

    /** 取 DB Helper qf5.k0（storage 的字段 d） */
    fun dbHelper(storage: Any): Any {
        val f = storage.javaClass.getDeclaredField("d")
        f.isAccessible = true
        return f.get(storage)!!
    }

    /** 执行任意 SQL → Cursor */
    fun rawQuery(db: Any, sql: String): Cursor {
        val m = db.javaClass.getMethod("B", String::class.java, Array<String>::class.java)
        return m.invoke(db, sql, null) as Cursor
    }

    fun dbPath(db: Any): String? =
        db.javaClass.getMethod("getPath").invoke(db) as String?

    /* ---------- 联系人全套 ---------- */
    data class Contact(
        val wxid: String?, val nickname: String?, val remark: String?,
        val alias: String?, val sex: Int, val type: Int
    )

    fun dumpAllContacts(cl: ClassLoader): List<Contact> {
        val db = dbHelper(contactStorage(cl))
        // 关键：SELECT 必须包含 lvbuff，convertFrom 才能解出 sex
        val sql = "SELECT username,alias,conRemark,nickname,lvbuff,type " +
                  "FROM rcontact WHERE type & 1 != 0"        // type&1!=0 = 好友
        val c = rawQuery(db, sql)

        val y3Cls = Class.forName("com.tencent.mm.storage.y3", false, cl)
        val convertFrom = y3Cls.getMethod("convertFrom", Cursor::class.java)
        val out = ArrayList<Contact>()

        while (c.moveToNext()) {
            val contact = y3Cls.getDeclaredConstructor().newInstance()
            convertFrom.invoke(contact, c)          // 内部解析 lvbuff → 字段 I = 性别
            val iField = y3Cls.getField("I")        // 继承自 im.f2 的 public int
            out.add(Contact(
                wxid     = c.getString(c.getColumnIndexOrThrow("username")),
                nickname = c.getString(c.getColumnIndexOrThrow("nickname")),
                remark   = c.getString(c.getColumnIndexOrThrow("conRemark")),
                alias    = c.getString(c.getColumnIndexOrThrow("alias")),
                sex      = iField.getInt(contact),   // 1=男 2=女 0=未知
                type     = c.getInt(c.getColumnIndexOrThrow("type"))
            ))
        }
        c.close()
        return out
    }

    /* ---------- 群聊列表 ---------- */
    fun dumpAllChatrooms(cl: ClassLoader): List<Array<Any?>> {
        val db = dbHelper(chatroomStorage(cl))
        val c = rawQuery(db,
            "SELECT chatroomname,displayname,memberCount,roomowner,memberlist FROM chatroom")
        val out = ArrayList<Array<Any?>>()
        while (c.moveToNext()) {
            out.add(arrayOf(
                c.getString(0), c.getString(1), c.getInt(2), c.getString(3), c.getString(4)
            ))
        }
        c.close()
        return out
    }

    /* ---------- 当前登录者 ---------- */
    fun currentWxid(cl: ClassLoader): String? {
        val core = Class.forName("gp0.j1", false, cl).getMethod("x").invoke(null)
        val cfg  = core.javaClass.getMethod("c").invoke(core)
        return cfg.javaClass.getMethod("m", Int::class.java, Any::class.java)
            .invoke(cfg, 2, null) as String?
    }

    fun currentSex(cl: ClassLoader): Int {
        val core = Class.forName("gp0.j1", false, cl).getMethod("x").invoke(null)
        val cfg  = core.javaClass.getMethod("c").invoke(core)
        return (cfg.javaClass.getMethod("m", Int::class.java, Any::class.java)
            .invoke(cfg, 12290, 0) as? Int) ?: 0
    }
}

/* ---------- Xposed 入口 ---------- */
class MainHook : IXposedHookLoadPackage {
    override fun handleLoadPackage(lpparam: XC_LoadPackage.LoadPackageParam) {
        if (lpparam.packageName != "com.tencent.mm") return
        // 必须等内核初始化（登录后）。挂 LauncherUI.onCreate 是稳妥切入点。
        try {
            val cl = lpparam.classLoader
            val ui = Class.forName("com.tencent.mm.ui.LauncherUI", false, cl)
            XposedBridge.hookAllMethods(ui, "onCreate", object : de.robv.android.xposed.XC_MethodHook() {
                override fun afterHookedMethod(param: MethodHookParam) {
                    Thread {
                        val contacts = WxDb.dumpAllContacts(cl)
                        val rooms    = WxDb.dumpAllChatrooms(cl)
                        XposedBridge.log("wxid=${WxDb.currentWxid(cl)} sex=${WxDb.currentSex(cl)}")
                        XposedBridge.log("联系人=${contacts.size} 群=${rooms.size}")
                        XposedBridge.log("DB=${WxDb.dbPath(WxDb.dbHelper(WxDb.contactStorage(cl)))}")
                    }.start()
                }
            })
        } catch (t: Throwable) { XposedBridge.log(t) }
    }
}
六、二次重审（逐条对账，确保精准）
1
 gp0.j1.v  是 静态 方法， (Class):lp0.a 
 gp0.j1.v  Smali： .method public static v(Ljava/lang/Class;)Llp0/a; 
✅
2
 tn3.c4  是存储插件接口， h2  实现， cj() →j4
 a80.p0.W7  Smali 100-110 行
✅
3
 q02.f  是群服务接口， p02.a  实现， a() →a3
 b41.d2.E  Smali 34-44 行
✅
4
 j4.d / a3.d  类型 =  qf5.k0 
decompile_class_fields
✅
5
 qf5.k0.B(sql,args)  即 rawQuery
 kh5.b0.B→f→b.z() ；接口声明第 10 行
✅
6
rcontact 列名
 j4.D() / j4.q()  SQL 字面量
✅
7
联系人 getter 映射
逐个 decompile_method（b1/M0/r0/l0/getType…）
✅
8
性别 =  im.f2.I （lvbuff 解出）
 parseBuff()  解码序 + 头像 Smali 2765-2841 行
✅
9
性别图标 1=男/2=女
 SelfQRCodeUI.initView  用 2131821769(男)/2131821768(女)
✅
10
chatroom 列名
 im.y1.initAutoDBInfo  33 列全量
✅
11
群 getter 字段
 im.y1  field_* 字段表
✅
12
当前 wxid=cfg(2)，自身性别=cfg(12290)
 b41.y1.u()  /  SelfQRCodeUI 
✅
已排除的误区：
•  
❌  ContactStorage.getSex()  不存在 —— 性别不在 rcontact 列中，只能读  im.f2.I  或解析 lvbuff。
•  
❌ 不能用  find_class(class_name_pattern="j4")  之类短名精确匹配，必须用完整混淆名。
•  
❌  contact.s.l0()  会返回「群昵称」而非纯 alias；要原始微信号需读  field_alias （ im.f2.l0() ）。
七、注意事项 / 风险
1.  
混淆名随版本变化： gp0.j1 / tn3.c4 / h2 / j4 / a3 / q02.f / p02.a / qf5.k0  均会随微信版本改名。建议把上述名称做成 可配置常量，并做「多版本候选探测」。
2.  
调用时机： gp0.j1.v()  在内核未初始化时返回  null （日志  Skeleton not initialized! ）。必须在登录后（如  LauncherUI.onCreate  之后）调用。
3.  
DB 已解密： qf5.k0  是微信进程内已解密的 WCDB 句柄，无需自己算 key（ md5(IMEI+uin)[:7] ）。若你要离线读取  EnMicroMsg.db ，用  getPath()  拿路径后需自备 SQLCipher + key。
4.  
线程：DB 查询建议放子线程；主线程查询可能触发微信自身 ANR 检测。
5.  
sex 取值： I  为 0 表示未知（很多好友本地不存性别，微信也不显示）。
6.  
群成员： field_memberlist  是  wxid1;wxid2;... ，需再对每个 wxid 调  j4.q()  才能拿成员昵称。
以上即为完整链路与可直接落地的 Kotlin 验证代码。