# 微信联系人动态抓取 & 修改日志 — Xposed 实现方案

> 基于微信 8.0+ 分析，使用 LSPilot / LSPosed 模块实现

---

## 一、核心数据模型继承链

```
com.tencent.mm.storage.y3       ← Contact（完整联系人，直接 Hook）
    └── com.tencent.mm.contact.s  ← RContact（中间层，含 contactExtra）
          └── dm.f2               ← BaseContact（rcontact 表 ORM 映射）
                └── s85.f0        ← AutoDB 基类
```

---

## 二、关键类与职责

### 2.1 联系人存储引擎

| 类 | 职责 |
|---|---|
| `com.tencent.mm.storage.j4` | ContactStorage — 联系人 DB 读写枢纽 |
| `com.tencent.mm.storage.y3` | Contact 实例 — 所有字段 getter/setter |
| `dm.f2` | BaseContact — rcontact 表字段映射 |
| `com.tencent.mm.contact.s` | RContact — 含 contactExtra 扩展 |

### 2.2 头像服务

| 类 | 职责 |
|---|---|
| `com.tencent.mm.modelavatar.g` | 头像获取核心 |
| `com.tencent.mm.modelavatar.r0` | 头像数据载体 |
| `com.tencent.mm.modelavatar.d1` | 头像存储单例 |

### 2.3 UI 层

| 类 | 职责 |
|---|---|
| `com.tencent.mm.plugin.profile.ui.ContactInfoUI` | 联系人详情页 |

---

## 三、rcontact 表字段映射（dm.f2）

| 中文含义 | SQL 列名 | Getter | 返回类型 |
|----------|----------|--------|----------|
| **昵称** | `nickname` | `M0()` | String |
| **微信号** | `alias` | `t0()` | String |
| **用户名(wxid)** | `username` | `d1()` | String |
| **备注** | `conRemark` | `w0()` | String |
| **头像标记** | `showHead` | `R0()` | int |
| **类型** | `type` | `getType()` | int |
| **验证标志** | `verifyFlag` | `f1()` | int |
| **加密用户名** | `encryptUsername` | `I0()` | String |
| **拼音首字母** | `pyInitial` | `P0()` | String |
| **全拼** | `quanPin` | `Q0()` | String |
| **微博昵称** | `weiboNickname` | `k1()` | String |
| **扩展信息** | `contactExtra` | `z0()` | a65.z80 (protobuf) |
| **标签列表** | `contactLabelIds` | `A0()` | String |
| **域名列表** | `domainList` | `H0()` | String |
| **来源扩展** | `sourceExtInfo` | `X0()` | String |
| **ticket** | `ticket` | `Z0()` | String |
| **创建时间** | `createTime` | `getCreateTime()` | long |
| **删除标志** | `deleteFlag` | `u0()` | int |

> ⚠️ **签名 (Signature)** 不在 rcontact 基础表中，在 `z0()` 返回的 `a65.z80` protobuf 扩展对象内。

---

## 四、ContactStorage 核心 API

| 方法 | 签名 | 描述 |
|------|------|------|
| `m()` | `(String username) → y3` | 取联系人（带缓存） |
| `n()` | `(String, boolean) → y3` | 取联系人（直查 SQL） |
| `i()` | `(y3) → void` | 更新内存缓存 |
| `i0()` | `(y3) → int` | INSERT 联系人 |
| `l0()` | `(y3) → boolean` | REPLACE/UPDATE 联系人 |
| `h0()` | `(y3) → boolean` | → `i0()` > 0 的封装 |

---

## 五、头像服务 API（modelavatar.g）

| 方法 | 描述 |
|------|------|
| `a(String username) → Bitmap` | 获取头像 Bitmap（默认尺寸） |
| `b(String, boolean, int, e) → Bitmap` | 获取指定尺寸头像 |
| `c(String, boolean, boolean) → String` | 获取头像文件路径 |
| `h(String, r0, String)` | 更新/设置头像缓存 |

---

## 六、动态 Hook 方案设计

### 方案 1：读拦截 — 修改返回值（影响范围：全 App）

```
Hook dm.f2.M0() → 昵称
Hook dm.f2.t0() → 微信号
Hook dm.f2.w0() → 备注
Hook g.c()      → 头像路径
```

每个 Hook 在 `After` 中：
1. 读取原始返回值
2. 写日志（username + 原始值 + 时间戳）
3. 根据规则判断是否替换返回值

### 方案 2：写拦截 — 记录修改事件（影响范围：同步/保存）

```
Hook j4.l0() → 拦截 REPLACE
Hook j4.i0() → 拦截 INSERT
```

每个 Hook 在 `Before` 中：
1. 提取 y3 对象字段
2. 对比旧值/记录新值
3. 输出完整变更日志

### 方案 3：UI 层拦截 — 展示层（影响范围：仅当前页面）

```
Hook ContactInfoUI.onResume() / 数据绑定方法
```

---

## 七、完整 BSH 插件代码

```bsh
// 插件名: ContactLogger
// 功能: 动态抓取联系人修改 + 日志记录到 /sdcard/contact_log.txt

import dm.f2;
import com.tencent.mm.storage.j4;
import com.tencent.mm.storage.y3;
import com.tencent.mm.modelavatar.g;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.io.FileWriter;
import java.io.File;

logFile = "/sdcard/contact_log.txt";

// ========== 工具函数 ==========
writeLog(String tag, String username, String field, String value) {
    sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss.SSS");
    time = sdf.format(new Date());
    line = "" + time + " | " + tag + " | " + username + " | " + field + " = " + value;
    log(line);
    try {
        fw = new FileWriter(logFile, true);
        fw.write(line + "\n");
        fw.close();
    } catch (Exception e) {
        log("Write log error: " + e.getMessage());
    }
}

// ========== 1. 昵称读拦截 ==========
hookMethodAfter(f2.class, "M0", param -> {
    username = ((f2) param.thisObject).d1();
    nickname = (String) param.getResult();
    writeLog("[READ]", username, "nickname", nickname);
    // 动态替换示例（取消注释生效）:
    // if (username.equals("wxid_xxxxx")) param.setResult("自定义昵称");
});

// ========== 2. 微信号读拦截 ==========
hookMethodAfter(f2.class, "t0", param -> {
    username = ((f2) param.thisObject).d1();
    alias = (String) param.getResult();
    writeLog("[READ]", username, "alias", alias);
});

// ========== 3. 备注读拦截 ==========
hookMethodAfter(f2.class, "w0", param -> {
    username = ((f2) param.thisObject).d1();
    remark = (String) param.getResult();
    writeLog("[READ]", username, "conRemark", remark);
});

// ========== 4. 联系人更新拦截 ==========
hookMethodBefore(j4.class, "l0", param -> {
    contact = (y3) param.args[0];
    username = contact.d1();
    writeLog("[UPDATE]", username, "nickname", contact.M0());
    writeLog("[UPDATE]", username, "alias", contact.t0());
    writeLog("[UPDATE]", username, "conRemark", contact.w0());
    writeLog("[UPDATE]", username, "type", "" + contact.getType());
});

// ========== 5. 联系人插入拦截 ==========
hookMethodBefore(j4.class, "i0", param -> {
    contact = (y3) param.args[0];
    username = contact.d1();
    writeLog("[INSERT]", username, "nickname", contact.M0());
    writeLog("[INSERT]", username, "alias", contact.t0());
    writeLog("[INSERT]", username, "type", "" + contact.getType());
});

// ========== 6. 头像路径拦截 ==========
hookMethodAfter(g.class, "c", param -> {
    username = (String) param.args[0];
    path = (String) param.getResult();
    writeLog("[AVATAR]", username, "path", path);
    // 动态替换头像:
    // if (username.equals("wxid_xxxxx")) param.setResult("/sdcard/custom_avatar.jpg");
});

log("=== ContactLogger 已加载，日志输出到 " + logFile);
```

---

## 八、架构总览

```
┌──────────────────────────────────────────────────────┐
│                   Xposed Hook 层                      │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 昵称 get │  │ 微信号get │  │ 头像路径 get     │   │
│  │ M0()     │  │ t0()     │  │ g.c()            │   │
│  └────┬─────┘  └────┬─────┘  └───────┬──────────┘   │
│       │              │               │               │
│       └──────────────┼───────────────┘               │
│                      ▼                               │
│           ┌──────────────────┐                       │
│           │  j4.l0() UPDATE  │ ← 写拦截              │
│           │  j4.i0() INSERT  │                       │
│           └────────┬─────────┘                       │
│                    │                                 │
└────────────────────┼─────────────────────────────────┘
                     ▼
            ┌─────────────────┐
            │  日志记录引擎    │
            │  时间+用户+字段  │
            │  +原始值+新值    │
            └────────┬────────┘
                     ▼
            ┌─────────────────┐
            │  /sdcard/        │
            │  contact_log.txt │
            └─────────────────┘
```

---

## 九、Hook 点优先级

| 优先级 | Hook 点 | 覆盖范围 | 性能开销 |
|--------|---------|----------|----------|
| ⭐⭐⭐ | `dm.f2.M0()` (nickname) | 全局读昵称 | 低 |
| ⭐⭐⭐ | `dm.f2.t0()` (alias) | 全局读微信号 | 低 |
| ⭐⭐⭐ | `j4.l0()` (写入) | 拦截修改操作 | 低 |
| ⭐⭐ | `j4.i0()` (插入) | 拦截新增联系人 | 低 |
| ⭐⭐ | `g.c()` (头像路径) | 全局头像读取 | 中 |
| ⭐ | `ContactInfoUI.onResume()` | 仅详情页 | 低 |

---

## 十、已知限制

1. **签名 (Signature)** 不在 rcontact 表，需解析 `z0()` → `a65.z80` protobuf
2. 微信版本升级时类名可能混淆变更，需用 `search_classes` 重新定位
3. 头像路径 Hook 在高频场景（列表滑动）会有性能影响，建议加白名单过滤
4. `dm.f2` 的字段通过 `ContentValues` 的 trace flag 做脏标记，直接修改字段可能不触发更新
