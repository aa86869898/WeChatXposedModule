已完成地毯式分析与插件开发，并按要求做了二次审查。以下是完整交付内容。
一、微信主页聊天分组过滤 —— 完整链路分析（基于当前 APK：WeChat 8.0.78_3180）
1. 主界面结构（“标题栏下方”的注入点）
com.tencent.mm.ui.LauncherUI                      ← 主界面 Activity（微信首页）
└─ com.tencent.mm.ui.HomeUI                       ← 管理标题栏 ActionBar（"微信"标题/搜索/＋）、tab
   └─ com.tencent.mm.ui.MainTabUI                 ← 底部 Tab 容器
      └─ com.tencent.mm.ui.conversation.MainUI    ← “微信”Tab 会话列表 Fragment ★核心
           ├─ getLayoutView()：ud.c.a("LayoutsForLauncherUI").a(…, 2131629235, …) 加载布局
           │     根视图 this.H = MainUIView（RelativeLayout）
           │     this.p = ConversationListView（id 0x7F13155B=2131379963）
           │     this.u = TextView(2131369308) / this.v = LinearLayout(2131379962) / this.r = View(2131387734)
           ├─ this.o = u5 列表封装（HeaderView 注入点）
           │     jo5.x.e（RepairerConfigConvRecyclerView，默认 true）
           │       true  → po5.m0(ConversationRecyclerView)   ★默认新路径（RecyclerView）
           │       false → com.tencent.mm.ui.conversation.p3(ConversationListView)
           └─ this.w = jo5.l0 会话列表适配器/控制器（MainUI.onCreate 里创建）
标题栏（ActionBar）在 LauncherUI/HomeUI 里；MainUI 的 Fragment 内容区位于其下方，其会话列表支持  u5.addHeaderView(View) ，因此在列表顶部注入一行按钮 = 标题栏/搜索栏下方，天然贴合需求。
2. 会话列表数据链路（过滤挂载点）
jo5.l0 接口: getCount() / getItem(int)->storage.k4 / getView / notifyDataSetChanged / C3 / E6 / x0 / a5
├─ 新路径（默认）po5.u  extends zs3.c0  extends com.tencent.mm.view.recyclerview.WxRecyclerAdapter
│     zs3.c0.I = ConcurrentMvvmList ；I.r = ArrayList<jo5.z>（与 WxRecyclerAdapter.data 同一引用）
│     getCount() = I.r.size() ;  getItem(i) = ((jo5.z) I.r.get(i)).d
│     刷新回调 : po5.u.q(zs3.s1)（MvvmList 变更通知，s1.a=新全量数据）
│     数据源   : po5.l（implements jo5.e）→ jo5.e.g(jo5.m0) → jo5.n0.b（首屏30条）
└─ 旧路径 jo5.y0（ListView）
      q = jo5.f : a=计数 / b=SparseArray(位置→用户名) / c=HashMap(用户名→k4) / d=ArrayList<jo5.z>
      getCount()=q.a ; getItem(i)=q.c.get(q.b.get(i)) ; getView()=q.d.get(i)
      notifyDataSetChanged() → q.a(mvvmList.o) 重建索引 → super.notifyDataSetChanged()
3. 会话数据模型与分组判断依据
•  
会话模型： com.tencent.mm.storage.k4 （链： k4 → vp.a → im.j2 → qf5.f0 ）
•  
★ 核心字段： im.j2.i1()  =  field_username （会话对方用户名）
•  
辅助： im.j2.b1()  = 未读数、 im.j2.f1()  = 免打扰未读数
•  
列表项包装： jo5.z （ .d =k4， .v() =用户名）
•  
分组规则（基于用户名）：
•  
群聊： endsWith("@chatroom") / "@im.chatroom" / "@lbsroom" 
•  
服务号/公众号： startsWith("gh_") （服务号+订阅号等均为 gh_ 前缀）
•  
好友：非群聊 && 非 gh_ && 非系统账号
•  
系统账号：filehelper / floatbottle / medianote / qqmail / tmessage / fmessage / weibo / officialaccounts / newsapp / weixin / 及  @openim/@kefu.openim/@qqim/@pdcallback/@stranger  等
4. 汇总 Hook 点（三处覆盖新旧两条路径）
 com.tencent.mm.ui.conversation.MainUI.w0(Bundle) 
after
取  this.o(u5).addHeaderView(按钮栏)  注入 UI；缓存  this.w(jo5.l0) 
 po5.u.q(zs3.s1) 
after
缓存全量列表；过滤开启时原地重建  I.r  +  notifyDataSetChanged 
 jo5.f.a(List) 
before
缓存全量；过滤开启时把参数替换为过滤后列表（onCreate/notifyDataSetChanged 均触发）
按钮点击 → 修改全局  mode  →  applyFilterToAdapter() ：
•  
新路径： I.r.clear()+addAll(filterList)  →  notifyDataSetChanged() 
•  
旧路径： notifyDataSetChanged() （内部经  jo5.f.a  before-hook 再次过滤）
二、二次审查结论与补全
复查确认的完整性要点：
1.  
双路径完整：默认 RecyclerView（po5.u）路径 + 旧 ListView（jo5.y0）路径均被覆盖，且  data  与  I.r  为同一引用（通过  zs3.c0  构造器 smali 确认），过滤不矛盾；
2.  
点击一致性：直接过滤数据列表（而非拦截 View 层），因此点击/长按打开的是正确会话；
3.  
实时性：新消息/置顶/删除等触发  po5.u.q  /  jo5.f.a  时自动重过滤，不漏不串；
4.  
首次加载安全： MainUI.w0  时预缓存 fullCache，防止“q 未回调前点击”丢数据；
5.  
线程安全： notifyDataSetChanged  统一切回主线程；
6.  
健壮性：所有反射均 try-catch，类缺失自动跳过不崩溃。
已知边界（已在代码注释中说明）：“服务号”与“订阅号”无法仅凭会话用户名区分（两者都是  gh_  前缀，需查询联系人 BizInfo 才能拆分），当前将 gh_ 统一归入“服务号”标签；如需严格拆分，可扩展  isBiz()  内部调用  com.tencent.mm.plugin.brandservice  的 BizInfo 查询。
三、导出文件（可直接使用）
已创建并写入插件  wx_group_filter （ main.java ，约 15.5KB，顶部含完整链路分析注释），包含：标题栏下方按钮行（全部/群聊/好友/服务号）、三处 Hook、双路径过滤、实时刷新、Toast/日志反馈。
使用说明
1.  
在 LSPilot 中启用插件  wx_group_filter （作用于  com.tencent.mm ）；
2.  
重启微信（或回到主页）；
3.  
主页“微信”Tab 搜索栏下方出现  [全部] [群聊] [好友] [服务号]  一行按钮，点击即过滤，无需重启；
4.  
日志：Logcat  MicroMsg  + 插件目录  log/<日期>.log ，可用  read_plugin("wx_group_filter")  查看/二次编辑。
如需调整按钮（如加“订阅号”、改配色、改过滤规则），直接改  wx_group_filter/main.java  中的  titles/modes 、 isGroup/isBiz/isSystem  即可。