# WeKit 微信设置界面注入方案技术分析

> 分析日期：2025-01  
> 分析目标：`dev.ujhhgtg.wekit`（WeKit）  
> 核心类：`dev.ujhhgtg.wekit.features.api.ui.WeSettingsInjector`

---

## 一、整体架构概览

WeKit 向微信设置界面注入入口，采用 **Xposed Hook + DexKit 动态类/方法定位 + 动态字节码生成代理类** 的三合一方案。

核心入口是 `WeSettingsInjector`（单例），由其 `D()` 方法驱动三路并行注入：

```
                        WeSettingsInjector.D()
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
        I()                  J()                  K()
   Hook LauncherUI      Hook 旧版设置UI       Hook 新版设置UI
   (外部唤起入口)        (兼容旧微信)          (主力方案 ★)
          │                    │                    │
    Intent "WeKit"        IconPreference    WeChatSettingsManager
    extra 触发跳转        直插Preference     createItem + install
          │                    │                    │
          └────────────────────┼────────────────────┘
                               │
                     SettingsActivity
```

---

## 二、路 1：Hook LauncherUI（外部唤起入口）

### 2.1 方法：`I()`

```java
public static void I() {
    kn2 kn2Var = new kn2(LauncherUI.class);
    // 定位 LauncherUI.onCreate
    obj.Y = "onCreate";  // v65 是 DexKit 的 MethodFinder 约束对象
    qr6 K = kn2Var.K(obj);
    // Hook onCreate → rb9(19)
    o40.A(weSettingsInjector, K, new rb9(19));

    // 定位 LauncherUI.onNewIntent
    obj2.Y = "onNewIntent";
    qr6 K2 = kn2Var.K(obj2);
    // Hook onNewIntent → rb9(20)
    o40.A(weSettingsInjector, K2, new rb9(20));
}
```

### 2.2 Hook 回调行为

| Hook 点 | 回调 ID | 触发时机 | 行为 |
|---------|---------|---------|------|
| `LauncherUI.onCreate` | `rb9(19)` | 主界面创建 | 检查 `intent.getStringExtra("WeKit")`，非空则 `Handler.postDelayed(500ms)` 延迟触发 |
| `LauncherUI.onNewIntent` | `rb9(20)` | 主界面收到新 Intent | 检查 `intent.getStringExtra("WeKit")`，非空则立即调用 `WeSettingsInjector.L(activity)` |

### 2.3 跳转方法

```java
public static void L(Context context) {
    context.startActivity(new Intent(context, SettingsActivity.class));
}
```

即 `dev.ujhhgtg.wekit.activity.settings.SettingsActivity`

### 2.4 用途

**外部唤起**：只要构造一个带 `"WeKit"` extra 的 Intent 发给 LauncherUI，即可触发打开 WeKit 设置页。这是 Xposed 模块常用的"从宿主唤起自己"的手法。

---

## 三、路 2：Hook 旧版设置界面（兼容旧微信）

### 3.1 方法：`J()`

```java
public final void J() {
    Class e = oy0.e("com.tencent.mm.plugin.setting.ui.setting.SettingsUI");
    if (e == null) {
        k99.k("WeSettingsInjector", "legacy settings class not found, skipping");
        return;
    }
    // Hook SettingsUI.initView (0 参数版本)
    kn2.V("initView").e0(0);
    s(K.a(), new rb9(24));

    // Hook SettingsUI.onPreferenceTreeClick
    kn2.V("onPreferenceTreeClick");
    o40.A(this, K2, new rb9(25));
}
```

不存在则静默跳过。

### 3.2 `rb9(24)` — initView 回调（旧版插入条目）

```java
// 伪代码还原
SettingsUI settingsUI = (SettingsUI) methodHookParam.thisObject;

// 创建 IconPreference
IconPreference iconPreference = new IconPreference(settingsUI);
// 设置 key = "wekit_settings_entry"
t32Var.e().invoke(iconPreference, "wekit_settings_entry");
// 设置标题 = "WeKit 设置"
t32Var2.e().invoke(iconPreference, "WeKit 设置");

// 反射调用 getPreferenceScreen() → addPref(iconPreference, 0)
Method method = findMethod("getPreferenceScreen");
Object prefScreen = method.invoke(settingsUI);
addPrefMethod.invoke(prefScreen, iconPreference, 0);
```

### 3.3 `rb9(25)` — onPreferenceTreeClick 回调

检查点击的 Preference key 是否为 `"wekit_settings_entry"`，是则调用 `WeSettingsInjector.L(activity)`。

---

## 四、路 3：Hook 新版设置界面（主力方案 ★）

这是最核心、最精巧的方案。分三个阶段：

### 阶段 A：DexKit 动态定位微信内部类

`WeSettingsInjector` 在 `static{}` 块中用 DexKit 的 DSL 预设了 12 个查找委托：

```java
// DexKit DSL 查找委托（在 static{} 中初始化）
static i  → methodSetKey         // setKey(String) 方法
static j  → methodSetTitle       // setTitle(CharSequence) 方法
static k  → methodGetKey         // getKey() 方法
static l  → methodAddPref        // addPref(Preference, int) 方法
static m  → classSettingItemClassesProvider  // 设置项类提供者
static n  → classBaseSettingItem             // 设置项基类
static o  → classBaseSettingSwitchItem       // 开关项基类
static p  → classSettingLocation             // 位置描述类
static q  → methodSettingGroupAccountInfoGetStringId
static r  → methodSettingGroupAccountInfoReturns1
static s  → methodSettingGroupPersonalInfoGetGroupNameResId
static t  → methodResourceHelperGetStringById
```

### 阶段 B：反射分析 SettingGroupAccountInfo

`K()` 方法遍历 `SettingGroupAccountInfo.class.getDeclaredMethods()`，通过**返回类型和参数个数**自动匹配关键方法名：

| 匹配规则 | 存入字段 | 含义 |
|---------|---------|------|
| 返回类型是 `Class` | `u` → `mGetPageGroupItemClass` | 获取页面分组类 |
| 3 个参数 | `w` → `mOnClick` | 点击回调 |
| 返回类型匹配 `classSettingLocation` | `y` → `mGetSettingLocation` | 获取位置信息 |
| （继续匹配其他方法） | `v` → `mGetLevel` | 获取层级 |
| | `z` → `mGetNameResId` | 获取名称资源ID |
| | `A` → `mGetGroupNameResId` | 获取分组名资源ID |
| | `B` → `mGetSwitchState` | 获取开关状态 |
| | `C` → `mGetSwitchProperty` | 获取开关属性 |

然后将所有这些动态定位到的类和方法传入 `WeChatSettingsManager` 构造器。

### 阶段 C：WeChatSettingsManager 核心机制

#### 4.1 构造函数参数（共 16 个）

```
位置  参数                            说明
───────────────────────────────────────────────────
 1    classBaseSettingItem            设置项基类
 2    classBaseSettingSwitchItem      开关基类
 3    classSettingLocation            位置类
 4    classSettingItemClassesProvider 设置项类提供者
 5    classBaseSettingPrefUI          Preference UI 基类
 6    classBaseSettingUI              设置 UI 基类
 7    methodResourceHelperGetStringById 资源获取方法
 8    mGetPageGroupItemClass          方法名
 9    mGetLevel                       方法名
10    mOnClick                        方法名
11    mGetKey                         方法名
12    mGetSettingLocation             方法名
13    mGetNameResId                   方法名
14    mGetGroupNameResId              方法名
15    mGetSwitchState                 方法名
16    mGetSwitchProperty              方法名
```

#### 4.2 `createItem(j43)` — 动态生成代理类

`rb9(13)` 作为参数传入 → 由 `n89`（数据容器）承载配置：

```
n89 字段        值                                 含义
────────────────────────────────────────────────────────
a (key)        "SettingGroup_Main_WeKitTest1"     设置项唯一标识
b (标题)       "WeKit 设置"                        显示的标题文字
c (副标题)     "插件"                              显示的摘要文字
d (页面类)     SettingGroupMain.class              所属 SettingsGroup
e (前置参照)   SettingAdditionHeaderSearch.class   排在哪个项后面
f (父分组)     SettingGroupPersonalInfo.class      插入到哪个分组 ★
g (点击回调)   sb9(2)                              点击行为
```

**`sb9(2)` 点击回调**：
```java
case 2:
    Activity activity = (Activity) obj;
    WeSettingsInjector.L(activity);  // 打开 SettingsActivity
```

`createItem()` 内部做：

1. **分配虚拟资源 ID**：`allocateString("WeKit 设置")` 返回一个虚拟 `resId`
2. **选取 marker 接口**：从预置的 `markers[]` 数组按 `itemIndexCounter` 取，用于区分不同 item 的代理类
3. **动态生成 class**：使用 `kj6`（ASM/ByteBuddy 包装）生成一个继承 `BaseSettingItem` + 实现 marker 接口的类，写入到 `generated_proxy_classes/` 目录
4. **所有 getter 代理**：`mGetKey` → 返回 key 字符串；`mOnClick` → 触发回调；`mGetLevel` → 返回层级值；等等
5. **存入 `registeredItems`**：`CopyOnWriteArrayList<h79>`，其中 `h79` 包含 `n89`（配置）+ `Class`（代理类）

#### 4.3 `install()` — 三 Hook 注入

```java
public final void install() {
    // Hook ①：拦截设置项类提供者的方法
    // 目标：classSettingItemClassesProvider 的某个方法
    // 回调：o89(this, 0)
    XposedBridge.hookMethod(method, new o89(this, 0));

    // Hook ②：拦截 BaseSettingPrefUI.superImportUIComponents
    // 目标：classBaseSettingPrefUI 的 "superImportUIComponents"
    // 回调：o89(this, 1)
    XposedBridge.hookMethod(method, new o89(this, 1));

    // Hook ③：拦截 BaseSettingUI.onDestroy
    // 目标：classBaseSettingUI 的 "onDestroy"
    // 回调：o89(this, 2)
    XposedBridge.hookMethod(method, new o89(this, 2));
}
```

### 4.4 三个 Hook 回调详解

#### `o89(0)`：拦截 SettingItemClassesProvider

```java
// afterHookedMethod
void case_0(MethodHookParam param) {
    Map result = (Map) param.getResult();  // Map<GroupClass, List<ItemClass>>
    // 遍历 registeredItems
    for (h79 item : registeredItems) {
        Class groupClass = item.a.d;  // 目标分组（SettingGroupPersonalInfo）
        Class proxyClass = item.b;    // 动态生成的代理类
        // 把 proxyClass 加到 groupClass 对应的 List 中
        map.get(groupClass).add(proxyClass);
    }
    param.setResult(map);
}
```

这样微信在构建设置页时就会"看到" WeKit 的条目。

#### `o89(1)`：拦截 superImportUIComponents

```java
// afterHookedMethod
void case_1(MethodHookParam param) {
    String name = param.thisObject.getClass().getName();
    // 只在 MainSettingsUI 或 CommonSettingsUI 生效
    if (name 匹配 "MainSettingsUI" 或 "CommonSettingsUI") {
        HashSet set = (HashSet) param.args[0];
        // 把 WeKit 代理类加到组件集合中
        for (h79 item : registeredItems) {
            set.add(item.b);
        }
        // ★ 同时 Hook Context.getString(int) 做字符串注入
        XposedBridge.hookMethod(Context.getString, o89(this, 3));
        // ★ 同时 Hook ResourceHelper.getString(int, int)
        XposedBridge.hookMethod(resourceHelperGetStringById, o89(this, 4));
    }
}
```

#### `o89(3)` 和 `o89(4)`：字符串池注入

```java
// beforeHookedMethod - Context.getString(int)
void case_3(MethodHookParam param) {
    int resId = (Integer) param.args[0];
    String str = stringPool.get(resId);  // ConcurrentHashMap<Integer, String>
    if (str != null) param.setResult(str);
}

// beforeHookedMethod - ResourceHelper.getString(int, int)
void case_4(MethodHookParam param) {
    int resId = (Integer) param.args[1];
    String str = stringPool.get(resId);
    if (str != null) param.setResult(str);
}
```

由于动态生成的代理类中引用的"资源 ID"是 WeKit 虚拟分配的，微信的字符串加载会失败。这里通过 Hook `getString` 方法，从 stringPool 中拦截并返回正确字符串。

#### `o89(2)`：拦截 onDestroy（清理）

```java
// afterHookedMethod
void case_2(MethodHookParam param) {
    String name = param.thisObject.getClass().getName();
    if (name 匹配 "MainSettingsUI" 或 "CommonSettingsUI") {
        // 解注册字符串 Hook
        contextGetStringUnhook.unhook();
        resourcesGetStringUnhook.unhook();
    }
}
```

防止设置页面销毁后 Hook 仍然存活导致内存泄漏或行为异常。

---

## 五、数据流总图

```
WeSettingsInjector.D()
│
├─ I(): Hook LauncherUI
│    └─ intent["WeKit"] != null → startActivity(SettingsActivity)
│
├─ J(): Hook 旧版 SettingsUI (兼容)
│    ├─ initView → new IconPreference → getPreferenceScreen().addPref()
│    └─ onPreferenceTreeClick → key=="wekit_settings_entry" → 跳转
│
└─ K(): 新版主力 ★
     │
     ├─ 反射 SettingGroupAccountInfo.getDeclaredMethods()
     │    └─ 按返回类型 + 参数数 自动匹配方法名
     │         → mGetPageGroupItemClass, mGetLevel, mOnClick, ...
     │
     ├─ new WeChatSettingsManager(16个参数)
     │    │
     │    ├─ createItem(rb9(13)) 
     │    │    ├─ n89 配置:
     │    │    │    key   = "SettingGroup_Main_WeKitTest1"
     │    │    │    标题  = "WeKit 设置"
     │    │    │    副标题 = "插件"
     │    │    │    父分组 = SettingGroupPersonalInfo
     │    │    │    前置   = SettingAdditionHeaderSearch
     │    │    │    点击   = → SettingsActivity
     │    │    │
     │    │    ├─ allocateString() → 虚拟资源ID
     │    │    ├─ kj6 动态生成 BaseSettingItem 子类
     │    │    └─ 写入 generated_proxy_classes/v*/
     │    │
     │    └─ install()
     │         ├─ Hook ①: SettingItemClassesProvider
     │         │    └─ 向 Map<分组, List<条目>> 注入代理类
     │         ├─ Hook ②: superImportUIComponents
     │         │    ├─ 向 HashSet 注入代理类
     │         │    └─ 附加 Hook Context.getString + ResourceHelper.getString
     │         │         └─ stringPool 拦截虚拟资源ID → 返回真实字符串
     │         └─ Hook ③: onDestroy
     │              └─ 清理字符串 Hook
     │
     └─ 生命周期注册: t59.h.addIfAbsent(this)
          └─ D() 返回后监听模块卸载事件 → C() 移除
```

---

## 六、关键参数速查表

### 6.1 触发参数

| 参数 | 值 |
|------|-----|
| Intent extra key | `"WeKit"` |
| extra value | 任意非 null 值即可 |
| 目标 Activity | `dev.ujhhgtg.wekit.activity.settings.SettingsActivity` |

### 6.2 设置项参数

| 参数 | 值 |
|------|-----|
| 设置项 key | `"SettingGroup_Main_WeKitTest1"` |
| 标题 | `"WeKit 设置"` |
| 副标题 | `"插件"` |
| 插入分组 | `SettingGroupPersonalInfo`（个人信息组） |
| 前置参照项 | `SettingAdditionHeaderSearch` |
| 所属 SettingGroup | `SettingGroupMain` |

### 6.3 兼容性参数

| 微信版本 | 设置类 | 方案 |
|---------|--------|------|
| 新版 | `com.tencent.mm.plugin.setting.ui.setting_new.*` | 路 3（K()） |
| 旧版 | `com.tencent.mm.plugin.setting.ui.setting.SettingsUI` | 路 2（J()） |
| 所有版本 | `com.tencent.mm.ui.LauncherUI` | 路 1（I()） |

### 6.4 Hook 点汇总

| Hook # | 目标类/方法 | 回调类 | 作用 |
|--------|-----------|--------|------|
| 1 | `LauncherUI.onCreate` | `rb9(19)` | 延迟触发入口 |
| 2 | `LauncherUI.onNewIntent` | `rb9(20)` | 即时触发入口 |
| 3 | `SettingsUI.initView` | `rb9(24)` | 旧版插入条目 |
| 4 | `SettingsUI.onPreferenceTreeClick` | `rb9(25)` | 旧版点击响应 |
| 5 | `SettingItemClassesProvider.get*` | `o89(0)` | 新版注入 Map |
| 6 | `BaseSettingPrefUI.superImportUIComponents` | `o89(1)` | 新版注入 HashSet + 附加字符串 Hook |
| 7 | `BaseSettingUI.onDestroy` | `o89(2)` | 清理字符串 Hook |
| 8 | `Context.getString(int)` | `o89(3)` | 字符串池拦截 |
| 9 | `ResourceHelper.getString(int,int)` | `o89(4)` | 字符串池拦截 |

---

## 七、方案亮点总结

### 7.1 零硬编码
所有微信内部类名、方法名均通过 DexKit 动态查找，不依赖任何硬编码的混淆类名，跨版本兼容性强。

### 7.2 反射智能匹配
`SettingGroupAccountInfo` 的方法匹配不是按名称，而是按**返回类型 + 参数个数**推断语义。即使未来微信重命名这些方法，只要类型签名不变就能正确匹配。

### 7.3 动态代理类
每个设置项通过运行时字节码生成独立的代理类，完全符合微信设置框架的类型体系，不会被类型检查拒绝。

### 7.4 虚拟资源 ID + 字符串池
通过 `allocateString` + Hook `getString` 的方式，让动态生成的类可以"引用"不存在的资源 ID 而不会崩溃，优雅解决了资源引用问题。

### 7.5 生命周期管理
Hook 在 `onDestroy` 时自动解注册，防止内存泄漏；`t59.h` 监听模块卸载事件，确保 Xposed 模块被禁用时能正确清理。

### 7.6 新旧兼容
三路并行注入，新版本走主力方案（路 3），旧版本降级到 Preference 方案（路 2），找不到则静默跳过不报错。
