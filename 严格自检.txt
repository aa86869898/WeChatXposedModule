全局完整扫描整个项目全部源码，做全量Bug、异常、兼容性自检，严格遵守下面全部要求：
 
1. 基准规则：业务逻辑1:1参照【乐少助手旧版完整源码】，md文档仅作辅助，冲突一律以原版源码为准，禁止私自新增、删减、优化任何原有功能。
​
2. 扫描范围：所有Hook代码、注入逻辑、数据库WCDB读写、异步线程、UI弹窗、Dialog栈导航、开关控件、TTS语音播报、长按菜单、微信设置页入口、版本判断代码、混淆配置文件。
​
3. Bug排查清单逐一校验：
① 微信8.0.72~8.0.76+适配问题，类名混淆变更导致的ClassNotFoundException、Hook失效
② 主线程卡顿问题：数据库查询、数据加载有没有放到异步执行，防止界面卡死闪退
③ 视图注入BUG：设置页入口层级错误、挂载位置异常，判断ViewRootImpl强转崩溃风险
④ 开关、星星收藏按钮逻辑：功能开启/关闭、置顶收藏逻辑是否正常，状态持久化是否存在漏洞
⑤ TTS消息播报全部类型校验：文字、语音、图片、红包、转账、表情、群聊播报有没有遗漏逻辑
⑥ 好友列表过滤逻辑：是否正确剔除群成员、公众号，只展示真实好友，筛选逻辑无漏洞
⑦ 版本获取异常wxVersionName报错修复，处理PackageManager反射失败的兼容方案
⑧ R8/ProGuard混淆规则校验：所有Hook类、实体类、匿名内部类是否添加keep保护，release打包后不会丢失方法导致崩溃
⑨ 日志管理：Release包多余调试日志清理，异常捕获补全，空指针、数组越界、判空缺失检查
⑩ AntiDetectionHook代码是否过于激进，存在闪退风险，改为轻量安全版本
​
4. 输出格式：
分点列出每一项存在的问题 + 问题代码位置 + 修复后的完整替换代码；不要笼统描述，直接给出修改方案；修复完成后再执行一次全量自检，确认全部问题处理完毕，最后编译打包apk。
