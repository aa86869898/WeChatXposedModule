# 微信 Xposed 群发功能 · 全链路逆向分析报告

> 目标应用：com.tencent.mm (微信 8.x 当前 APK)
> 能力范围：向多个群批量发送 文本/图片/视频/语音/名片/视频号/小程序 七类消息
> 分析方法：字符串锚点 → 类定位 → 方法降级 → Smali/Java 取证
> 本报告所有类名/方法/字段均来自 DexKit + jadx/baksmali 实证，标注 ✅=已反编译实证，⚠️=运行期待确认

---

## 一、整条发送链路总览

```
入口(UI层: ChatFooter / SendMsgMgr qs5.v5)
  → 类型分派 (qs5.v5 各发送方法 或 v51 新PPC框架)
  → 构造消息对象 (com.tencent.mm.storage.e9 / dx0.r AppMessage)
  → 写入消息表 + 触发网络上传/发送 (b41.aa / f9 存储 + k0 AppMsgLogic / 各CDN scene)
  → 消息通过 g(场景) 发送完成后回调更新
```

多群本质：微信官方群发 = 对目标群列表（多个 xxx@chatroom）逐个走同一套发送代码，或调用方传"逗号分隔群列表"由内部循环。

核心服务定位器：`ph5.n0.c(X.class)` / `n0.c(X.class)`；网络场景执行：`h9.e().g(scene)`；消息存储服务：`h9.b().v()`（→ vn3.m0/f9）。

## 二、核心发送管理器 SendMsgMgr (qs5.v5 = MicroMsg.SendMsgMgr)

✅ 已反编译完整方法列表（日志 tag `MicroMsg.SendMsgMgr`，类签名 `public class v5 extends w implements a1`）

| 方法 | 逻辑标签 | 用途 |
|---|---|---|
| `b(Context,toUser,fileName,i,...,k7,d)` | sendImg | 发送图片(CDN) |
| `bj(Context,int,List,x0)` | 批量场景 | 注册场景9146+队列 |
| `cj(String,byte[],String,String)` | sendAppMsg 简版 | 发AppMsg |
| `dj(String,byte[],String,String,String,MsgIdTalker,String,Z,String)` | sendAppMsg | 发AppMsg(小程序/视频号/文件) |
| `ej(String,String,Z,yl)` | sendContactCard 新 | 发名片(新服务优先) |
| `fj(String,String,Z,yl)` | sendContactCard 旧 | 发名片(旧方法) |
| `gj(String,String,String,Z)` | 多目标发送 | str2 逗号分隔多群循环 |
| `hj(String,String,String)` | 多目标文本 | SendMsgEvent 广播逐群发 |
| `ij/jj/kj(...)` | sendImg 变体 | 图片+ScanCode/SourceImgInfo |
| `mj/nj/oj/pj(...)` | 通用文本发送 | 走 PPC v51 新框架 |
| `qj/rj(...)` | 相册名片 | send photo account name card |
| `sj/tj/uj/vj(...)` | sendVedio | 视频(CDN两种路径) |

### 2.1 多群批量关键逻辑（✅ 已反编译）
```java
// gj/hj/ej/fj/rj 方法内（已实证）：
ArrayList P1 = y8.P1(str2.split(","));   // 逗号分隔 = 多个群
for (int i = 0; i < P1.size(); i++) {
    String str4 = (String) P1.get(i);
    // 对每个群单独走 r1 builder / SendMsgEvent
}
```
→ **Xposed 传逗号分隔群列表即群发**。

### 2.2 文本发送内部（oj 方法 ✅ Smali 实证）
```java
v51.s1.a(群用户名)            // 新建 PPC builder
    .h(群).e(文本).i(msgType) // e=内容字段, i=类型
    → .a()=n1 → n1.a()=执行 CGI
// 类型由 b41.d2.C(username) 判定: @chatroom→1, 公众号→11, 其他→36/1
```

## 三、按消息类型链路详解

### 3.1 文本消息
- ✅ 入口A(SendMsgMgr, 无UI依赖): `qs5.v5.mj/nj/oj/pj(toUser, content, type, flag)`；多群 `hj(atStr, usersCsv, extra)`
- ✅ 入口B(UI链路): `ChatFooter.d(chatFooter, text, z)` → `V0(str,num,hashMap)` → `k4`组件 → `om.SendTextComponent.w0` → `pm.run` → 新框架 `oh0.c.a(toUser,content,type,...)` 或 PPC `v51.s1`
- 相关类:
  - `com.tencent.mm.pluginsdk.ui.chat.ChatFooter`（输入框, 334字段）
  - `com.tencent.mm.ui.chatting.k4`（ChattingFooter, 实现 a6 接口, s/d 发文本）
  - `com.tencent.mm.ui.chatting.component.om`（SendTextComponent, A0/y0/w0）
  - `com.tencent.mm.ui.chatting.component.pm`（SendText Runnable）
  - `oh0.c`（SendTextTask 新框架, 日志 MicroMsg.SendTextLogic）
  - `v51.r1/s1/n1`（PPC builder→CGI）
  - `b41.d2`（C()=会话类型判定, z()=群列表, j()=联系人列表）

### 3.2 图片消息
- ✅ `qs5.v5.b(Context,toUser,fileName,i,...)` = sendImg 核心
- ✅ 变体 `ij/jj(ScanCode)/kj(SourceImgInfo)`
- 链路: 检查sdcard → 构造 `da0.g` 上传参数(i0: a=4原图, o=k7, j=...) → `((d1)服务).rj(g)` CDN上传 → `qs5.a6` 回调
- ⚠️ CDN类 `kt.d1`(R1), `w90.i0`(i0) 跨版本易变; 绕CDN直接塞 e9(type=3) 更通用

### 3.3 视频消息
- ✅ `qs5.v5.sj/tj(Context,toUser,file,thumb,i,i2,qn6,..)` = sendVedio 核心；uj/vj 变体
- 两条路径:
  - 新: `((ab5.s)服务).cj(new qi0.w2(...), toUser)` CDN视频上传
  - 旧: `b41.d2` 异步任务逐个字段上传
- ⚠️ 依赖 `l36.g` 对话框等待、`p87` 参数, 建议直接构造 e9(type=43) 入存储

### 3.4 语音消息（type=34）
- ✅ 新框架(PPC): `fj0.x`(VoiceMsgSendTask 主类) → 阶段 C(prepare)/P(initVoice)/Q(beginVoice)/k+l(sendCgi)/u(cdn上传)/B(updateMsg)
  - 入口协程 `fj0.j`: 构造 e9(type=34) + voiceinfo(c1) → `k0.F0.r(e9)` 插消息 → 拿 localId → 入 voiceinfo
  - `fj0.g` = newSendVoice_ 入口; `fj0.i/m/o/s/u/v` = 重发/校验/上传子阶段
- ✅ 旧框架: `yl.y0`(SceneVoiceService) 轮询发送; `v61.o`(NetSceneUploadVoice→/cgi-bin/micromsg-bin/uploadvoice); `v61.k`(NetSceneDownloadVoice); `ln4.g`(uploadvoicerecognize)
- ✅ 语音文件命名规则(实证):
  - `v61.m1.b1(user, prefix)` = prefix + `b41.x1.a(user, time)`
  - `b41.x1.a`: `SimpleDateFormat("ssHHmmMMddyy")` + (无用户名?"fffffff":md5(user).substring(0,7)) + `%04x(time%65535)` + `(time%7)+100`
  - 前缀: amr_/spx_/silk_/opus_（`v61.b1.c` 按文件头识别; 魔数 #!AMR\n / #!SILK_V3 / #!OPUS_RAW_V1）
- ✅ voiceinfo 表结构(v61.m1): FileName(PK),User,MsgId,NetOffset,FileNowSize,TotalLen,Status,CreateTime,LastModifyTime,ClientId,MasterBufId,MsgFlag,MsgSeq,MsgSource,MsgTalker,VoiceFlag,VoiceInfoExt,checksum
- ✅ v61.c1 字段: a=flag,b=fileName,c=user,d=clientMsgId,e=svrId,f=length,g=cancelFlag,h=voiceLen,i=status,j=createTime,k=lastModifyTime,l=duration,m=msgId,n=fromusername,p=voiceformat,x=talker
- ✅ 语音XML键(v61.f1.k): .msg.voicemsg.$length/$clientmsgid/$endflag/$cancelflag/$voicelength/$fromusername/$forwardflag/$voiceformat/$bufid/$voiceurl/$aeskey

### 3.5 名片消息（type=42个人/66群）
- ✅ 双路径(qs5.v5.ej/fj):
  - 新(默认): e10.d(IContactCardMsgFeatureService) → jj(new e10.m(toUser,contentUser)) 创建任务 → kj(task) sendSync 执行
    - e10.m 构造器: a=toUserName, b=contentUserName, 后续 c=Z,d=[B,e=String,f=sp3/g,g-i=状态
    - e10.h = ContactCardMsgSendTask(继承 up3/c) 阶段: C prepare(buildCardXmlContent→insertMsg) → B updateMsg → k/l sendCgi
  - 旧: qb.b(username, ylVar) 生成名片XML → v51.r1 builder → 类型 y3.o4(群)?66:42
- ✅ 名片XML模板(qb.b 实证, 17参数, scene=17):
<msg username="%s" nickname="%s" alias="%s" fullpy="%s" shortpy="%s"
     imagestatus="%d" scene="17" province="%s" city="%s" sign="%s"
     percard="%d" sex="%d" certflag="%d" certinfo="%s" certinfoext=""
     brandIconUrl="%s" brandHomeUrl="" brandSubscriptConfigUrl="" brandFlags=""
     regionCode="%s" biznamecardinfo="%s" brandType="%d"/>
- ✅ OpenIM/企业微信风格:
<msg bigheadimgurl="%s" smallheadimgurl="%s" username="%s" nickname="%s" sex="%d"
     openimappid="%s" openimdesc="%s" openimdescicon="%s"/>
- ✅ 相册/企业特殊名片: dx0.r 构造, rVar.i=132, AppMsg包裹

### 3.6 视频号卡片(Finder)
- ✅ 本质 = AppMsg(type=50/105 或 75/59/72) + content 内含 <finder><type>N</type><detail>...</detail></finder>
- ✅ 外层封装 a53.t5.a(i,str)=<finder><type>i</type><detail>str</detail></finder>; 判定键 .msg.appmsg.finder.type
- ✅ finderFeed 序列化 a53.t5.f(b03)(b03=pc5.b03) 完整标签:
<finderFeed>
  <objectId>str0</objectId> <objectNonceId>str8</objectNonceId> <feedType>int5</feedType>
  <nickname>str2</nickname> <username>str1</username> <avatar>str3</avatar>
  <desc>str4</desc> <mediaCount>int6</mediaCount> <localId>long9</localId>
  <authIconType>int13</authIconType> <authIconUrl>str14</authIconUrl>
  <mediaList>  list(7)循环
    <media><mediaType>int0</mediaType><url>str1</url><thumbUrl>str2</thumbUrl>
    <coverUrl>str8</coverUrl><fullCoverUrl>str6</fullCoverUrl><fullClipInset>str7</fullClipInset>
    <width>float3</width><height>float4</height><videoPlayDuration>int?</videoPlayDuration></media>
  </mediaList>
  其他变体: 直播/话题/合集/POI 见 a53.t5.e/h/g
</finderFeed>
- ✅ 分享到群链路(l9.L 实证):
intent extras:
  Retr_Msg_Type = 21/25/27/32/39 (按类型)
  Multi_Retr = true
  Retr_Msg_content = dx0.r.u() 生成 AppMsg XML
  Retr_go_to_chattingUI / Retr_show_success_tips
  → 跳 com.tencent.mm.ui.transmit.MsgRetransmitUI → 按 Select_Conv_User/ToUsername 拆群逐群 sendAppMsg
多群名单: SendMsgUsernames (逗号/分号分隔, 在 a9/b8/c8/d8/g8/i8/k8 的 mmOnActivityResult 实证)
落库表: FinderShareInfo(username, shareTime)
- 管理器: com.tencent.mm.plugin.finder.assist.l9 (日志 Finder.FinderShareManager), A()/B()=share finderObject

### 3.7 小程序
- ✅ AppMsg(type=49) + dx0.r 解析；辅助类 com.tencent.mm.modelmulti.WxaInfo
- ✅ 小程序专用piece dx0.a(AppContentAppBrandPiece) 字段键(实证):
  - .msg.appmsg.weappinfo.username/pagepath/appid/version/type
  - .msg.appmsg.weappinfo.pkginfo.type/md5
  - .msg.appmsg.weappinfo.weappiconurl/shareId/sharekey
  - .msg.appmsg.weappinfo.nativeappinfo.appnamemultilanguagekey/sourceiconurl/...
  - .msg.appmsg.weappinfo.disableforward/secflagforsinglepagemode
  - .msg.appmsg.weappinfo.appservicetype/...

## 四、AppMessage (dx0.r) 完整字段与 XML

✅ 已反编译：dx0.r = AppMessage 主模型(192字段, 日志 MicroMsg.AppMessage)，xml生成入口 u()→t()→c()，解析入口 d()/v()。

### 4.1 XML 生成出口（实证）
```java
u(): <msg> + t() + </msg>     // t() 生成 <appmsg appid=".." sdkver="..">
t(): <appmsg appid=d sdkver=e> + c()公共字段 + s.a(type).c()专用piece + </appmsg>
// type==50||105 时打印 "FinderNameCard xml content(type=..)"
```

### 4.2 公共字段（c() 实证，字段名已对齐）
```xml
<appmsg appid="d" sdkver="e">
  <title>f</title> <des>g</des> <username>n0</username>
  <action>h(默认view)</action> <type>i</type> <showtype>K</showtype>
  <content>n</content> <msgcontent>o</msgcontent>
  <url>k</url> <lowurl>l</lowurl> <forwardflag>F2</forwardflag>
  <dataurl>W</dataurl> <lowdataurl>X</lowdataurl> <contentattr>w</contentattr>
  <syncrecordctx>z2</syncrecordctx>
  <streamvideo>...b2..i2...</streamvideo>
  <appattach>
    <attachid>q</attachid><aeskey>U</aeskey><totallen>m</totallen>
    <cdnattachurl>M</cdnattachurl> <fileext>p</fileext> <filename>j</filename>
    <cdnthumburl>N</cdnthumburl><cdnthumbmd5>P</cdnthumbmd5>
    <cdnthumblength>O</cdnthumblength><cdnthumbwidth>R</cdnthumbwidth>
    <cdnthumbheight>Q</cdnthumbheight><cdnthumbaeskey>V</cdnthumbaeskey>
    <emoticonmd5>r</emoticonmd5> <islargefilemsg>v</islargefilemsg>
    <tpurl>B</tpurl><tpthumburl>C</tpthumburl>...
  </appattach>
  <extinfo>s</extinfo>  <androidsource>t</androidsource>
  <sourceusername>x</sourceusername> <sourcedisplayname>y</sourcedisplayname>
  <thumburl>A</thumburl> <mediatagname>Y</mediatagname>
  <messageaction>Z</messageaction> <messageext>a0</messageext>
  <commenturl>z</commenturl>
</appmsg>
```

### 4.3 解析路径键（d() 实证，可直接用于构造/提取）
```
.msg.appmsg.$appid / .msg.appmsg.$sdkver / .msg.appmsg.title / .msg.appmsg.des
.msg.appmsg.username / .msg.appmsg.action / .msg.appmsg.type / .msg.appmsg.content
.msg.appmsg.msgcontent / .msg.appmsg.url / .msg.appmsg.lowurl / .msg.appmsg.forwardflag
.msg.appmsg.dataurl / .msg.appmsg.lowdataurl / .msg.appmsg.contentattr
.msg.appmsg.appattach.totallen / .attachid / .aeskey / .filename / .fileext
.msg.appmsg.appattach.cdnattachurl / .cdnthumburl / .cdnthumbmd5 / .cdnthumbaeskey
.msg.appmsg.thumburl / .mediatagname / .messageaction / .messageext / .commenturl
.msg.fromusername / .msg.commenturl / .msg.appinfo.appid / .msg.appinfo.appname
.msg.appmsg.mmreadershare.* / .msg.appmsg.mmbrandmpvideo.* (dx0.g)
.msg.appmsg.finder.type / finderFeed 各标签 (a53.t5)
.msg.appmsg.weappinfo.* (dx0.a 小程序)
.msg.appmsg.wcpayinfo.* (dx0.b/c/d 微信支付 2001/2002/2003)
.msg.appmsg.emoji.* (dx0.e) / .msg.appmsg.voice.* (dx0.f)
.msg.appmsg.location.* (地图)
```

### 4.4 文件/链接类型 dx0.g（✅ 实证，见"三、3.6"前小节）
- itemshowtype=b(-1); 视频字段 f(type)=type==5||16
- mmbrandmpvideo: vid/videourl/mpurl/thumbwidth/thumbheight/duration
- quick_reply>0 时输出

## 五、低层存储/消息对象

### 5.1 消息实体 com.tencent.mm.storage.e9（已实证关键setter）
- b1(String content) 内容; u1(String talker) 会话; t1(int stFlag); e1(long createTime)
- setType(int) 消息类型; r3(String) MsgSource; k1(int); 等 200+ 方法

### 5.2 消息类型常量（微信标准）
- 1=文本, 3=图片, 34=语音, 42=名片(个人), 43=视频, 49=AppMsg(小程序/链接/文件), 62=短视频, 66=名片(群), 68, 10000=系统

### 5.3 存储链路
- b41.aa.y(e9) = 插入消息+建会话(✅ 实证: ((c4)j1.v(c4.class)).lj().yb(e9))
- f9.yb(e9)/Bb(e9,z) = message表插入, 返回 localId (✅ 实证 yb→Bb)
- f9.Bb(✅ 137行实证): 查表→dealMsgSource→insertOrReplace→日志

## 六、群列表获取

### 6.1 群列表（✅ b41.d2.z 实证）
```java
LinkedList list = new LinkedList();
Cursor c = ((c4) j1.v(c4.class)).cj().J(256, false);  // 256=? 群聊
// 遍历 y3.convertFrom → list.add(y3.b1())  ← 群 username
```
### 6.2 联系人列表（✅ b41.d2.j 实证）
```java
Cursor r = ((c4) j1.v(c4.class)).cj().r();
// 遍历 y3 → 存对象列表
```
### 6.3 相关存储
- com.tencent.mm.storage.l4 (会话存储, 方法 q(List)/z(String) 等)
- com.tencent.mm.storage.a3 (联系人存储, K1/L1/N1/t1 等)
- com.tencent.mm.storage.z2 (联系人实体, D0/F0/r0/s0 等)
- com.tencent.mm.storage.k4 (联系人信息基类)
- com.tencent.mm.storage.d2 / b41.d2 (群工具: C()会话类型, z()群列表, j()联系人)

## 七、PPC 新发送框架（v51 包）

✅ 已反编译核心 builder/执行链。

| 类 | 作用 | 关键方法/字段 |
|---|---|---|
| v51.s1 | 工厂 | a(talker) → r1 builder；b(talker,p1,i,obj) |
| v51.r1 | builder(17字段) | e(content), h(talker), i(type), f(k7), g(str), c(callback), a()→n1 |
| v51.n1 | CGI封装 | a(m1); a()=执行 (modelbase.z2.b.a.b.h(m1,0)) |
| v51.b0 | PPC CGI scene | doScene/onGYNetEnd/type 522 等 |
| v51.r0 | 发送CGI(旧) | doScene/onGYNetEnd, type=... |
| v51.p1 | 发送类型枚举 | TEXT/IMAGE/VIDEO/CDN_IMAGE/EMOJI/APPMSG/LOCATION/SHARECARD |
| x51.r | SendMsgMainFlowPPC | handleSendMsgInitAction 等状态机 |
| up3.* | PPC任务状态 | h0=Init/Prepare/SendFirstCgi/GenVideos/UploadAttach/SendCgi/UpdateMsg/Finish/Cancel; f0=事件; c0=任务接口 |

### 7.1 v51.r1 字段（✅ decompile_class_fields 实证）
a:String b:String c:String d:String e:int f:int g:long h:Object i:int j:k7 k:String l:p1 m:boolean n:String o:String p:String q:String

### 7.2 执行示例（✅ pm.run 实证）
```java
if (t1.a(talker)) {            // PPC 可用
    r1 a = s1.a(talker); a.e = type; a.f = flag; a.e(content); a.h(talker);
    a.h = map; a.o = src; a.q = quote; a.b();          // 异步
} else {
    r1 a = s1.a(talker); ...; a.i = 5; n1 n = a.a(); r0 q = n.a; q.q=cb; n.a();
}
```

## 八、多群转发生成（MsgRetransmitUI）

✅ 已反编译（日志 MicroMsg.MsgRetransmitUI，236字符串/21方法）
- intent keys: Retr_Msg_Type / Retr_Msg_content / Multi_Retr / Retr_go_to_chattingUI / Retr_show_success_tips / Select_Conv_User / ToUsername / SendMsgUsernames / msg_forward_sns_obj_id
- 视频转发 XML 键: .msg.videomsg.$aeskey/$cdnrawvideoaeskey/$cdnrawvideourl/$cdnthumblength/$cdnvideourl/$length/$md5/$newmd5/$originsourcemd5/$playlength/$rawlength/$rawmd5
- AppMsg 转发: processAppMessageTransfer, 键 appbrand_params/img_url
- 静态方法: Z6(talker, Long msgId) 更新转发id; a7(n1) 522类型转发完成


## 九、Java Xposed 工具类（可直接编译）

> 所有反射以"类名+方法名"为准，跨版本漂移改常量即可。群发策略=遍历群列表逐群调用+节流。

```java
package com.example.wxgroupcast;

import android.content.Context;
import java.lang.reflect.*;
import java.text.SimpleDateFormat;
import java.util.*;

public class WxMsgSender {
    private static final String CLS_SEND_MSG_MGR = "qs5.v5";
    private static final String CLS_SERVICE_CTX  = "ph5.n0";
    private static final String CLS_E10_M        = "e10.m";
    private static final String CLS_E10_D        = "e10.d";
    private static final String CLS_D2           = "b41.d2";

    private final ClassLoader cl;
    private final Object sendMsgMgr;

    public WxMsgSender(ClassLoader loader) throws Exception {
        this.cl = loader;
        Class<?> nc = Class.forName(CLS_SERVICE_CTX, true, cl);
        Method m = nc.getDeclaredMethod("c", Class.class);
        m.setAccessible(true);
        this.sendMsgMgr = m.invoke(null, Class.forName(CLS_SEND_MSG_MGR, true, cl));
    }

    // 群列表
    public List<String> getGroups() throws Exception {
        Object list = Class.forName(CLS_D2, true, cl).getMethod("z").invoke(null);
        List<String> out = new ArrayList<>();
        for (Object o : (List<?>) list) out.add(String.valueOf(o));
        return out;
    }

    // 文本群发
    public void sendTextToGroups(String text, List<String> groups) throws Exception {
        String csv = join(groups);
        Method m = sendMsgMgr.getClass().getMethod("hj", String.class, String.class, String.class);
        m.setAccessible(true); m.invoke(sendMsgMgr, null, csv, null);
    }

    // 名片群发（推荐：csv 多群）
    public void sendContactCard(String cardUser, List<String> groups) throws Exception {
        String csv = join(groups);
        Class<?> yl = Class.forName("pc5.yl", true, cl);
        Method m = sendMsgMgr.getClass().getMethod("ej", String.class, String.class, boolean.class, yl);
        m.setAccessible(true); m.invoke(sendMsgMgr, cardUser, csv, false, null);
    }

    // 图片群发
    public void sendImage(Context ctx, String filePath, List<String> groups) throws Exception {
        for (String g : groups) {
            Method m = sendMsgMgr.getClass().getMethod("b",
                Context.class, String.class, String.class, int.class,
                String.class, String.class, String.class,
                Class.forName("b41.k7", true, cl), Class.forName("s0.d", true, cl));
            m.setAccessible(true);
            m.invoke(sendMsgMgr, ctx, g, filePath, 3, "", "", "", null, null);
            Thread.sleep(600);
        }
    }

    // 视频群发
    public void sendVideo(Context ctx, String file, String thumb, List<String> groups) throws Exception {
        Class<?> qn6 = Class.forName("pc5.qn6", true, cl);
        Class<?> talker = Class.forName("com.tencent.mm.plugin.msg.MsgIdTalker", true, cl);
        Method m = sendMsgMgr.getClass().getMethod("sj", Context.class, String.class,
            String.class, String.class, int.class, int.class, qn6, boolean.class,
            boolean.class, String.class, String.class, talker);
        m.setAccessible(true);
        for (String g : groups) {
            m.invoke(sendMsgMgr, ctx, g, file, thumb, 62, 0, null, true, false, "", "", null);
            Thread.sleep(600);
        }
    }

    // 语音文件名（对齐微信算法）
    public static String buildVoiceFileName(String user, String prefix, long time) {
        if (prefix == null || prefix.isEmpty()) prefix = "amr_";
        String fmt = new SimpleDateFormat("ssHHmmMMddyy").format(new Date(time));
        String head;
        if (user == null || user.length() <= 1) {
            head = fmt + "fffffff";
        } else {
            try {
                byte[] dg = java.security.MessageDigest.getInstance("MD5")
                    .digest(user.getBytes("UTF-8"));
                StringBuilder sb = new StringBuilder();
                for (byte b : dg) sb.append(String.format("%02x", b & 0xff));
                head = fmt + sb.substring(0, 7);
            } catch (Exception e) { head = fmt + "fffffff"; }
        }
        return prefix + head + String.format("%04x", time % 65535) + ((time % 7) + 100);
    }

    private static String join(List<String> list) {
        StringBuilder sb = new StringBuilder();
        for (String s : list) { if (sb.length() > 0) sb.append(","); sb.append(s); }
        return sb.toString();
    }
}
```

### 9.1 推荐 hook 点汇总
| 场景 | 推荐 hook |
|---|---|
| 文本/名片/图片/视频群发 | qs5.v5 各方法（csv 多群） |
| 语音新框架 | fj0.j.invokeSuspend（锚定 newSendVoice_） |
| 语音旧框架 | v61.d1 / yl.y0 / v61.o |
| 视频号分享 | a53.t5.a / l9.L（Retr_Msg_Type/Retr_Msg_content） |
| 消息落库 | b41.aa.y / f9.yb |


## 十、二次审查结论（准确性/完整性）

### ✅ 已实证（反编译/Smali 证人）
1. qs5.v5 全部方法签名、MicroMsg.SendMsgMgr 日志身份、gj/hj/ej/fj/rj 多目标逗号分隔逻辑
2. v51.r1/s1/n1 builder → n1.a() 执行 CGI 完整链路；v51.r1 17 字段
3. ChatFooter.d → V0 → k4 → om.SendTextComponent.w0 → pm → oh0.c.a 文本 UI 链路
4. com.tencent.mm.pluginsdk.model.app.k0.M() 创建 e9 + iy.f0.ej 入队发送
5. b41.aa.y / f9.yb(Bb) 消息落库；b41.d2.C() 会话类型；b41.d2.z() 群列表
6. fj0.* 语音任务框架 + v61.* 语音存储/上传下载 + 文件名算法
7. e10.d/m/h 名片新服务完整签名 + qb.b 名片 XML 模板(17参数/OpenIM)
8. dx0.r 192 字段 + 公共 XML 标签 + 解析键
9. a53.t5 视频号 finderFeed 完整标签 + l9.L 分享 intent
10. dx0.g 文件/链接 mmreadershare/mmbrandmpvideo XML
11. MsgRetransmitUI 转发 intent keys + 视频 XML 键

### ⚠️ 存在版本漂移风险（运行期兜底）
1. v51/x51（新 PPC 框架）小类高度混淆，跨版本可能变化 → 以方法名+反射兜底
2. fj0 语音是新开关(RepairerConfigNewSendVoice)，旧版可能只有 yl.y0 SceneVoiceService
3. 图片/视频 CDN 上传类(kt.d1/qi0.w2/ab5.s)依赖场景，绕 CDN 直接塞 e9 更通用
4. 视频号卡片 XML 内部 mediaList 子标签较多，建议运行时抓真实消息反推
5. e10.d 服务可能未注册(降级旧方法 qb.b)，代码应双路径 try-catch
6. 名片自定义扩展 yl.toByteArray()（biznamecardinfo）格式未深挖，默认传 null

### 建议的 Xposed 主策略（最稳）
1. 文本/名片：优先走 qs5.v5（服务定位+反射，类名稳定）
2. 图片/视频/语音/AppMsg：构造 e9 → h9.b().v().yb(e9) 入库 → 触发发送场景（aa.y/f9.Bb 兜底），绕开 UI 与 CDN 细节
3. 多群：逐群循环 + 限速（每条 0.5~2s）避免风控

---
END OF REPORT
