群聊@文档:
已经把整条链路从微信 APK 里逐层挖到底了。下面是经过二次核查的完整实现方案，所有类名/方法名/字段名都在当前  com.tencent.mm  包里实证过。
一、整体链路总览
┌─────────────────────────────────────────────────────────────────┐
│ ① 收消息（Hook 数据库 insert）                                    │
│   com.tencent.mm.storage.f9.Bb(e9 msg, boolean)                  │
│   → msg.N0() = "xxx@chatroom"   (群ID)                           │
│   → msg.s0() = "wxid_xxx"       (发言人wxid)                      │
│   → msg.j()  = 文本             (内容)                            │
│   → msg.z0() = 0                (0=收到 1=自己发的)                │
├─────────────────────────────────────────────────────────────────┤
│ ② 群昵称 → wxid 反查                                             │
│   com.tencent.mm.storage.z2.x0(wxid)  → 群昵称                    │
│   b41.d2.t(z2, wxid, false)         → 显示名                     │
├─────────────────────────────────────────────────────────────────┤
│ ③ 投喂 AI（你自己的 HTTP，异步）                                   │
├─────────────────────────────────────────────────────────────────┤
│ ④ 组包：@协议                                                    │
│   content  = "@小王\u2005" + AI回复                               │
│   atMap    = { "atuserlist" : "<![CDATA[wxid_xxx]]>" }            │
├─────────────────────────────────────────────────────────────────┤
│ ⑤ 发送                                                           │
│   com.tencent.mm.ui.chatting.component.om.A0(content, 1, atMap)   │
│     → om.w0(...)                                                 │
│     → com.tencent.mm.ui.chatting.component.pm.run()               │
│     → v51.r1 builder → v51.n1 → NetScene队列 → v51.r0 → 入库      │
└─────────────────────────────────────────────────────────────────┘
二、消息模型（字段名没混淆，这是最大的运气）
微信 8.x 用 AutoGen 表实体，字段名是明文：
 im.c8 
 MicroMsg.SDK.BaseMsgInfo ，基类，108 个明文字段
 com.tencent.mm.storage.e9 
 MicroMsg.MsgInfo ，extends  dx0.p3  →  im.c8 
 com.tencent.mm.storage.f9 
 MicroMsg.MsgInfoStorage ，消息存储
 im.c8  关键字段（DB 列名，明文）
java
field_talker        // 会话对象；群里 = "xxx@chatroom"
field_fromUsername  // ★ 发言人 wxid（群里就是群成员wxid）
field_toUsername
field_content       // 消息内容
field_createTime    // ms
field_type          // 1=文本
field_status
field_isSend        // 0=收到  1=发出
field_msgSvrId
field_msgSeq
field_flag
G                   // ★ msgSource XML（atuserlist 在这里）
 im.c8  访问器（混淆方法名，但已实证）
java
N0()  → field_talker
s0()  → field_fromUsername   // 群里 = 发言人
Q0()  → field_toUsername
j()   → field_content
E0()  → G (msgSource)
F0()  → field_msgSvrId
D0()  → field_msgSeq
r0()  → field_flag
z0()  → field_isSend
M0()  → field_status
getType() / getCreateTime() / getMsgId()
 e9  的 setter
java
e9.r3(String)  → 写 msgSource (G)
e9.b1(String)  → 写 content
e9.setType(int)
e9.t1(int)     → 写 field_status
⚠️ 二次核查发现：群消息 content 可能是  "wxid:\n正文"  的老格式。微信自己有  b41.aa （ MicroMsg.MsgInfoStorageLogic ）处理：
•  
 aa.u(str)  找  :  的位置
•  
 aa.v(str)  = 剥掉  wxid:\n  前缀（ substring(u+2) ）
•  
 aa.s(str)  = trim 后剥前缀
•  
 aa.t(str)  = 取 wxid 前缀
•  
 aa.B(text, prefix)  = 加回前缀
8.x 因为有  field_fromUsername  列，多数场景 content 是干净的，但必须兜底剥前缀。
三、① 接收：Hook 点
主 Hook 点（所有消息，收+发，都过这里）：
java
com.tencent.mm.storage.f9.Bb(com.tencent.mm.storage.e9 msg, boolean z)
实证：方法体日志  Log.i("MicroMsg.MsgInfoStorage", "insert:%d talker:%s ... issend:%d ...") ，调用方包括  v51.r0.<init> （发送）、 cy1.e/f/h 、 x51.q.i 、 f9.yb （接收）。
过滤条件（在  afterHookedMethod  里做）：
java
if (msg == null) return;
String talker = (String) XposedHelpers.callMethod(msg, "N0");
if (talker == null || !talker.endsWith("@chatroom")) return;   // 只要群
int isSend = (int) XposedHelpers.callMethod(msg, "z0");
if (isSend != 0) return;                                       // 只要别人发的
int type = (int) XposedHelpers.callMethod(msg, "getType");
if (type != 1) return;                                         // 只要文本
String from  = (String) XposedHelpers.callMethod(msg, "s0");    // 发言人wxid
String content = (String) XposedHelpers.callMethod(msg, "j");
long svrId = (long) XposedHelpers.callMethod(msg, "F0");
long seq   = (long) XposedHelpers.callMethod(msg, "D0");
备选 Hook 点（不想 Hook 数据库时）：
•  
 f9.Qc(long, e9, boolean)  — 更新（发送成功后回写）
•  
 im.c8.convertFrom(Cursor)  — 每条从 DB 读出的消息都过（会重复触发历史消息，不推荐）
•  
 f9.Db(e9)  —  insertForBackupRecover ，别用
四、② 群昵称 / wxid 互查
java
// 群成员存储实体
com.tencent.mm.storage.z2   // MicroMsg.ChatRoomMember
  .q0(wxid)  → so.b         // 成员对象
  .x0(wxid)  → String       // ★ 群昵称（so.b.e）
  .z0()      → List         // 全部成员
  .Y0(str)   → List
java
// 显示名统一入口（优先备注/群昵称 → 好友昵称 → wxid）
b41.d2.t(z2 roomStorage, String wxid, boolean useRemark)

// chatType 判定
b41.d2.C(String talker)
  // endsWith("@chatroom") → 1
  // endsWith("@t.qq.com") → 11
  // endsWith("@qqim")     → 36
  // 其他                  → 1
服务定位器（拿 storage 实例用）：
java
gp0.j1.v(Class)  → lp0.a     // 按 class 取 provider
gp0.j1.j()       → 单例

// 群成员存储：
z2 room = ((q02.f) j1.v(q02.f.class)).ce().a().t1(chatroomId);

// 好友/联系人存储：
y3 contact = ((c4) j1.v(c4.class)).cj().n(wxid, true);
//   y3.b1() = username, y3.r0() = 备注, y3.f2() = 昵称
五、④ @ 人的底层协议 ⭐（本次最关键的发现）
全部来自  com.tencent.mm.ui.g1 （ MicroMsg.AtSomeOneHelper ）和  ChatFooter.I0 ，是微信自己的源码逻辑，权威：
1. content 里的 @ 标记
java
// g1.a(String) 第 45 行 —— 接收端/发送端统一的匹配式
if (str.contains("@" + displayName + (char) 8197)) { ... }

// g1.d(List) 第 126/129 行 —— 往输入框插入时的拼法
substring = substring + "@" + str + (char) 8197;   // 第2个起
substring = substring + str + (char) 8197;          // 第1个（前面已有@）
 8197 = 0x2005 = U+2005 FOUR-PER-EM SPACE ，不是普通空格！
多人时： @小王\u2005@小明\u2005问题内容 
2. msgsource 里的 atuserlist
java
// g1.a() 第 57 行 / ChatFooter.I0() 第 2310 行，一字不差
hashMap.put("atuserlist", "<![CDATA[" + y8.b1(linkedList, ",") + "]]>");
最终落库到  msgSource （ im.c8.G ）：
xml
<msgsource>
  ...
  <atuserlist><![CDATA[wxid_1,wxid_2]]></atuserlist>
  ...
</msgsource>
读取方式（ e9.s2()  /  d0.r0() ）：
java
Map m = fa.d(msgSource, "msgsource", null);
String at = (String) m.get(".msgsource.atuserlist");
3. 两个"待发送 @"缓冲池
java
com.tencent.mm.ui.g1  // AtSomeOneHelper
  static a              : EditText        当前输入框
  static d              : LinkedHashMap<群昵称, wxid>   全局待发@
  static e/f/g/h/i      : 会话状态
  g1.a(text)            → HashMap{"atuserlist": "<![CDATA[...]]>"}
  g1.d(List<昵称>)      → 往 EditText 插入 "@昵称\u2005"
  g1.e(EditText, Context, chatroom, boolean, listener) → 注册

com.tencent.mm.pluginsdk.ui.chat.ChatFooter
  field S1 (m5) → .e = Map<chatroom, LinkedList<HashMap<昵称, wxid>>>
  ChatFooter.I0(talker, text) → 同上格式的 atuserlist HashMap
4. 如果你要走"UI 输入框"路线
java
// 构造带 @ 高亮的文本（必须用这个，否则 @ 会变纯文本）
SpannableString s = com.tencent.mm.pluginsdk.ui.span.c0.j(context, text, textSize);
六、⑤ 发送链路（逐层实证）
ChatFooter.d(ChatFooter, String text, boolean z)          ← 算 atuserlist
   └─ g1.a(text) / ChatFooter.I0(talker, text)
   └─ g6(1, HashMap(atuserlist))   // 有@
      g6(0, new HashMap())         // 无@
        ↓
ChatFooter.V0(String text, Integer atType, HashMap atMap)  ← 引用回复分支
        ↓
com.tencent.mm.ui.chatting.component.la.C1(String, gb)
        ↓
com.tencent.mm.ui.chatting.component.om.A0(String, int atType, HashMap atMap)
   ↑★★★★ 这是 "MicroMsg.ChattingUI.SendTextComponent" 的公开入口
        ↓
com.tencent.mm.ui.chatting.component.om.w0(text, atType, str2, atMap)
        ↓
new com.tencent.mm.ui.chatting.component.pm(...).run()
        ↓ 两条落地路径：
   ├─(A) RepairerConfigSendTextTask == 1
   │     oh0.c.a(toUser, content, chatType, atType, atMap, str6, str7)
   │       → oh0.f(toUserName, content, chatType, atType, msgType=0, msgsrc, atMap, str6)
   │       → ph0.e (TextMsgFeatureService)
   │
   └─(B) 默认（NetScene / PPC）★推荐
         v51.r1  = "MicroMsg.SendMsgCgiFactory"
路径 B 的 builder（ v51.r1 ）字段全集
java
public class r1 {
    String a, b, c, d;   // b=talker(toUser)  d=content
    int    e;            // chatType  ← d2.C(talker)，群=1
    int    f;            // atType    ← 0无@ / 1有@
    long   g;
    Object h;            // ★ atMap（atuserlist 的 HashMap）
    int    i;            // = 5（文本发送固定值）
    k7     j;            // 附加信息
    String k, n, o, p, q;
}
 pm.run()  里的真实组包代码（已实证）
java
r1 a3 = v51.s1.a(str3);      // s1.a(talker) → new r1(); r.h(talker)
a3.e = C;                    // chatType
a3.f = i;                    // atType
a3.e(str4);                  // .d = content
a3.h(str3);                  // .b = talker
a3.h = map;                  // ★ atuserlist map
a3.o = str6;
a3.q = str5;
a3.i = 5;
v51.n1 a4 = a3.a();
a4.a();                      // → z2.b.a.b.h(m1, 0) 入 NetScene 队列
随后  v51.r0 （NetSceneSendMsg）→  K(e9)  →  f9.Qc(msgId, e9, true)  落库。
七、完整 Xposed 模块骨架（独立模块，标准 Xposed API）
java
public class WeChatGroupAI implements IXposedHookLoadPackage {

    private static final String WM = "com.tencent.mm";
    // 按版本做模糊兜底（下面给的是当前包实证名）
    private static final String C_MSGINFO   = WM + ".storage.e9";
    private static final String C_STORAGE   = WM + ".storage.f9";
    private static final String C_SENDTEXT  = WM + ".ui.chatting.component.om";
    private static final String C_ROOMMEMBER= WM + ".storage.z2";
    private static final String C_ATSOMEONE = WM + ".ui.g1";
    private static final char  AT_SEP = (char) 8197;   // \u2005

    @Override
    public void handleLoadPackage(LoadPackageParam lp) throws Throwable {
        if (!WM.equals(lp.packageName)) return;
        hookReceive();
    }

    /* ---------------- ① 收消息 ---------------- */
    private void hookReceive() {
        Class<?> storage = XposedHelpers.findClassIfExists(C_STORAGE, null);
        Class<?> msgCls  = XposedHelpers.findClassIfExists(C_MSGINFO, null);
        if (storage == null || msgCls == null) return;

        XposedHelpers.findAndHookMethod(storage, "Bb", msgCls, boolean.class,
            new XC_MethodHook() {
                @Override protected void afterHookedMethod(MethodHookParam p) {
                    Object msg = p.args[0];
                    if (msg == null) return;
                    try {
                        String talker = (String) XposedHelpers.callMethod(msg, "N0");
                        if (talker == null || !talker.endsWith("@chatroom")) return;
                        if ((int) XposedHelpers.callMethod(msg, "z0") != 0) return;   // 只要收到的
                        if ((int) XposedHelpers.callMethod(msg, "getType") != 1) return; // 只要文本

                        String from = (String) XposedHelpers.callMethod(msg, "s0");  // 发言人wxid
                        String raw  = (String) XposedHelpers.callMethod(msg, "j");
                        if (from == null || raw == null) return;

                        String text = stripGroupPrefix(raw);   // 兜底剥 "wxid:\n"
                        long svrId  = (long) XposedHelpers.callMethod(msg, "F0");
                        long seq    = (long) XposedHelpers.callMethod(msg, "D0");

                        GroupAIBus.onGroupMessage(talker, from, text, svrId, seq);
                    } catch (Throwable t) {
                        XposedBridge.log("[GroupAI] recv err " + t);
                    }
                }
            });
    }

    /* ---------------- ② 群昵称 ---------------- */
    public static String roomDisplayName(String chatroom, String wxid) {
        try {
            Object room = getRoomMemberStorage(chatroom);   // z2
            if (room != null) {
                String n = (String) XposedHelpers.callMethod(room, "x0", wxid);
                if (n != null && !n.isEmpty()) return n;
            }
            Class<?> d2 = XposedHelpers.findClass(WM + ".b41.d2", null);
            return (String) XposedHelpers.callStaticMethod(d2, "t", room, wxid, Boolean.FALSE);
        } catch (Throwable t) { return wxid; }
    }

    private static Object getRoomMemberStorage(String chatroom) {
        try {
            Class<?> j1  = XposedHelpers.findClass("gp0.j1", null);
            Class<?> f   = XposedHelpers.findClass("q02.f", null);
            Object prov  = XposedHelpers.callStaticMethod(j1, "v", f);
            Object e8    = XposedHelpers.callMethod(prov, "ce");
            Object core  = XposedHelpers.callMethod(e8, "a");
            return XposedHelpers.callMethod(core, "t1", chatroom);
        } catch (Throwable t) { return null; }
    }

    /* ---------------- ④⑤ 发送带@的消息 ---------------- */
    public static void sendAt(String chatroom, String askerWxid, String askerName, String reply) {
        // 1. 组 content：@昵称\u2005 + 回复
        String content = "@" + askerName + AT_SEP + reply;

        // 2. 组 atuserlist map
        HashMap<String, String> atMap = new HashMap<>();
        atMap.put("atuserlist", "<![CDATA[" + askerWxid + "]]>");

        // 3. 走 SendTextComponent
        try {
            Class<?> om = XposedHelpers.findClassIfExists(C_SENDTEXT, null);
            Object comp = getChattingComponent(om);   // 见下
            if (comp != null) {
                XposedHelpers.callMethod(comp, "A0", content, 1, atMap);
                return;
            }
        } catch (Throwable t) {
            XposedBridge.log("[GroupAI] A0 failed: " + t);
        }
        // 4. 兜底：直接打 SendMsgCgiFactory
        sendViaCgi(chatroom, content, atMap);
    }

    private static void sendViaCgi(String chatroom, String content, HashMap<String,String> atMap) {
        try {
            Class<?> s1 = XposedHelpers.findClass("v51.s1", null);
            Class<?> d2 = XposedHelpers.findClass(WM + ".b41.d2", null);
            int chatType = (int) XposedHelpers.callStaticMethod(d2, "C", chatroom);

            Object r = XposedHelpers.callStaticMethod(s1, "a", chatroom);  // r1
            XposedHelpers.setObjectField(r, "e", chatType);   // chatType
            XposedHelpers.setObjectField(r, "f", 1);          // atType = 有@
            XposedHelpers.callMethod(r, "e", content);        // .d = content
            XposedHelpers.callMethod(r, "h", chatroom);       // .b = talker
            XposedHelpers.setObjectField(r, "h", atMap);      // ★ atuserlist
            XposedHelpers.setObjectField(r, "i", 5);
            Object n = XposedHelpers.callMethod(r, "a");      // n1
            XposedHelpers.callMethod(n, "a");                 // 入队执行
        } catch (Throwable t) {
            XposedBridge.log("[GroupAI] cgi failed: " + t);
        }
    }

    /* ---------------- 取 SendTextComponent ---------------- */
    private static Object getChattingComponent(Class<?> omCls) {
        try {
            // com.tencent.mm.ui.chatting.manager.c 容器，a(Class) 取组件
            Object container = ChattingContextProvider.get();   // 你自己从 ChattingUI/Fragment 拿
            if (container == null) return null;
            return XposedHelpers.callMethod(container, "a", omCls);
        } catch (Throwable t) { return null; }
    }

    /* ---------------- 群消息 content 剥前缀 ---------------- */
    static String stripGroupPrefix(String s) {
        if (s == null || s.isEmpty()) return s;
        if (s.startsWith("~SEMI_XML~")) return s;
        int i = s.indexOf(':');
        if (i <= 0) return s;
        if (s.substring(0, i).contains("<")) return s;   // XML，别剥
        int st = i + 2;                                   // 跳过 ":\n"
        return st < s.length() ? s.substring(st) : s;
    }
}
拿 Chatting 容器（ getChattingComponent  用）：
java
// Hook com.tencent.mm.ui.chatting.BaseChattingUIFragment / ChattingUIFragment
// 或 Activity com.tencent.mm.ui.chatting.ChattingUI / variants.ChattingMainUI
// 反射找字段 gk5/d（presenter）→ .c 是 com.tencent.mm.ui.chatting.manager.c
XposedHelpers.getObjectField(fragment, "xxx");  // 字段名按版本模糊匹配
Object presenter = ...;
Object container = XposedHelpers.getObjectField(presenter, "c");
更省事的替代方案（不碰内部组件，纯 UI 复用）：
java
// 1. 拿到 ChatFooter 实例（Hook ChattingUI 时从组件管理器取）
// 2. 设置输入框（EditText 在 ChatFooter 里，用 c0.j 构造带@样式的文本）
// 3. 把 <昵称,wxid> 塞进 ChatFooter.S1.e 的 LinkedList<HashMap> 里
//    （key = chatroom，value = LinkedList，元素 = HashMap{昵称: wxid}）
// 4. 调 ChatFooter.d(footer, content, false)
这条路最稳，但必须前台停在群聊天界面。
八、二次核查清单（精准性 + 完整性）
✅ 已实证的关键点
1
@ 分隔符是  \u2005 (8197)，不是空格
 g1.a()  L45 /  g1.d()  L126,129 /  ChatFooter.I0()  L2297
2
atuserlist 格式  <![CDATA[wxid1,wxid2]]> 
 g1.a()  L57 /  ChatFooter.I0()  L2310
3
群发言人 wxid 在  field_fromUsername  独立列
 im.c8.convertFrom()  L466-471
4
群 ID 在  field_talker ， xxx@chatroom 
 im.c8.N0() 
5
群昵称取法  z2.x0(wxid) 
 com.tencent.mm.storage.z2.x0()  →  so.b.e 
6
群 content 可能带  wxid:\n  前缀
 b41.aa.u/v/s/t/B 
7
发送主入口  om.A0(content, atType, atMap) 
 com.tencent.mm.ui.chatting.component.om  =  MicroMsg.ChattingUI.SendTextComponent 
8
chatType： @chatroom →1
 b41.d2.C()  L153
9
atType：有@→1，无@→0
 ChatFooter.d()  L834  new g6(1/0, ...) 
10
接收 Hook 点  f9.Bb(e9, boolean) 
 MicroMsg.MsgInfoStorage  insert，收发都过
⚠️ 必须兜底/注意
1.  
混淆名会随版本变。 im.c8 / e9 / f9  的字段名和  g1  的 @ 协议是稳定的；但  b41.d2 、 v51.s1 、 gp0.j1  这类类名可能变。上 DexKit 时用  using_strings  锚定：
•  
 "MicroMsg.MsgInfoStorage"  → 找  f9 
•  
 "MicroMsg.MsgInfo"  → 找  e9  /  im.c8 
•  
 "MicroMsg.AtSomeOneHelper"  → 找  com.tencent.mm.ui.g1 
•  
 "MicroMsg.ChattingUI.SendTextComponent"  → 找  om 
•  
 "atuserlist"  → 一把梭定位所有相关类
2.  
 type == 1  才是文本（业界共识，本次未逐字节验证类型常量表，建议自己打日志验一遍）。
3.  
 f9.Bb  对"自己发的消息"也会触发（发送路径  v51.r0  也调它），务必用  z0()==0  过滤，否则会自问自答死循环。
4.  
 f9.Bb  在历史同步/备份时也会触发，用  field_msgSeq  +  field_msgSvrId  做去重（每个群维护一个  lastSeq ）。
5.  
AI 回包要异步，别卡 Hook 线程；发送要 post 到主线程或走微信自己的队列（ v51.n1.a()  内部已处理）。
6.  
@ 高亮依赖 content 里真有  @昵称\u2005 。只塞 atuserlist 不塞 content，对方收到的是纯文本不会有 @ 样式，也不会触发"有人@我"提醒。
7.  
昵称必须和群里显示名完全一致（ z2.x0(wxid)  拿到的值）。用好友昵称或备注会导致  contains("@" + name + "\u2005")  匹配不上，@ 失效。
8.  
别同时 Hook 多个 insert 重载（ Bb / Db / Hb ），只用  Bb 。
9.  
如果想后台也能回（不停在聊天页），必须走  om.A0  /  v51.r1  内部 API，UI 复用方案做不到。
建议的落地顺序
1.  
先只做 ①：Hook  f9.Bb ，把  talker/fromUsername/content/msgSeq  打到 log，验证群消息能完整拿到（重点看 content 有没有  wxid:\n  前缀）。
2.  
再做 ②：反射拿  z2 ，验证  x0(wxid)  能返回正确群昵称。
3.  
再做 ④⑤：先手工在  om.A0  里发一条  @自己\u2005test ，确认群里出现蓝色 @ 且对方能收到提醒。
4.  
最后串起 AI 异步链路 + 防循环（ isSend  + 消息内容标记，比如回复末尾加隐藏标识，或用  msgSeq  窗口去重）。

消息引用文档:
已经把"引用回复"这条链路挖到底了。结论先说：微信的"引用"本质是把被引用消息的信息打包成一个  MsgQuoteItem ，挂在发送请求上，同时往本地  yp3  表插一条引用关系记录。下面给你完整方案。
一、先分清两种"引用"
A. 文本式引用
回复内容里带  【小敏】原文…  前缀
⭐⭐⭐ 100%
无，纯字符串
B. 微信原生引用气泡
和手动长按"引用"一模一样的气泡卡片
⭐⭐ 中
需构造 appmsg XML + 本地插表
建议：先用 A 跑通业务闭环（AI 投喂→@提问人→回复），再上 B。B 的  <refermsg>  里带  <signature>  字段，新版微信可能校验，构造不全对会掉成纯文本。
二、方案 A：文本式引用（推荐先上）
就是把"被引用消息"作为上下文拼进 content，和 @ 协议完全复用上一轮的发送链路：
java
// 小敏的原文（已从 f9.Bb hook 里拿到，注意剥 "wxid:\n" 前缀）
String quoted = stripGroupPrefix(minMsg.j());

// 组 content：@ + 原文 + 分隔 + AI 回复
String content = "@" + xiaominName + AT_SEP          // \u2005
              + "\n"
              + "「" + quoted + "」\n"                 // 文本式引用
              + "————————\n"
              + aiReply;

HashMap<String,String> atMap = new HashMap<>();
atMap.put("atuserlist", "<![CDATA[" + xiaominWxid + "]]>");

// 走上一轮的 SendTextComponent
omComp.A0(content, 1, atMap);
优点：不碰任何内部结构，任何微信版本都不会挂。缺点：没有微信那个灰色引用气泡。
三、方案 B：微信原生引用气泡（完整链路）
3.1 入口态：ChatFooter 必须处于"正在引用"
 ChatFooter.V0(String text, Integer atType, HashMap atMap)  第一行就是门槛：
java
if (this.L.getTag() == null || !(this.L.getTag() instanceof ba)
    || !this.L.isShown() || y8.J0(str)
    || (baVar = (ba) this.L.getTag()) == null
    || (e9Var = baVar.a) == null) {
    return false;                      // ← 没在引用态，直接不发
}
e9 yi = ex0.k0.yi(e9Var.N0(), e9Var.getMsgId());
if ((((c8) yi).F & 4) == 4 || yi.getType() == 10000) {
    Log.e("MicroMsg.ChatFooter", "msg is revoked!");   // 被引用的消息已撤回
    return false;
}
•  
 this.L ：ChatFooter 里那个"引用提示条"View
•  
tag 类型： com.tencent.mm.storage.ba 
java
public final class ba {
    public final e9 a;    // ★ 被引用的 MsgInfo
    public final i  b;    // p95.i，partialQuoteText（部分引用）
}
程序化进入引用态（反射）：
java
Object quotedMsg = ...;                       // 小敏那条 e9
Object ba = new com.tencent.mm.storage.ba(quotedMsg, null);
View quoteBar = (View) getField(chatFooter, "L");   // 字段名按版本模糊匹配
quoteBar.setTag(ba);
quoteBar.setVisibility(View.VISIBLE);               // isShown() 必须为 true
3.2 构造 MsgQuoteItem（13 个字段，全部实证）
 com.tencent.mm.plugin.msgquote.model.MsgQuoteItem ：
 d 
type
 k0.c(quoted.getType()) 
 e 
svrid
 quoted.F0() 
 f 
fromusr
 quoted.N0() （talker）
 g 
chatuser
 ou5.c1.d(quoted)  = 发言人 wxid
 h 
displayname
 xp3.i.e(quoted, chatroom) 
 i 
msgsource
 quoted.G 
 m 
content
被引用消息正文（剥  wxid:\n  + 剥  <refermsg> / <recorditem> ）
 n 
msgSource
 ou5.c1.f(quotedMsgsource, atMap, atType) 
 o 
referfromscene
 quoted.A2 
 p 
strid
 ma.u(quoted.G) 
 q 
createTime
 quoted.getCreateTime() / 1000 
 r 
partialQuoteText
 p95.i （可空）
 ou5.c1.d(e9)  实证逻辑（取发言人）：
java
if (e9.z0() == 1) return y1.u();          // 自己发的 → 我的wxid
String N0 = e9.N0();
if (y3.V4(N0)) {                           // 是群
    String t = aa.t(e9.j());               // 从 "wxid:\n正文" 剥出 wxid
    if (t != null && t.length() > 0) return t;
}
return N0;
3.3 构造发送请求  dx0.r （核心）
java
dx0.r r = new dx0.r();
r.f = aiReply;          // ★ 回复文本
r.i = 57;               // ★★ 0x39 = 57 → 引用消息标记（xb.d / k0.M 都判这个值）
r.x2 = msgQuoteItem;    // ★★ 挂上引用项
if (quoted.A2 == 1) {
    com.tencent.mm.pluginsdk.ui.tools.o0 o0 = new com.tencent.mm.pluginsdk.ui.tools.o0();
    o0.b = 1;
    r.f(o0);
}
3.4 发送
java
// 实证：ChatFooter.V0 第 3692/3732/3752 行，一字不差
Pair I = com.tencent.mm.pluginsdk.model.app.k0.I(r, "", "", chatroom, "", (byte[]) null);
 k0.I → J → K → L → M ， M()  内部关键动作：
java
e9Var5.b1(r.u(rVar, null, null));   // content
e9Var5.t1(1);                        // status = 1
e9Var5.u1(str3);                     // ★ field_talker = 第4个参数 = 群ID
e9Var5.e1(aa.p(str3));               // field_talkerId
e9Var5.k1(1);
e9Var5.setType(p(rVar));             // type
e9Var5.r3(ma.o(str7));               // msgSource
if (rVar.i == 57 && rVar.x2 != null) e9Var5.r3(msgQuoteItem.n);   // 引用项的 msgSource
if (rVar.i == 57) e9Var5.r3(vu1.v0.a(G, rVar.f));                 // 0x39 附加节点
...
long yb = h9.b().v().yb(e9Var5);     // 入库，返回 msgId
3.5 ⭐ 插引用关系表（不做这步气泡不显示！）
java
if (I.second != null) {
    yp3.b b = new yp3.b();                       // extends im.e8
    b.field_msgId           = ((Long) I.second).longValue();  // 新消息 msgId
    b.field_quotedMsgId     = quoted.getMsgId();              // 被引用 msgId
    b.field_quotedMsgSvrId  = quoted.F0();                    // 被引用 svrid
    b.field_quotedMsgTalker = quoted.N0();                    // 被引用 talker
    ((yp3.a) vp3.e.ej()).x1(b);
    Log.i("MicroMsg.msgquote.PluginMsgQuote", "sendQuoteMsg result:%s msgId:%s result:%s", ...);
}
 im.e8  字段全集： field_msgId / field_msgSvrId / field_quotedMsgId / field_quotedMsgSvrId / field_quotedMsgTalker / field_status 
3.6  <refermsg>  到底藏在哪（渲染端反推）
 com.tencent.mm.ui.chatting.viewitems.xb.d ：
java
String N1 = e9Var.N1();                 // 显示内容（群消息剥 "wxid:\n"）
r rVar = r.v(N1);                        // 解析 content 里的 appmsg XML
if (rVar != null && rVar.i == 57) {
    int i = fa.d(N1, "msg", null).get(".msg.appmsg.referfromscene");
    msgQuoteItem = rVar.x2;
    ...
}
// 兜底手工剥：
int i1 = N1.indexOf("<refermsg>"), i2 = N1.indexOf("</refermsg>");
String sub = N1.substring(i1, i2);
String dn  = sub.substring(sub.indexOf("<displayname>")+13, sub.indexOf("</displayname>"));
所以  <refermsg>  在 content 的 appmsg XML 里，不是 msgsource：
xml
<msg>
  <appmsg type="57">
    <refermsg>
      <type>1</type>
      <svrid>1234567890</svrid>
      <fromusr>wxid_xiaomin</fromusr>
      <chatuser>xxx@chatroom</chatuser>
      <displayname>小敏</displayname>
      <content>小敏的原文</content>
      <msgsource>…</msgsource>
      <referfromscene>0</referfromscene>
      <strid>…</strid>
      <createtime>1699999999</createtime>
      <signature>…</signature>   ← ⚠️ 风险点
    </refermsg>
    <title>AI 的回复</title>
  </appmsg>
</msg>
 oy.u1.h  也印证： o0.C(str4, "<refermsg>", false) || o0.C(str4, "<type>57</type>", false) 。
四、完整代码：引用 + @ 一起发
java
public static void sendQuoteAndAt(Object quotedMsg, String chatroom,
                                  String askerWxid, String askerName, String aiReply) {
    try {
        // ---------- 1. 组装 MsgQuoteItem ----------
        Class<?> qCls = XposedHelpers.findClass(
            "com.tencent.mm.plugin.msgquote.model.MsgQuoteItem", null);
        Object item = XposedHelpers.newInstance(qCls);

        Class<?> k0 = XposedHelpers.findClass("com.tencent.mm.pluginsdk.model.app.k0", null);
        Class<?> c1 = XposedHelpers.findClass("ou5.c1", null);
        Class<?> ma = XposedHelpers.findClass("b41.ma", null);
        Class<?> xp3i = XposedHelpers.findClass("xp3.i", null);

        int qType  = (int) XposedHelpers.callMethod(quotedMsg, "getType");
        long qSvr  = (long) XposedHelpers.callMethod(quotedMsg, "F0");
        String qTalker = (String) XposedHelpers.callMethod(quotedMsg, "N0");
        String qFrom   = (String) XposedHelpers.callStaticMethod(c1, "d", quotedMsg);
        String qName   = (String) XposedHelpers.callStaticMethod(xp3i, "e", quotedMsg, chatroom);
        String qSrc    = (String) XposedHelpers.getObjectField(quotedMsg, "G");   // im.c8.G
        long   qCtime  = (long) XposedHelpers.callMethod(quotedMsg, "getCreateTime");
        int    qScene  = (int) XposedHelpers.getObjectField(quotedMsg, "A2");

        // atMap：@提问人
        HashMap<String,String> atMap = new HashMap<>();
        atMap.put("atuserlist", "<![CDATA[" + askerWxid + "]]>");

        // 被引用消息正文
        String qContent = (String) XposedHelpers.callMethod(quotedMsg, "N1");  // 已剥 wxid:\n
        if (qContent == null) qContent = "";

        XposedHelpers.setObjectField(item, "d",
                XposedHelpers.callStaticMethod(k0, "c", qType));
        XposedHelpers.setObjectField(item, "e", qSvr);
        XposedHelpers.setObjectField(item, "f", qTalker);
        XposedHelpers.setObjectField(item, "g", qFrom);
        XposedHelpers.setObjectField(item, "h", qName);
        XposedHelpers.setObjectField(item, "i", qSrc);
        XposedHelpers.setObjectField(item, "m", qContent);
        XposedHelpers.setObjectField(item, "n",
                XposedHelpers.callStaticMethod(c1, "f", qSrc, atMap, 1));
        XposedHelpers.setObjectField(item, "o", qScene);
        XposedHelpers.setObjectField(item, "q", qCtime / 1000);

        // ---------- 2. content：@ + 引用 + AI 回复 ----------
        String content = "@" + askerName + AT_SEP + aiReply;

        // ---------- 3. dx0.r ----------
        Class<?> rCls = XposedHelpers.findClass("dx0.r", null);
        Object r = XposedHelpers.newInstance(rCls);
        XposedHelpers.setObjectField(r, "f", content);   // 文本
        XposedHelpers.setObjectField(r, "i", 57);        // ★ 引用消息
        XposedHelpers.setObjectField(r, "x2", item);     // ★ 引用项

        // ---------- 4. 发送 ----------
        Object pair = XposedHelpers.callStaticMethod(k0, "I",
                r, "", "", chatroom, "", null);

        Object second = XposedHelpers.getObjectField(pair, "second");  // Pair.second = msgId
        if (second == null) { XposedBridge.log("[GroupAI] quote send failed"); return; }

        // ---------- 5. 插引用关系 ----------
        Class<?> yb = XposedHelpers.findClass("yp3.b", null);
        Object rel = XposedHelpers.newInstance(yb);
        XposedHelpers.setObjectField(rel, "field_msgId",           ((Long) second));
        XposedHelpers.setObjectField(rel, "field_quotedMsgId",
                XposedHelpers.callMethod(quotedMsg, "getMsgId"));
        XposedHelpers.setObjectField(rel, "field_quotedMsgSvrId",  qSvr);
        XposedHelpers.setObjectField(rel, "field_quotedMsgTalker", qTalker);

        Class<?> vp3e = XposedHelpers.findClass("vp3.e", null);
        Object ya = XposedHelpers.callStaticMethod(vp3e, "ej");
        XposedHelpers.callMethod(ya, "x1", rel);

        XposedBridge.log("[GroupAI] quote+at sent, msgId=" + second);
    } catch (Throwable t) {
        XposedBridge.log("[GroupAI] sendQuoteAndAt err: " + t);
        // 兜底：退回文本式
        sendAtPlain(chatroom, askerWxid, askerName, aiReply);
    }
}
五、二次核查清单
✅ 已实证（本次新挖）
1
引用消息标记 =  dx0.r.i == 57 （0x39）
 ChatFooter.V0  L3629  rVar.i = 57 ； xb.d  L283  rVar.i == 57 ； k0.M  L347/381/493/497
2
发送入口  k0.I(r, "", "", talker, "", null) ，第4个参数是 talker
 ChatFooter.V0  L3692/3732/3752； k0.M   e9Var5.u1(str3) 
3
 MsgQuoteItem  13 字段及赋值来源
 ChatFooter.V0  L3630-3743
4
必须再插  yp3.b  关系记录，否则气泡不显示
 ChatFooter.V0  L3753-3759  eVar.ej().x1(bVar) 
5
 yp3.b  4 个字段：msgId / quotedMsgId / quotedMsgSvrId / quotedMsgTalker
 ChatFooter.V0  L3755-3758
6
 <refermsg>  在 content 的 appmsg XML，不在 msgsource
 xb.d  L269-291  r.v(N1)  +  fa.d(N1,"msg") 
7
被引消息撤回/系统消息不能引用
 ChatFooter.V0  L3596-3600  (F & 4) == 4 || type == 10000 
8
引用态挂在  ChatFooter.L  的 tag 上，类型  storage.ba{e9, p95.i} 
 ChatFooter.V0  L3593
⚠️ 风险与注意
1.  
 <signature>  是最大不确定点。 p95.e.a()  里 refermsg 带  <signature> ，如果你只填  <svrid>/<strid>/<createtime>  而不填 signature，新版微信可能判定引用无效。缓解：让微信自己生成——即走 3.1 的"设置 ChatFooter 引用态"再调  V0 ，这样  MsgQuoteItem  是微信亲手填的，signature 天然正确。
2.  
方案 B 依赖  dx0.r  这个 192 字段的大家伙。类名稳定（ dx0.r  在  com.tencent.mm.pluginsdk.model.app  包），但字段名  f/i/x2/K/A2/w2  可能随版本变。上 DexKit 时用  using_strings=["MicroMsg.AppMsgLogic"]  定位  k0 ，再顺藤摸瓜。
3.  
 yp3  表插入失败不会抛异常， x1()  返回 boolean，务必打日志。
4.  
线程： k0.I()  内部会走 NetScene，建议 post 到微信自己的队列或主线程，别在 Hook 线程直接调。
5.  
别在  f9.Bb  的 hook 里同步调  k0.I() ——会重入 insert，先用 Handler post 出去。
6.  
引用 + @ 同时用时， atMap  要通过  msgQuoteItem.n = ou5.c1.f(qSrc, atMap, 1)  合并进 msgSource（实证路径），不要自己拼 XML。
推荐落地顺序
1.  
Step 1：只上方案 A（文本式 + @），验证 AI 闭环。
2.  
Step 2：Hook  ChatFooter  的  V0 ，打印  rVar  的所有字段值，拿到一份真机引用消息的完整字段快照（尤其是  x2  里  n / p / r  和 content 里的  <refermsg>  全貌）。
3.  
Step 3：拿着快照比对程序化构造的  MsgQuoteItem ，缺什么补什么。
4.  
Step 4：确认  yp3  关系表插成功后，气泡才正常。这步没过 → 气泡显示成"消息不存在"或掉成纯文本。
