# 微信 Xposed 模块开发 — 完整类参考手册

> **版本**: 微信 8.0.76 (2800)  |  **扫描范围**: 全量 DEX  |  **类总数**: ~100000+
> 
> 本文档涵盖所有可用于 Xposed Hook 的关键类、方法、调用链。

---

## 目录

1. [包结构总览](#一包结构总览)
2. [UI 层 — 聊天窗口](#二ui-层--聊天窗口-chatting)
3. [UI 层 — 通讯录](#三ui-层--通讯录-contact)
4. [UI 层 — 会话列表](#四ui-层--会话列表-conversation)
5. [UI 层 — 发现页](#五ui-层--发现页)
6. [UI 层 — 设置页](#六ui-层--设置页-setting)
7. [UI 层 — 主界面](#七ui-层--主界面-launcher)
8. [数据层 — 存储 Storage](#八数据层--存储-storage)
9. [数据层 — 数据库 WCDB](#九数据层--数据库-wcdb)
10. [业务逻辑层 — 联系人](#十业务逻辑层--联系人-contact)
11. [业务逻辑层 — 消息收发](#十一业务逻辑层--消息收发)
12. [业务逻辑层 — 头像 Avatar](#十二业务逻辑层--头像-avatar)
13. [业务逻辑层 — 红包 LuckyMoney](#十三业务逻辑层--红包-luckymoney)
14. [业务逻辑层 — 撤回 Revoke](#十四业务逻辑层--撤回-revoke)
15. [插件模块 — Plugin 体系](#十五插件模块--plugin-体系)
16. [SDK 平台工具层](#十六sdk-平台工具层)
17. [自动生成事件层](#十七自动生成事件层-autogen)
18. [Hooking 速查表](#十八hooking-速查表)
19. [类名混淆应对策略](#十九类名混淆应对策略)

---

## 一、包结构总览

```
com.tencent.mm/
│
├── ui/                          ← UI 层 (Activity/Fragment/View)
│   ├── LauncherUI.java          ★ 主界面 (4Tab)
│   ├── MMActivity.java          ★ Activity 基类
│   ├── MMFragment.java             Fragment 基类
│   ├── ga.java                  ★ ActionBar 控制器
│   ├── t1.java                     Tab 接口
│   ├── chatting/                ★ 聊天 (843+1240+202=2285 类)
│   ├── contact/                   通讯录 (494 类)
│   ├── conversation/              会话列表 (>200 类)
│   └── ...
│
├── storage/                     ← 数据存储层 (594 类)
│   ├── j4.java                  ★ ContactStorage (联系人)
│   ├── i4.java                     ContactStorage 包装
│   ├── f9.java                  ★ MsgInfoStorage (消息存储)
│   ├── y3.java                  ★ Contact 实体
│   ├── e9.java                  ★ MsgInfo 实体 (消息)
│   ├── c4.java                  ★ MessengerService 接口
│   └── ...
│
├── plugin/                      ← 插件模块 (5000+ 类)
│   ├── luckymoney/              ★ 红包
│   ├── profile/                    个人信息
│   ├── setting/                 ★ 设置
│   ├── finder/                    视频号
│   ├── sns/                       朋友圈
│   ├── appbrand/                  小程序
│   ├── messenger/               ★ 消息
│   ├── webview/                   WebView
│   └── ...
│
├── model/                       ← 业务模型层
│   ├── modelavatar/             ★ 头像
│   ├── newabtest/                  AB测试
│   └── ...
│
├── contact/                     ← 联系人体系 (22 类)
├── sdk/                         ← 平台工具 (338 类)
├── autogen/                     ← 自动生成事件 (1000+ 类)
├── app/                         ← Application
└── boot/                        ← 启动初始化
```

---

## 二、UI 层 — 聊天窗口 (chatting)

### 2.1 核心入口

| 类名 | 完整路径 | 职责 | Hook 方法 |
|---|---|---|---|
| **ChattingUI** | `com.tencent.mm.ui.chatting.ChattingUI` | 聊天 Activity | `onCreate`, `onResume` |
| **ChattingUIFragment** | `com.tencent.mm.ui.chatting.ChattingUIFragment` | 聊天 Fragment | `dealContentView`, `onResume` |
| **BaseChattingUIFragment** | `com.tencent.mm.ui.chatting.BaseChattingUIFragment` | 基类 | `onCreate` |

### 2.2 聊天背景

| 类名 | 职责 | Hook 方法 |
|---|---|---|
| **ChattingImageBGView** | 聊天背景图片 View | `setImageBitmap(Bitmap)` ★ |

### 2.3 消息渲染 (viewitems — 1240 类)

| 类名 | 包 | 职责 | Hook 方法 |
|---|---|---|---|
| **a0** | `viewitems.a0` | ★ ChattingItem 抽象基类 | `S(View,fd5.d,e9)→boolean` |
| **g0** | `viewitems.g0` | ★ ViewHolder | `create(View)`, fields: `avatarIV`, `userTV`, `timeTV` |
| **b4** | `viewitems.b4` | 红包消息渲染 | `S()`, `e0()` |
| **c2** | `viewitems.c2` | AppMsg/系统消息 | `S()`, `G()`, `m()` |
| **h** | `viewitems.h` | 文字消息 | `S()` 等 |
| **a4** | `viewitems.a4` | 消息条目装饰 | `a(View,boolean)` |
| **c6** | `viewitems.c6` | Attach 监听 | `onViewAttachedToWindow` |
| **d6** | `viewitems.d6` | 消息数据 | `invoke()` |
| **e6** | `viewitems.e6` | 消息 View 设置 | `a(View,boolean)` |

### 2.4 长按菜单 (component — 843 类)

| 类名 | 包 | 职责 | Hook 方法 |
|---|---|---|---|
| **mk** | `component.mk` | ★ 消息操作菜单 | `l0(MenuItem,e9)→boolean` |
| **ad** | `component.ad` | ★ 菜单项点击处理 | `onMMMenuItemSelected(MenuItem,int)` |
| **a7** | `component.a7` | ★ 菜单执行 Runnable | `run()` |
| **cd** | `component.cd` | 菜单上下文数据 | 构造方法 |
| **ol** | `component.ol` | 菜单生命周期 | `K()` |
| **yl** | `component.yl` | 菜单项构建 | `a(String,int)`, `b(String,int,float)` |
| **ah** | `component.ah` | 多媒体菜单 | `l0()`, `i0()` |
| **zh** | `component.zh` | 菜单显示 | — |

### 2.5 长按事件 (presenter — 202 类)

| 类名 | 包 | 职责 | Hook 方法 |
|---|---|---|---|
| **g5** | `presenter.g5` | ★ 长按事件处理 | `a(View,int,f0)`, `b(View,int,f0)` |
| **m5** | `presenter.m5` | 主 Presenter | — |

### 2.6 适配器 (adapter — 40+ 类)

| 类名 | 包 | 职责 |
|---|---|---|
| **q** | `adapter.q` | ChatHolder 接口 |
| **a** | `adapter.a` | 适配器基类 |
| **f0** | `adapter.f0` | 适配器数据 |

### 2.7 底部输入栏

| 类名 | 职责 |
|---|---|
| **ChattingFooterMoreBtnBar** | "+"按钮展开栏 |
| **ChattingAnimFrame** | 动画帧 |

---

## 三、UI 层 — 通讯录 (contact)

| 类名 | 完整路径 | 职责 | Hook 方法 |
|---|---|---|---|
| **AddressUI** | `com.tencent.mm.ui.contact.AddressUI` | 通讯录主页 | `onCreate` |
| **ContactInfoUI** | `com.tencent.mm.plugin.profile.ui.ContactInfoUI` | 联系人详情 | `onCreate` |
| **OpenIMAddressUI** | `com.tencent.mm.ui.contact.OpenIMAddressUI` | 企业微信通讯录 | `onCreate` |
| **SnsAddressUI** | `com.tencent.mm.ui.contact.SnsAddressUI` | 朋友圈通讯录选择 | `onCreate` |
| **SelectContactMvvmList** | `com.tencent.mm.ui.mvvm.list.SelectContactMvvmList` | MVVM 联系人选择列表 | — |

---

## 四、UI 层 — 会话列表 (conversation)

| 类名 | 完整路径 | 职责 |
|---|---|---|
| **ConversationUI** | `com.tencent.mm.ui.conversation.ConversationUI` | 会话列表主页 |
| **OpenIMKefuServiceConversationUI** | `com.tencent.mm.ui.conversation.OpenIMKefuServiceConversationUI` | 客服会话 |

---

## 五、UI 层 — 发现页

| 类名 | 职责 |
|---|---|
| `com.tencent.mm.plugin.finder.ui.FinderHomeUI` | 视频号首页 |
| `com.tencent.mm.plugin.sns.ui.SnsTimeLineUI` | 朋友圈 |
| `com.tencent.mm.plugin.sns.ui.SnsUserUI` | 朋友圈个人页 |

---

## 六、UI 层 — 设置页 (setting)

### 6.1 新版设置 (setting_new — 178 类)

| 类名 | 职责 | Hook 方法 |
|---|---|---|
| **MainSettingsUI** | ★ 设置主页 (我→设置) | `onCreate` |
| **SearchSettingsUI** | 设置搜索 | `onCreate` |
| **CommonSettingsUI** | 通用设置中间页 | `onCreate` |
| **BaseSettingUI** | 设置页基类 ★ (Hook 一次覆盖所有) | `onCreate` |
| **BaseSettingPrefUI** | 设置列表基类 | `onCreate` |

### 6.2 设置子模块 (settings/ 下 146 类)

| 分组类 | 对应设置项 |
|---|---|
| `SettingGroupPersonalInfo` | 个人信息 |
| `SettingGroupAccountInfo` | 帐号与安全 |
| `SettingGroupMain` | 通用设置 |
| `SettingGroupNotify` | 新消息通知 |
| `SettingGroupFriendPrivacy` | 朋友权限 |
| `SettingGroupPrivacyPermission` | 个人信息与权限 |
| `SettingGroupChatting` | 聊天 |
| `SettingGroupDisplay` | 显示 |
| `SettingGroupAboutItem` | 关于微信 |
| `SettingGroupHelpItem` | 帮助与反馈 |
| `SettingGroupCleanItem` | 存储空间 |
| `SettingGroupMore` | 更多 |
| `SettingGroupOther` | 其他 |

### 6.3 旧版设置页面 (setting/ — 58 类)

| 类名 | 职责 |
|---|---|
| `SettingsPersonalInfoPreviewUI` | 个人信息预览 |
| `SettingsModifyNameUI` | 修改昵称 |
| `SettingsAliasUI` | 微信号 |
| `SettingsLanguageUI` | 多语言 |
| `SettingsFontUI` | 字体大小 |
| `SettingsChattingBackgroundUI` | ★ 聊天背景设置 |
| `SettingsAboutMicroMsgUI` | 关于微信 (蓝底) |
| `SettingsAuthUI` | 授权管理 |
| `EditSignatureUI` | 编辑签名 |
| `SettingsCareModeIntro` | 关怀模式 |

### 6.4 设置细分模块

| 子包 | 类数 | 说明 |
|---|---|---|
| `notify/` | 16 | 新消息通知 |
| `privacy_permission/` | 5 | 隐私权限 |
| `other/` | 37 | 通用→其他 (含 accessibility 辅助功能) |
| `display/` | ~5 | 显示 (深色模式/字体) |
| `chatting/` | ~5 | 聊天设置 |
| `acount_info/` | 12 | 帐号安全 |
| `more/` | 2 | 更多 (设备/青少年模式) |

---

## 七、UI 层 — 主界面 (Launcher)

| 类名 | 完整路径 | 职责 | Hook 方法 |
|---|---|---|---|
| **LauncherUI** | `com.tencent.mm.ui.LauncherUI` | ★ 主界面 Activity | `onCreate`, `onResume` |
| **MMActivity** | `com.tencent.mm.ui.MMActivity` | Activity 基类 | `onCreate`, `onResume` |
| **ga** | `com.tencent.mm.ui.ga` | ★ ActionBar 管理器 | `p0(int)`, `E0(int)` |
| **t1** | `com.tencent.mm.ui.t1` | Tab 接口 | `setTo(int)` |
| **af** | `com.tencent.mm.ui.af` | ActionBar 辅助 | — |

---

## 八、数据层 — 存储 (Storage)

### 8.1 核心存储类

| 类名 | 完整路径 | 职责 | 关键方法 |
|---|---|---|---|
| **j4** | `com.tencent.mm.storage.j4` | ★ ContactStorage 联系人 | `m(str)→y3`, `n(str,bool)→y3`, `D()→Cursor`, `A(...)→Cursor` |
| **i4** | `com.tencent.mm.storage.i4` | ContactStorage 包装 | `get(str)→y3`, `n0(y3,bool)→int` |
| **f9** | `com.tencent.mm.storage.f9` | ★ MsgInfoStorage 消息 | `Ra(long,e9)→int` (更新消息), `t6(long)→e9` |
| **d8** | `com.tencent.mm.storage.d8` | 另一联系人存储 | — |
| **e9** | `com.tencent.mm.storage.e9` | ★ MsgInfo 消息实体 | `getType()`, `getMsgId()`, `f2()→String`, `P2()→boolean` |
| **y3** | `com.tencent.mm.storage.y3` | ★ Contact 联系人实体 | `t0()→nickname`, `d1()→username`, `M0()→remark` |
| **c4** | `com.tencent.mm.storage.c4` | MessengerService 接口 | `getDBInfo()` |
| **g4** | `com.tencent.mm.storage.g4` | ContactLabelStorage | `k1()→ArrayList`, `l1()→ArrayList`, `F1(str)→labelId` |

### 8.2 继承关系

```
s85.f0 (DB 实体基类)
  └── dm.f2 (BaseContact)
        └── com.tencent.mm.contact.s (联系人扩展)
              └── y3 (联系人实体 ★)
        └── Cloneable (接口)

dm.c8 (消息基类)
  └── e9 (消息实体 ★)

s85.e0 (DBInfo 接口)
  └── c4 (Messenger DB Info)
```

### 8.3 获取 Storage 实例

```java
// 服务定位器模式
Class<?> j1 = cl.loadClass("hm0.j1");
Class<?> c4 = cl.loadClass("sh3.c4");
Object service = j1.getMethod("s", Class.class).invoke(null, c4);

// service → h2 实例
Object h2 = (com.tencent.mm.plugin.messenger.foundation.h2) service;

// 获取 ContactStorage
Object contactStg = h2.getClass().getMethod("ij").invoke(h2);  // → j4

// 获取 MsgInfoStorage
Object msgStg = h2.getClass().getMethod("wj").invoke(h2);      // → f9
```

---

## 九、数据层 — 数据库 (WCDB)

| 类名 | 完整路径 | 职责 | 关键方法 |
|---|---|---|---|
| **ka5.f** | `ka5.f` | ★ WCDB 数据库封装 | `s(path,pwd,flags,WAL)→f` (打开), `u(sql,args)→Cursor`, `c()` (关闭) |
| **ka5.b0** | `ka5.b0` | ★ SqliteDB 包装 | `R(...)→boolean` (初始化), `S(...)→boolean` (系统DB) |
| **ka5.a** | `ka5.a` | DB 内部辅助 | — |

### 9.1 打开加密数据库

```java
// 步骤: 路径 + 密码 → s() → 返回 db wrapper → u() 查询 → c() 关闭
String dbPath = "/data/user/0/com.tencent.mm/MicroMsg/{hash}/EnMicroMsg.db";
String pwd = MD5(IMEI + uin).substring(0, 7);

Class<?> ka5f = cl.loadClass("ka5.f");
Method s = ka5f.getDeclaredMethod("s",
    String.class, String.class, int.class, boolean.class);
Object db = s.invoke(null, dbPath, pwd, 0, true);

Method u = db.getClass().getDeclaredMethod("u", String.class, String[].class);
Cursor c = (Cursor) u.invoke(db, "SELECT ... FROM rcontact", null);

Method close = db.getClass().getDeclaredMethod("c");
close.invoke(db);
```

---

## 十、业务逻辑层 — 联系人 (Contact)

### 10.1 联系人实体 y3

```java
// 完整路径: com.tencent.mm.storage.y3
// 继承: com.tencent.mm.contact.s → dm.f2 → s85.f0

// 关键 getter (来自 dm.f2 / contact.s):
y3.d1()  → username        (wxid_xxx 或自定义号)
y3.t0()  → nickname         (昵称)
y3.M0()  → conRemark        (备注名)
y3.P0()  → alias            (微信号)
y3.Q0()  → pyInitial        (拼音首字母)
y3.getType()   → type       (4=好友, 3/2=群聊, 33=系统号)
y3.getCreateTime() → 创建时间
y3.v0()  → encryptUsername  (加密用户名)
y3.f1()  → contactLabelIds  (标签ID)
```

### 10.2 联系人分类 (基于 type 值)

| type | 数量 (8.0.76) | 分类 | username 特征 |
|---|---|---|---|
| 4 | 13950 | ★ 好友 | wxid_ 或自定义号 |
| 3 | 99 | 群聊 / 公众号 | @chatroom 或 gh_ |
| 2 | 28 | 群聊 | @chatroom |
| 33 | 24 | 系统号 | 固定英文名 |
| 2050 | 2 | 群聊 | @chatroom |
| 2049 | 1 | 文件传输助手 | filehelper |
| 2051 | 1 | 群聊 | @chatroom |
| 1 | 1 | 陌生人 | wxid_ |
| 259 | 1 | 未分类 | wxid_ |

### 10.3 获取联系人

```java
// 方法1: 通过 ContactStorage
Object contactStg = ...; // 获取 j4 实例
Object contact = contactStg.getClass()
    .getMethod("m", String.class)
    .invoke(contactStg, "wxid_xxx");  // → y3

// 方法2: 直接数据库查询
Cursor c = db.u("SELECT * FROM rcontact WHERE username=?", new String[]{"wxid_xxx"});
```

---

## 十一、业务逻辑层 — 消息收发

### 11.1 核心类

| 类名 | 职责 | 关键方法 |
|---|---|---|
| `com.tencent.mm.plugin.messenger.foundation.h2` | ★ 消息基础服务 | `ij()→j4`, `wj()→f9`, `kj()→d8` |
| `com.tencent.mm.storage.f9` | ★ MsgInfoStorage | `Ra(long,e9)→int` (更新消息) |
| `com.tencent.mm.modelgetchatroommsg.h` | 获取群聊消息 | `a(...)` |
| `com.tencent.mm.modelsimple.d1` | 简单消息发送 | `J(...)` |

### 11.2 红包消息类型

```
MSG_TYPE_RED_ENVELOPE = 436207665  (0x1A000031)
```

### 11.3 防撤回双路径

```
路径1 (XML):  收到撤回 XML → af5.b.b() 识别 "revokemsg"
             → af5.b.c() 超时检查 → af5.a.run()
             → msg.setType(10002)  ← Hook 点

路径2 (Proto): 收到撤回 Proto → e01.u.q7()
              → e01.u.f()  ← Hook 点
```

---

## 十二、业务逻辑层 — 头像 (Avatar)

### 12.1 核心类

| 类名 | 完整路径 | 职责 |
|---|---|---|
| **z** | `com.tencent.mm.modelavatar.z` | ★ 头像服务 |
| **x** | `com.tencent.mm.modelavatar.x` | SmallBM 管理 |
| **AvatarStorage** | `com.tencent.mm.modelavatar.AvatarStorage` | 头像存储 |

### 12.2 头像路径公式

```
accountDir = j1.u().h()  // 账号目录
md5 = MD5(username)
avatar = accountDir + "avatar/" + md5[0:2] + "/" + md5[2:4] + "/user_" + md5 + ".png"
hdAvatar = accountDir + "avatar/" + md5[0:2] + "/" + md5[2:4] + "/user_hd_" + md5 + ".png"
```

### 12.3 头像方法

```java
// z.f(username, isHd, unused) → 头像文件路径
// z.e(username, tag, isHd) → Bitmap
// z.h(context) → 默认头像 Bitmap
// z.d(sb, username, isHd) → 拼接头像路径到 StringBuilder
```

---

## 十三、业务逻辑层 — 红包 (LuckyMoney)

### 13.1 核心类

| 类名 | 完整路径 | 职责 |
|---|---|---|
| **LuckyMoneyNewReceiveUI** | `com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyNewReceiveUI` | ★ 红包收取界面 |
| **LuckyMoneyNotHookReceiveUI** | `com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyNotHookReceiveUI` | 备用收取界面 |
| **LuckyMoneyBeforeDetailUI** | `com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyBeforeDetailUI` | 红包详情前页 |
| **LuckyMoneyDetailUI** | `com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyDetailUI` | 红包详情 |
| **LuckyMoneyBusiReceiveUI** | `com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyBusiReceiveUI` | 企业红包收取 |
| **LuckyMoneyHKReceiveUI** | `com.tencent.mm.plugin.luckymoney.hk.ui.LuckyMoneyHKReceiveUI` | 香港红包 |
| **LuckyMoneyPrepareUI** | `com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyPrepareUI` | 发红包页面 |
| **f6** | `com.tencent.mm.plugin.luckymoney.model.f6` | 红包网络请求 NetSceneOpenLuckyMoney |
| **URISpanHandlerSet$LuckyMoneyUriSpanHandler** | — | 聊天中红包消息点击处理 |

### 13.2 抢红包 Hook 点

```java
// LuckyMoneyNewReceiveUI.initView() → after
// → 递归找 Button → "开" → performClick()

// 覆盖所有收取界面:
Class[] receiveUIs = {
    "com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyNewReceiveUI",
    "com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyNotHookReceiveUI",
    "com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyBusiReceiveUI",
    "com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyBusiReceiveUIV2",
    "com.tencent.mm.plugin.luckymoney.hk.ui.LuckyMoneyHKReceiveUI",
};
```

---

## 十四、业务逻辑层 — 撤回 (Revoke)

| 类名 | 完整路径 | 职责 | Hook 方法 |
|---|---|---|---|
| **af5.a** | `af5.a` | ★ XML 撤回 Runnable | `run()` — Hook before → setResult(null) |
| **af5.b** | `af5.b` | XML 撤回解析 | `b()` (还调用了 `c()`) |
| **e01.u** | `e01.u` | ★ Protobuf 撤回 Handler | `f(String,long,p0,String,String,String)` |
| **f21.c** | `f21.c` | 撤回基类 | `a()`, `b()` |
| **RevokeMsgEvent** | `com.tencent.mm.autogen.events.RevokeMsgEvent` | 撤回事件 | — |
| **RevokeMsgListener** | `com.tencent.mm.ui.chatting.RevokeMsgListener` | 撤回监听 | `c(e9)`, `callback(IEvent)` |

---

## 十五、插件模块 — Plugin 体系

### 15.1 核心插件

| 插件模块 | 包路径 | 核心类 | 功能 |
|---|---|---|---|
| **messenger** | `plugin.messenger.foundation` | h2 | 消息基础服务 |
| **luckymoney** | `plugin.luckymoney` | LuckyMoney*UI | 红包 |
| **profile** | `plugin.profile.ui` | ContactInfoUI | 个人信息 |
| **setting** | `plugin.setting.ui` | MainSettingsUI | 设置 |
| **finder** | `plugin.finder` | Finder*UI | 视频号 |
| **sns** | `plugin.sns.ui` | SnsTimeLineUI | 朋友圈 |
| **appbrand** | `plugin.appbrand` | AppBrandUI | 小程序 |
| **webview** | `plugin.webview.ui` | WebViewUI | WebView |
| **fav** | `plugin.fav.ui` | FavUI | 收藏 |
| **emoji** | `plugin.emoji.ui` | EmojiUI | 表情 |
| **wallet** | `plugin.wallet` | WalletUI | 钱包 |
| **label** | `plugin.label.ui` | ContactManagerUI | 标签 |
| **nearby** | `plugin.nearby.ui` | NearbyUI | 附近的人 |
| **story** | `plugin.story.ui` | StoryUI | 视频动态 |
| **brandservice** | `plugin.brandservice` | BrandServiceUI | 公众号 |
| **ringtone** | `plugin.ringtone.ui` | RingtoneUI | 铃声 |
| **mall** | `plugin.mall.ui` | MallUI | 购物 |
| **game** | `plugin.game` | GameUI | 游戏 |
| **music** | `plugin.music` | MusicUI | 音乐 |
| **clean** | `plugin.clean` | CleanUI | 清理 |

---

## 十六、SDK 平台工具层

| 类名 | 包 | 职责 |
|---|---|---|
| **x2** | `com.tencent.mm.sdk.platformtools.x2` | Application Context 持有者 |
| **t8** | `com.tencent.mm.sdk.platformtools.t8` | 字符串工具 `K0(str)→isEmpty` |
| **k** | `kk.k` | ★ MD5 工具 `g(byte[])→String` |
| **w6** | `w6` | 文件工具 `E(path)→InputStream`, `j(path)→boolean` |
| **b0** | `com.tencent.mm.sdk.platformtools.b0` | 编码工具 `O(str)→转义SQL` |
| **r2** | `com.tencent.mm.sdk.platformtools.r2` | 线程工具 |
| **n3** | `com.tencent.mm.sdk.platformtools.n3` | 延迟/定时器 |
| **Log** | `com.tencent.mars.xlog.Log` | ★ 微信日志 |

---

## 十七、自动生成事件层 (autogen)

| 类名 | 职责 |
|---|---|
| `RevokeMsgEvent` | 撤回消息事件 |
| `DoingInitContactEvent` | 联系人初始化事件 |
| `SelectContactListToOutsideEvent` | 联系人选择事件 |
| `SnsLuckyMoneyQunZhongKVReportEvent` | 红包统计事件 |
| `FinderPrivateMsgBubbleEvent` | 视频号私信气泡 |
| `OnSubCoreInitedEvent` | 子插件初始化完成 |

---

## 十八、Hooking 速查表

### 18.1 防撤回

```java
findClass("af5.a")  →  hookAllMethods → "run"   → before → setResult(null)
findClass("e01.u")  →  hookAllMethods → "f"     → before → setResult(null)
```

### 18.2 抢红包

```java
findClass("com.tencent.mm.plugin.luckymoney.ui.LuckyMoneyNewReceiveUI")
  → hookAllMethods → "initView" → after → 递归找Button("开") → performClick()
```

### 18.3 聊天背景

```java
findClass("com.tencent.mm.ui.chatting.ChattingImageBGView")
  → hookAllMethods → "setImageBitmap" → before → param.args[0] = myBitmap
```

### 18.4 聊天气泡换肤

```java
findClass("com.tencent.mm.ui.chatting.viewitems.a0")
  → 遍历子类 hookAllMethods → "S" → after → 对 itemView 换肤
```

或

```java
findClass("com.tencent.mm.ui.chatting.viewitems.g0")
  → hookAllMethods → "create" → after → addOnLayoutChangeListener
```

### 18.5 全局主题

```java
Resources.class → hookAllMethods → "getColor" → after → 颜色替换
Activity.class → hookAllMethods → "onCreate" → after → 改页面背景
TextView.class → hookAllMethods → "setTextColor" → before → 颜色替换
```

### 18.6 联系人

```java
// 数据库方案
hm0.b0.e(uin) → 路径
wo.w0.g(true) → IMEI → 密码
ka5.f.s(path,pwd,0,true) → 打开数据库
db.u("SELECT * FROM rcontact WHERE type=4") → 查询

// 或反射方案
j1.s(c4.class) → service
service.ij() → ContactStorage
ContactStorage.m(username) → y3
```

### 18.7 聊天长按菜单换肤

```java
findClass("com.tencent.mm.ui.chatting.presenter.g5")
  → hookAllMethods → "a" → before → 拦截长按

findClass("com.tencent.mm.ui.chatting.component.mk")
  → hookAllMethods → "l0" → before → 拦截菜单点击
```

### 18.8 ActionBar 换肤

```java
findClass("com.tencent.mm.ui.MMActivity")
  → hookAllMethods → "onResume" → after → 遍历View树找 ActionBar → setBackgroundColor
```

### 18.9 设置页换肤

```java
findClass("com.tencent.mm.plugin.setting.ui.setting_new.base.BaseSettingUI")
  → hookAllMethods → "onCreate" → after → 改背景
```

---

## 十九、类名混淆应对策略

### 19.1 混淆特点

微信 8.0.76 的混淆特点：
- **2-3 层包名**: `e01`, `af5`, `f21`, `hm0`, `ka5`, `kk`, `wo`, `mp0` 等是顶层混淆包
- **短类名**: 混淆包内的类名是 1-2 个字母 (`a`, `b`, `u`, `f`, `g`, `k`, `m`, `s`, `q7`)
- **plugin/sdk/ui/storage 子包不混淆**: `com.tencent.mm.plugin.*` / `.ui.*` / `.storage.*` / `.sdk.*` 下面的业务类名保留了可读性

### 19.2 DexKit 字符串特征定位

| 目标类 | 特征字符串 | 搜索方式 |
|---|---|---|
| `ka5.f` (WCDB) | `"EnMicroMsg.db"` | `findClassByString` |
| `af5.a` (XML撤回) | `"checkExpired:"` | `findClassByString` |
| `e01.u` (Proto撤回) | `"doRevokeMsg"` | `findClassByString` |
| `kk.k` (MD5) | `"0123456789abcdef"` | `findClassByString` |
| `wo.w0` (IMEI) | `"1234567890ABCDEF"` | `findClassByString` |
| `hm0.b0` (uin hash) | `"mm"` + MD5 签名 | 方法签名搜索 |
| `mp0.b` (数据目录) | `"/MicroMsg/"` | `findClassByString` |
| `j4` (ContactStorage) | `"MicroMsg.ContactStorage"` | `findClassByString` |
| `a0` (ChattingItem基类) | `"chatting/viewitems/a0"` | 父类搜索 |
| `g0` (ViewHolder) | `"avatarIV"` / `"userTV"` | 字段搜索 |

### 19.3 常量字符串索引

| 字符串 | 出现类 | 用途 |
|---|---|---|
| `"MicroMsg.ContactStorage"` | j4 | 联系人存储日志 |
| `"MicroMsg.InvokeMessageNewXmlMsg"` | af5.a/af5.b | XML 撤回日志 |
| `"MicroMsg.BigBallSysCmdMsgConsumer"` | e01.u | Proto 撤回日志 |
| `"MicroMsg.LuckyMoneyNewReceiveUI"` | LuckyMoneyNewReceiveUI | 红包界面日志 |
| `"MicroMsg.LuckyMoneyBeforeDetailUI"` | LuckyMoneyBeforeDetailUI | 红包详情 |
| `"MicroMsg.AvatarStorage"` | modelavatar.x | 头像存储 |
| `"MicroMsg.MMDataBase"` | ka5.f | WCDB 数据库 |
| `"MicroMsg.DBInit"` | ka5.b0 | DB 初始化 |
| `"MicroMsg.SqliteDB"` | ka5.b0 | SqliteDB |
| `"MicroMsg.BaseContact"` | dm.f2 | BaseContact基类 |
| `"revokemsg"` | af5.b | 撤回类型标识 |

---



## 附录 B: 常用反射工具方法

```java
// 获取微信 Context
Context ctx = (Context) XposedHelpers.getStaticObjectField(
    cl.loadClass("com.tencent.mm.sdk.platformtools.x2"), "a");

// 获取服务
Object service = XposedHelpers.callStaticMethod(
    cl.loadClass("hm0.j1"), "s", cl.loadClass("sh3.c4"));

// 获取 uin
SharedPreferences sp = ctx.getSharedPreferences("system_config_prefs", 0);
long uin = Long.parseLong(sp.getAll().get("default_uin").toString());

// 获取 IMEI
String imei = (String) XposedHelpers.callStaticMethod(
    cl.loadClass("wo.w0"), "g", true);

// MD5
String md5 = (String) XposedHelpers.callStaticMethod(
    cl.loadClass("kk.k"), "g", (Object) input.getBytes());

// 通用反射调实例方法
XposedHelpers.callMethod(obj, "methodName", arg1, arg2);

// 通用反射调静态方法
XposedHelpers.callStaticMethod(clazz, "methodName", arg1, arg2);

// 通用反射读字段
XposedHelpers.getObjectField(obj, "fieldName");

// 通用反射设置字段
XposedHelpers.setObjectField(obj, "fieldName", value);
```

---

> **全文统计**: 19 章节  |  涵盖 100+ 关键类  |  6 套完整 Hook 方案  |  10+ 字符串特征索引
> 
> 基于微信 8.0.76 (2800) 全量 DEX 扫描分析
