分析完成。以下是微信语音转发完整链路的地毯式逆向结果 + 二次审查结论 + 交付文件。
一、核心结论（先看结论）
微信原生禁止转发语音（资源串： 你选择的消息中，语音、动画表情、名片…不能转发给朋友 ），长按单条语音无"转发"入口。要实现语音转发，正确路线是：
复制语音文件 → 为目标会话插入  voiceinfo  记录 → 调  v61.d1.u()  创建 type=34 消息并触发  SceneVoiceService  上传 —— 与微信自己"录音→发送"完全同一条链路。
二、语音消息存储模型（已逐字段核实）
消息主表
 com.tencent.mm.storage.e9  (MsgInfo) /  f9  (MessageStorage)
语音 type=34； getMsgId() 本地id、 F0() svrId、 N0() talker、 j() 语音文件名
语音表
 v61.c1  (VoiceMsgInfo) /  v61.m1  (VoiceStorage)
voiceinfo 表，FileName 主键；字段映射： b=FileName c=User e=MsgId h=TotalLen i=Status l=VoiceLength(秒) m=MsgLocalId n=Human v=VoiceFlag p=format t=MasterBufId w=VoiceInfoExt x=MsgTalker 
语音文件
 voice2/  目录（ vfs.t7.b("voice2") ）
录制落盘路径= k1.d(voice2/,"msg_",fileName,".amr",2,true)  =  voice2/<hash>msg_<名>.amr 
三、语音"发送"完整链路（实现转发的基准）
k4.u (ChatFooter录音停止)
  → v61.d1.u(fileName, voiceLen, voiceFlag, quotedMsg, msgSource)   ★ 发送唯一入口
       ├─ d1.k(fileName) 取voiceinfo，状态改3
       ├─ b1.g(null,fileName) 读文件大小(必须>0 → 文件必须已存在)
       ├─ new e9: setType(34) setTalker setStatus setIsSend(1)
       │          setContent(fileName) setVoiceFlag setCreateTime setMsgSource
       ├─ aa.y(e9) → f9.yb 写消息DB，返回 msgLocalId
       ├─ voiceinfo回填 k.m(msgLocalId) k.x(talker)
       └─ rn3.u0.Dj(e9,fileName,false) → pv.p0.bj 解析C2C语音路径
  → v61.v0.dj().e()   ★ 踢发送队列
       └─ yl.y0 (SceneVoiceService).e() → postToWorker → 处理voiceinfo队列
  → v61.o (NetSceneUploadVoice) doScene分块上传 /cgi-bin/micromsg-bin/uploadvoice
       └─ onGYNetEnd 成功回填 msgSvrId
四、原生"转发"链路（被挡在哪）
a4.b(Context,List<e9>,...) 构建转发Intent(Retr_Msg_Id_List/Retr_Msg_Type)
  → MsgRetransmitUI(o7 → t0.d.r(qs5.u1,"MultiSendMsg"))
  → qs5.u1.run → com.tencent.mm.ui.chatting.w3.a()   ← 逐条遍历
  → a4.h(Context, target, e9, z, z2, str2)           ← 逐条转发执行点
       语音不命中任何媒体分支 → Retr_Msg_Type=2 → 原生直接丢弃
拦截点确定： a4.h （多选逐条转发必经，且传入的语音消息是原对象，msgId/fileName 完整可用——已核实  ma.B()  对普通语音返回 false，不会转成占位克隆）。
五、实现方案（已写入插件 VoiceForward）
forwardVoiceMsg(srcMsg, target):
  1. 取源voiceinfo: v61.v0.ej().x1(talker,msgLocalId)  (备选 u1(svrId) / d1.k(content))
  2. 源文件路径: rn3.u0.Ej(ou5.x.i.a(e9,ou5.e0.u), fileName, false)
  3. 新文件名: v61.m1.b1(target, v61.b1.c(fileName)前缀)
  4. 目标路径(录制落盘路径): k1.d(voice2/,"msg_",newFileName,".amr",2,true)
  5. vfs.z6.c 复制文件
  6. 插voiceinfo(新FileName+User=target+Status=1+MsgTalker="")  y1()
  7. v61.d1.u(newFileName, voiceLen, voiceFlag, null, "")  → 创建消息+入队
  8. v61.v0.dj().e()  → 踢 SceneVoiceService 上传
Hook 三处：
•  
 a4.h  Before：type==34 →  forwardVoiceMsg  + skipWith（多选逐条转发接管）
•  
 a4.b  Before：单条语音 → 弹窗输入目标 wxid 直接转发（原生单条必失败）；多选含语音 → 放行
•  
 a4.c  Before：编辑模式多选放行
六、二次审查（关键点复核结论 ✅）
1.  
 v61.d1.u  参数顺序  (String,int,int,e9,String)  ✅（smali 逐行核实， p1→VoiceLength 、 p2→VoiceFlag 、 p3→引用消息 、 p4→MsgSource ）
2.  
发送前文件必须先复制到录制路径（ d1.u  内  b1.g  读文件大小，不存在直接 false）✅
3.  
 v61.v0.dj()  返回  yl.y0 (SceneVoiceService)，jadx 常误显示为 v61.y0 ✅
4.  
新 voiceinfo 的  MsgTalker  必须留空 → 路径解析走录制路径，上传时  pv.p0.fj  再 move 到 C2C 路径 ✅
5.  
文件名前缀沿用源格式(amr_/silk_/opus_/spx_)，内容格式由文件头识别 ✅
6.  
 a4.h  传入的是原始语音消息（ma.B 为 false），msgId 可用 ✅
七、交付文件（已导出，位于  /storage/emulated/0/Android/media/com.tencent.mm/LSPilot/Plugin/ ）
 VoiceForward/ （19.4KB）
可直接使用的语音转发插件（核心引擎 + 3 个 Hook + 弹窗转发 + target.txt 自测）
 VoiceForwardChainDoc/ （12.5KB）
完整链路逆向分析文档（存储模型/发送链路/转发链路/Hook方案/类映射表/注意事项）
使用方法：
1.  
在 LSPilot 中启用  VoiceForward  插件并重启微信
2.  
方式A：聊天中长按语音 → 多选（勾选语音）→ 转发 → 逐条转发 → 选联系人 → 发送，插件自动接管
3.  
方式B：长按单条语音（若菜单出现转发项）→ 插件弹窗让你输入目标 wxid（逗号分隔多个）直接转发
4.  
自测：在插件目录建  target.txt （每行一个 wxid），打开任意聊天窗口，插件自动把该会话最近一条语音转发过去
5.  
日志：Xposed 日志搜  VForward  可跟踪每步结果
注意：混淆类名（v61/yl/ou5/rn3/wb0 等）随微信版本可能变化，若换版本后失效，按文档第三节重新定位  v61.d1.u  与  v61.v0.dj().e()  即可。