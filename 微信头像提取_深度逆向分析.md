# 微信头像提取 — 深度逆向分析

> 基于微信 8.0.x 实测，LSPilot 工具链分析
> 涵盖联系人头像 & 群聊头像，存储层面完全统一

---

## 一、总体架构

```
┌──────────────────────────────────────────────────────┐
│                    UI 层                              │
│  AvatarDrawable (w)  →  ImageView.setImageDrawable     │
│  AnyProcessAvatarAttacher (s)                         │
└────────────────────┬─────────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────────┐
│              逻辑层 (AvatarLogic: g)                  │
│  g.b() 加载 Bitmap     g.c() 获取路径                 │
│  g.h() 触发更新         g.e() 构建数据                 │
└────────────────────┬─────────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────────┐
│             服务层 (AvatarService: r)                  │
│  r.f() 加载（内存→磁盘→网络）                          │
│  r.j() 更新数据                                       │
└──────┬──────────────────────┬────────────────────────┘
       │                      │
┌──────▼────────┐    ┌───────▼──────────┐
│ 内存缓存 (LRU) │    │ 磁盘缓存 (z)      │
│ z.i() 读取    │    │ SmallBM (x)      │
│ z.p() 写入    │    │ .bm / .png 文件   │
└───────────────┘    └──────┬───────────┘
                            │
                    ┌───────▼──────────┐
                    │  DB 层 (s0)       │
                    │  r0: 数据实体     │
                    │  s0.x0() 查询     │
                    │  s0.F0() 保存     │
                    └──────────────────┘
```

---

## 二、核心类映射表

| 混淆类 | 真实身份 | 关键方法 |
|---|---|---|
| `com.tencent.mm.modelavatar.r` | **AvatarService** | `f(username)→Bitmap`、`j(username,r0)→保存` |
| `com.tencent.mm.modelavatar.z` | **AvatarStorage** | `i()→读内存`、`p()→写缓存`、`g()→WCF路径`、`f()→磁盘路径`、`r()→裁剪写入` |
| `com.tencent.mm.modelavatar.g` | **AvatarLogic** | `a()/b()→加载`、`c()→获取路径`、`h()→触发更新`、`e()→构建数据` |
| `com.tencent.mm.modelavatar.r0` | **头像数据实体** | `c()→大图URL`、`d()→小图URL`、`e()→username` |
| `com.tencent.mm.modelavatar.s0` | **头像 DAO** | `x0(username)→r0`、`F0(r0)→保存`、`z0(username)→删除` |
| `com.tencent.mm.modelavatar.x` | **SmallBM 磁盘读取** | `a(path,key)→Bitmap`、`b(path)→是否存在` |
| `com.tencent.mm.modelavatar.f` | **HD 头像回调** | `a(errType,errCode)→int` |
| `com.tencent.mm.modelavatar.h` | **过期检查任务** | `run()` |
| `com.tencent.mm.modelavatar.d1` | **服务定位器** | `ij()→AvatarStorage`、`hj()→AvatarService`、`mj()→DAO` |
| `com.tencent.mm.feature.avatar.w` | **AvatarDrawable** | `hj/kj/lj/mj→设置头像到 ImageView` |
| `com.tencent.mm.feature.avatar.s` | **AnyProcessAvatarAttacher** | `hj(ImageView,username)→跨进程绑定头像` |

---

## 三、头像磁盘路径公式（核心！）

### 3.1 路径构建算法 `z.d()`

```java
// 核心：对 username 做 MD5，取前 4 位作为两级目录
public static StringBuilder d(StringBuilder sb, String username, boolean hd) {
    String md5 = MD5(username.getBytes());  // 32位 hex
    sb.append(md5.substring(0, 2));         // 前 2 位 → 一级目录
    sb.append('/');
    sb.append(md5.substring(2, 4));         // 第 3-4 位 → 二级目录
    sb.append('/');
    sb.append("user_");
    if (hd) sb.append("hd_");               // HD 头像加前缀
    sb.append(md5);                         // 完整 MD5
    sb.append(".png");                      // 扩展名
    return sb;
}
```

### 3.2 完整磁盘路径 `z.f()`

```
{dataDir}/avatar/{md5[0:2]}/{md5[2:4]}/user_{hd_}{md5}.png
```

其中 `dataDir` = `j1.u().h()`（微信数据根目录，通常是 `/data/data/com.tencent.mm/MicroMsg/xxxxxxxxxxxx/`）。

### 3.3 WCF 虚拟路径 `z.g()`

```
wcf://avatar/{md5[0:2]}/{md5[2:4]}/user_{hd_}{md5}.png
```

微信内部 WCF 文件系统统一抽象，底层通过 `w6.r()` 解析为真实磁盘路径。

### 3.4 具体示例

```
username = "wxid_abc123"
md5("wxid_abc123") = "a1b2c3d4e5f6..."
dataDir = /data/data/com.tencent.mm/MicroMsg/xxxxxxxxxxxx/

小头像:
  WCF:   wcf://avatar/a1/b2/user_a1b2c3d4e5f6....png
  磁盘:  .../avatar/a1/b2/user_a1b2c3d4e5f6....png

大头像:
  WCF:   wcf://avatar/a1/b2/user_hd_a1b2c3d4e5f6....png
  磁盘:  .../avatar/a1/b2/user_hd_a1b2c3d4e5f6....png

加速缓存:
  磁盘:  .../avatar/a1/b2/user_a1b2c3d4e5f6....png.bm
```

---

## 四、联系人头像加载全链路

### 4.1 入口 `g.a()` / `g.b()`

```java
// 简化的单参数入口
public static Bitmap a(String username) {
    return b(username, false, -1, null);
}

// 完整入口
public static Bitmap b(String username, boolean forceDownload, int corner, e callback) {
    if (isEmpty(username) || !serviceReady()) return null;

    // 漂流瓶特殊处理：@bottle → 真实 username
    if (y3.F3(username)) {
        username = y3.Y4(username);
    }

    // 特殊用户 → 默认头像
    if (e2.L(username)) {
        username = "weixin";
    }

    // 委托给 AvatarService
    return d1.hj().f(username, forceDownload, corner, callback);
}
```

### 4.2 核心：`r.f()` — 三级缓存策略

```java
public Bitmap f(String username, boolean force, int corner, e callback) {
    z storage = e();

    // ── 三级缓存策略 ──

    // 第1级：内存缓存（LRU）
    Bitmap bitmap = storage.i(username);  // z.i()
    if (bitmap != null) {
        c(username);  // 记录访问时间
        return bitmap;
    }

    // 第2级：如果 force=true，不触发网络
    if (force) {
        c(username);
        return null;
    }

    // 第3级：网络下载（去重 + 异步）
    if (!pendingSet.contains(username)) {
        pendingSet.add(username);
        h(new n(this, username, callback));  // 发起 NetScene
    }
    return null;
}
```

### 4.3 内存缓存 `z.i()` — 两级 key

```java
public Bitmap i(String username) {
    // 第1级：标准 key
    Bitmap bitmap = memoryCache.get(username);

    // 第2级：派生 key（b0 配置决定）
    if (bitmap == null && !username.startsWith("key")) {
        String derivedKey = b0.tj(username, 0.1f);
        bitmap = memoryCache.get(derivedKey);
    }

    if (bitmap != null && bitmap.isRecycled()) {
        memoryCache.remove(username);
        return null;
    }
    return bitmap;
}
```

### 4.4 磁盘写入 `z.r()` / `z.s()`

```java
// z.r() — 完整裁剪+写入（height=156, width=156, flag=1）
// 内部做居中裁剪（方形 → 居中截取 → 缩放到 156×156）
public boolean r(String username, Bitmap bitmap, int height, int width, int flag) {
    // 1. 非正方形 → 居中裁剪为正方形
    // 2. createScaledBitmap → 缩放到目标尺寸
    // 3. 写入 .bm 加速文件
    // 4. 调用 p() 写入内存缓存
    return true;
}

// z.s() — 从原始字节写入
public boolean s(String username, byte[] data) {
    Bitmap bitmap = o(username, data);  // 解码
    if (valid(bitmap)) {
        p(username, bitmap);  // 写内存
        return true;
    }
    return false;
}
```

### 4.5 `.bm` 文件格式（SmallBM: `x`）

微信自定义磁盘缓存格式：

```
[header: flag + size] [compressed Bitmap data]
```

- 用于快速磁盘读取，比 PNG 解码快
- `x.a(path, key)` 读取
- `x.b(path)` 检查是否存在

---

## 五、群聊头像 — 与联系人的差异

### 5.1 存储层面：完全相同

群聊 ID（`123456789@chatroom`）直接作为 `username` 走同一套链路：

```
联系人:  g.b("wxid_abc123")        → MD5 → a1/b2/user_a1b2...png
群聊:    g.b("123456789@chatroom") → MD5 → c3/d4/user_c3d4...png
公众号:  g.b("gh_xxx")             → MD5 → e5/f6/user_e5f6...png
```

**全部统一，无差别处理！**

### 5.2 生成方式：服务端合成

```
客户端请求群聊头像
  → 服务端取群成员前 9 人
  → 服务端合成九宫格图片
  → 返回 Bitmap
  → 客户端存入磁盘（与联系人同路径）
```

**群聊头像不是客户端本地拼接的！** 不需要 Hook Canvas 或手动合成。

### 5.3 区分联系人和群聊

```java
boolean isGroupChat(String username) {
    return username != null && username.endsWith("@chatroom");
}
```

### 5.4 更新触发 `g.h()`

```java
public static void h(String username, r0 data, String extra) {
    if (isEmpty(username) || username.equals(ownUsername)) return;

    if (isEmpty(data.bigUrl())) {
        // URL 为空 → 删除磁盘缓存
        storage.m(username, false);  // 删小头像
        storage.m(username, true);   // 删大头像
    } else {
        data.modified = 1;
    }

    dao.F0(data);                // 更新 DB
    service.j(username, data);   // 触发重新下载
}
```

---

## 六、数据实体 `r0` 字段详解

| 字段 | 类型 | 含义 |
|---|---|---|
| `a` | String | **username**（用户/群聊 ID） |
| `b` | int | **imgFlag**（图片标记，控制过期） |
| `c` | int | 状态标记 |
| `d` | String | **小头像 URL** (smallUrl) |
| `e` | String | **大头像 URL** (bigUrl / HD URL) |
| `f` | int | 标记位 |
| `g` | int | 标记位 |
| `h` | int | **修改标记**（1=已修改需刷新） |
| `i` | int | 标记位 |

**获取方式**：

```java
Class<?> r0Class = XposedHelpers.findClass("com.tencent.mm.modelavatar.r0", classLoader);

// 从 DB 查询
Object dao = XposedHelpers.callMethod(d1, "mj");       // d1.mj() → s0
Object avatarData = XposedHelpers.callMethod(dao, "x0", username); // s0.x0(username)

String bigUrl   = (String) XposedHelpers.callMethod(avatarData, "c"); // r0.c() → 大图URL
String smallUrl = (String) XposedHelpers.callMethod(avatarData, "d"); // r0.d() → 小图URL
String user     = (String) XposedHelpers.callMethod(avatarData, "e"); // r0.e() → username
```

---

## 七、提取实战：四种 Hook 方案

### 方案 A：拦截磁盘写入 `z.r()`（推荐 ✅）

**优点**：不漏任何头像，时机在写入完成后，Bitmap 已裁剪好（156×156）  
**锚点**：`"MicroMsg.AvatarStorage"`

```java
// 用锚点定位 AvatarStorage 类
Class<?> zClass = finder.find("MicroMsg.AvatarStorage", "com.tencent.mm.modelavatar");

// Hook z.r(String username, Bitmap bitmap, int height, int width, int flag)
XposedHelpers.findAndHookMethod(zClass, "r",
    String.class, Bitmap.class, int.class, int.class, int.class,
    new XC_MethodHook() {
        @Override
        protected void afterHookedMethod(MethodHookParam param) {
            String username = (String) param.args[0];
            Bitmap avatar   = (Bitmap) param.args[1];
            int height      = (int) param.args[2];
            int width       = (int) param.args[3];
            int flag        = (int) param.args[4];

            if (avatar != null && !avatar.isRecycled()) {
                boolean isGroup = username.endsWith("@chatroom");
                String type = isGroup ? "群聊" : "联系人";

                // 保存到你的目录
                String filename = type + "_" + username.replace(":", "_").replace("/", "_")
                                  + "_" + width + "x" + height + ".png";
                saveBitmap(avatar, "/sdcard/wechat_avatars/" + filename);

                XposedBridge.log("[Avatar] " + type + "头像: " + username
                    + " (" + width + "×" + height + ")");
            }
        }
    });
```

### 方案 B：直接读磁盘文件（离线批量提取）

```java
import java.io.File;
import java.security.MessageDigest;

/**
 * 从微信磁盘缓存直接读取头像文件
 * @param username 联系人 wxid 或群聊 id
 * @param dataDir  微信数据目录，如 /data/data/com.tencent.mm/MicroMsg/abc123/
 * @param hd       true=大头像, false=小头像
 */
public Bitmap loadAvatarFromDisk(String username, String dataDir, boolean hd) {
    try {
        String md5 = md5(username.getBytes("UTF-8"));
        String path = dataDir + "avatar/"
            + md5.substring(0, 2) + "/"
            + md5.substring(2, 4) + "/"
            + "user_" + (hd ? "hd_" : "") + md5 + ".png";

        File file = new File(path);
        if (!file.exists()) {
            // 尝试 .bm 加速文件（需要特殊解析器，不通用）
            return null;
        }

        return BitmapFactory.decodeFile(path);
    } catch (Exception e) {
        return null;
    }
}

/** MD5 工具方法 */
public static String md5(byte[] data) {
    try {
        MessageDigest md = MessageDigest.getInstance("MD5");
        byte[] digest = md.digest(data);
        StringBuilder sb = new StringBuilder(32);
        for (byte b : digest) {
            sb.append(String.format("%02x", b & 0xff));
        }
        return sb.toString();
    } catch (Exception e) {
        return "";
    }
}

// ── 使用示例 ──
String dataDir = "/data/data/com.tencent.mm/MicroMsg/xxxxxxxxxxxx/";

// 加载联系人小头像
Bitmap contactAvatar = loadAvatarFromDisk("wxid_abc123", dataDir, false);

// 加载群聊大头像
Bitmap groupAvatar = loadAvatarFromDisk("123456789@chatroom", dataDir, true);
```

### 方案 C：Hook 加载入口 `g.b()`（实时监控）

```java
// 适用场景：不需要保存头像，只需要监控"谁在加载头像"
// 锚点："MicroMsg.AvatarLogic"

Class<?> gClass = finder.find("MicroMsg.AvatarLogic", "com.tencent.mm.modelavatar");

// g.b(String username, boolean force, int corner, e callback) → Bitmap
// 注意：第4个参数 e 是内部回调接口，建议用 findMethod 定位后用 MethodHook
```

### 方案 D：从 DB 获取 URL 后自行下载（获取高清原图）

```java
/**
 * 从微信 DB 读取头像 URL，自行下载原始高清图。
 * 注意：需要微信登录态 Cookie，否则 CDN 可能拒绝访问。
 */
public void downloadAvatarFromUrl(ClassLoader classLoader, String username) {
    // 1. 获取服务定位器
    Class<?> d1Class = XposedHelpers.findClass(
        "com.tencent.mm.modelavatar.d1", classLoader);

    // 2. d1.ij() → AvatarStorage, d1.mj() → DAO
    // 3. DAO.x0(username) → r0（头像数据实体）
    // 4. r0.e() → bigUrl, r0.d() → smallUrl

    String bigUrl = getBigUrlFromDB(classLoader, username);
    if (bigUrl != null && !bigUrl.isEmpty()) {
        new Thread(() -> {
            Bitmap bitmap = downloadImage(bigUrl);
            if (bitmap != null) {
                saveBitmap(bitmap, "/sdcard/wechat_avatars/" + username + "_hd.png");
            }
        }).start();
    }
}
```

---

## 八、方案对比速查

| 方案 | 实时性 | 覆盖度 | 性能开销 | 图片质量 | 推荐场景 |
|---|---|---|---|---|---|
| **A: Hook z.r()** | ⭐⭐⭐⭐⭐ | 所有写入 | 零 | 156×156 裁剪 | **实时监控+保存（首选）** |
| **B: 直接读磁盘** | ⭐⭐ | 已缓存的 | 无 Hook | 按需选择 HD | 离线批量提取 |
| **C: Hook g.b()** | ⭐⭐⭐⭐⭐ | 所有加载 | 低 | 原始 Bitmap | 监控加载行为 |
| **D: DB + 自行下载** | ⭐⭐⭐ | 全部（需网络） | 网络 | 原始高清图 | 需要未裁剪原图 |

---

## 九、完整调用链总结

```
ImageView.setImageDrawable(AvatarDrawable) / AnyProcessAvatarAttacher
  │
  └─ AvatarLogic.a/b/c(username, ...)
       │
       ├─ 漂流瓶 @bottle → y3.F3() / y3.Y4()
       ├─ 特殊用户 → "weixin" 默认头像
       │
       └─ AvatarService.f(username, force, corner, callback)
             │
             ├─ 内存缓存命中 → 直接返回 Bitmap ✅
             ├─ force=true → 返回 null
             └─ 未命中 → 发起 NetScene 网络下载
                   │
                   ├─ 下载成功
                   │   ├─ z.r() 裁剪 156×156 + 写磁盘 (.png + .bm)
                   │   ├─ z.p() 写内存缓存
                   │   └─ 回调通知 UI 刷新 ✅
                   │
                   └─ 下载失败 → 默认头像

磁盘路径（联系人 & 群聊 & 公众号 统一）:
  {dataDir}/avatar/{MD5[0:2]}/{MD5[2:4]}/user_{hd_}{MD5}.png

适用类型:
  个人联系人:  username = "wxid_xxxx"
  群聊:        username = "xxxx@chatroom"
  公众号:      username = "gh_xxxx"
  企业微信:    username = "xxxx@openim"
  漂流瓶:      username = "xxxx@bottle"
  全部统一，无差别！
```

---

## 十、微信锚点字符串速查

| 锚点字符串 | 目标类 | 用途 |
|---|---|---|
| `MicroMsg.AvatarLogic` | `com.tencent.mm.modelavatar.g` | 头像逻辑入口 |
| `MicroMsg.AvatarService` | `com.tencent.mm.modelavatar.r` | 头像服务 |
| `MicroMsg.AvatarStorage` | `com.tencent.mm.modelavatar.z` | 头像存储（磁盘+内存） |
| `AvatarService#checkAvatarExpire` | `com.tencent.mm.modelavatar.h` | 过期检查 |
| `MicroMsg.AvatarDrawable` | `com.tencent.mm.feature.avatar.w` | UI Drawable |
| `MicroMsg.AnyProcessAvatarAttacher` | `com.tencent.mm.feature.avatar.s` | 跨进程绑定 |

---

> 本文档基于 LSPilot 逆向分析工具生成，微信版本：8.0.x。
> 不同版本混淆类名可能不同，锚点字符串通常稳定。
> 磁盘路径公式（MD5 分片目录）跨版本不变。
