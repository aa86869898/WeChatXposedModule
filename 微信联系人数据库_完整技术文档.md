# 微信 rcontact 联系人数据库 — 完整分类提取与过滤技术文档

> **版本**: 微信 8.0.76 | **数据库**: EnMicroMsg.db | **表**: rcontact (13773行)  
> **用途**: Xposed / LSPosed 独立模块中做联系人分类管理

---

## 目录

- [第一章 数据库打开方法](#第一章-数据库打开方法)
- [第二章 rcontact 表完整字段参考](#第二章-rcontact-表完整字段参考)
- [第三章 七大分类详解](#第三章-七大分类详解)
  - [3.1 好友](#31-好友)
  - [3.2 群聊](#32-群聊)
  - [3.3 公众号](#33-公众号)
  - [3.4 系统号](#34-系统号)
  - [3.5 文件传输助手](#35-文件传输助手)
  - [3.6 陌生人](#36-陌生人)
  - [3.7 企业微信联系人](#37-企业微信联系人)
- [第四章 分类汇总 SQL](#第四章-分类汇总-sql)
- [第五章 分类管理实现建议](#第五章-分类管理实现建议)
- [第六章 verifyFlag 含义速查](#第六章-verifyflag-含义速查)
- [第七章 deleteFlag 说明](#第七章-deleteflag-说明)
- [第八章 contactLabelIds 标签说明](#第八章-contactlabelids-标签说明)
- [第九章 跨版本兼容策略](#第九章-跨版本兼容策略)
- [第十章 完整分类分布表](#第十章-完整分类分布表)
- [第十一章 昵称、备注、微信号、头像获取方法](#第十一章-昵称备注微信号头像获取方法)

---

## 第一章 数据库打开方法

### 1.1 核心调用

不引入任何第三方 WCDB/SQLCipher 库。反射调用微信进程中已加载的 `ka5.f.s()`，一行打开加密数据库。

```java
Class<?> ka5f = cl.loadClass("ka5.f");
Method s = ka5f.getDeclaredMethod("s",
        String.class, String.class, int.class, boolean.class);
Object db = s.invoke(null, dbPath, password, 0, true);
```

### 1.2 路径计算

```
数据目录 = mp0.b.X()
         → "/data/user/0/com.tencent.mm/"

uin哈希  = hm0.b0.e((int) uin)
         → MD5("mm" + uin)
         → 如 "7b998556beeef3a1552aa84157df99e8"

完整路径 = 数据目录 + "MicroMsg/" + uin哈希 + "/EnMicroMsg.db"
```

### 1.3 密码计算

```
IMEI = wo.w0.g(true)
       取不到时降级为 "1234567890ABCDEF"

密码 = MD5(IMEI + uin).substring(0, 7)
     = kk.k.g((IMEI + uin).getBytes()).substring(0, 7)
```

### 1.4 查询与关闭

```java
Method u = db.getClass().getDeclaredMethod("u", String.class, String[].class);
Cursor c = (Cursor) u.invoke(db, sqlString, null);

Method close = db.getClass().getDeclaredMethod("c");
close.invoke(db);
```

---

## 第二章 rcontact 表完整字段参考

建表语句 (来自 sqlite_master):

```sql
CREATE TABLE rcontact (
  username           TEXT     default ''   PRIMARY KEY,  -- 唯一标识 ★
  alias              TEXT     default '',                -- 别名/微信号 ★
  conRemark          TEXT     default '',                -- 备注名 ★
  domainList         TEXT     default '',                -- 域名列表
  nickname           TEXT     default '',                -- 昵称 ★
  pyInitial          TEXT     default '',                -- 拼音首字母
  quanPin            TEXT     default '',                -- 全拼
  showHead           INTEGER  default '0',               -- 头像显示标志
  type               INTEGER  default '0',               -- 类型 ★★★
  uiType             LONG     default '0',               -- UI类型
  weiboFlag          INTEGER  default '0',               -- 微博标志
  weiboNickname      TEXT     default '',                -- 微博昵称
  conRemarkPYFull    TEXT     default '',                -- 备注全拼 ★
  conRemarkPYShort   TEXT     default '',                -- 备注拼音首字母 ★
  lvbuff             BLOB,                               -- protobuf扩展数据
  verifyFlag         INTEGER  default '0',               -- 验证标志(公众号=24) ★
  encryptUsername    TEXT     default '',                -- 加密用户名
  chatroomFlag       INTEGER,                            -- 群聊标志
  deleteFlag         INTEGER  default '0',               -- 删除标志(1=已删) ★
  contactLabelIds    TEXT     default '',                -- 标签ID(逗号分隔)
  descWordingId      TEXT     default '',                -- 描述文案ID
  openImAppid        TEXT,                               -- 企业微信AppID
  sourceExtInfo      TEXT,                               -- 来源扩展信息
  ticket             TEXT     default '',                -- 票据
  usernameFlag       LONG     default '0',               -- 用户名标志
  contactExtra       BLOB,                               -- 联系人扩展数据
  createTime         LONG     default '0'                -- 创建时间戳 ★
);
```

> ★ = 分类管理中最常用的字段

---

## 第三章 七大分类详解

### 3.1 好友

| 属性 | 值 |
|---|---|
| 数量 | 13950 行 |
| 构成 | wxid_ 开头 13035 + 自定义微信号 911 + @openim 4 |

**识别方法:**
- 主要: `type = 4`
- 辅助: `username LIKE 'wxid_%'` 或自定义微信号
- 排除: `username NOT LIKE '%@chatroom'` / `username NOT LIKE 'gh_%'` / `type NOT IN (33, 2049)`

**过滤 SQL:**
```sql
-- 方法A: 直接用 type=4（最简，8.0.76 100%准确）
SELECT * FROM rcontact WHERE type = 4 AND deleteFlag = 0

-- 方法B: 排除企业微信联系人
SELECT * FROM rcontact WHERE type = 4 AND deleteFlag = 0
  AND username NOT LIKE '%@openim'

-- 方法C: 跨版本兜底
SELECT * FROM rcontact WHERE deleteFlag = 0
  AND username NOT LIKE '%@chatroom'
  AND username NOT LIKE 'gh_%'
  AND type NOT IN (33, 2049)
```

**提取关键字段:** username, nickname, alias, conRemark, conRemarkPYFull, conRemarkPYShort, pyInitial, quanPin, contactLabelIds, createTime

**显示名优先级:**
```
displayName = conRemark (备注名)
else         alias     (微信号)
else         nickname  (昵称)
else         username  (wxid)
```

**排序:**
```sql
ORDER BY CASE WHEN length(conRemarkPYFull) > 0
         THEN upper(conRemarkPYFull)
         ELSE upper(quanPin) END ASC
```

### 3.2 群聊

| 属性 | 值 |
|---|---|
| 数量 | 68 行 |
| 构成 | type=3 (37) + type=2 (28) + type=2050 (2) + type=2051 (1) |
| 样例 | `22110637730@chatroom \| 化州市上市公司车大炮群` |

**识别方法:**
- 主要: `username LIKE '%@chatroom'`
- 辅助: `type IN (2, 3, 2050, 2051)`

**过滤 SQL:**
```sql
SELECT * FROM rcontact WHERE username LIKE '%@chatroom' AND deleteFlag = 0
```

**提取关键字段:** username (群聊ID), nickname (群名称), conRemark (备注), type

> ⚠️ 群成员不存储在 rcontact 表中！需要从其他表或网络协议获取。

### 3.3 公众号

| 属性 | 值 |
|---|---|
| 数量 | 12 行 |
| 特征 | type=3, verifyFlag=24, username 以 gh_ 开头 |
| 样例 | `gh_3dfda90e39d6 \| 微信支付` |

**识别方法:**
- 主要: `username LIKE 'gh_%'`
- 辅助: `type = 3 AND verifyFlag = 24`

**过滤 SQL:**
```sql
-- 方法A: username 模式
SELECT * FROM rcontact WHERE username LIKE 'gh_%' AND deleteFlag = 0

-- 方法B: type + verifyFlag
SELECT * FROM rcontact WHERE type = 3 AND verifyFlag = 24 AND deleteFlag = 0
```

### 3.4 系统号

| 属性 | 值 |
|---|---|
| 数量 | 24 行 |
| 特征 | type=33, username 是固定英文名 |

**典型列表:** qqmail, floatbottle, shakeapp, lbsapp, medianote, newsapp, facebookapp, qqfriend, officialaccounts, service_officialaccounts, masssendapp 等

**过滤 SQL:**
```sql
SELECT * FROM rcontact WHERE type = 33 AND deleteFlag = 0
```

### 3.5 文件传输助手

| 属性 | 值 |
|---|---|
| 数量 | 1 行 |
| 特征 | type=2049, username='filehelper' |

**过滤 SQL:**
```sql
SELECT * FROM rcontact WHERE username = 'filehelper' AND deleteFlag = 0
```

### 3.6 陌生人

| 属性 | 值 |
|---|---|
| 数量 | 1 行 |
| 特征 | type=1, username 为 wxid_ 开头 |

**过滤 SQL:**
```sql
SELECT * FROM rcontact WHERE type = 1 AND deleteFlag = 0
```

### 3.7 企业微信联系人

| 属性 | 值 |
|---|---|
| 数量 | 4 行 (混在 type=4 中) |
| 特征 | type=4, username 以 @openim 结尾 |

**过滤 SQL:**
```sql
SELECT * FROM rcontact WHERE username LIKE '%@openim' AND deleteFlag = 0
```

---

## 第四章 分类汇总 SQL

一次性提取全部联系人并自动标注分类:

```sql
SELECT
  username, nickname, alias, conRemark, type, verifyFlag, deleteFlag,
  CASE
    WHEN username = 'filehelper'                          THEN '文件传输助手'
    WHEN username LIKE '%@chatroom'                       THEN '群聊'
    WHEN username LIKE 'gh_%'                             THEN '公众号'
    WHEN type = 33                                        THEN '系统号'
    WHEN type = 1                                         THEN '陌生人'
    WHEN username LIKE '%@openim'                         THEN '企业微信'
    WHEN type = 4                                         THEN '好友'
    ELSE '其他'
  END AS category
FROM rcontact
WHERE deleteFlag = 0
ORDER BY category, nickname
```

### 按分类统计数量

```sql
SELECT category, count(*) AS cnt FROM (
  SELECT CASE
    WHEN username LIKE '%@chatroom' THEN '群聊'
    WHEN username LIKE 'gh_%'       THEN '公众号'
    WHEN type = 33                  THEN '系统号'
    WHEN type = 2049                THEN '文件传输助手'
    WHEN type = 1                   THEN '陌生人'
    WHEN username LIKE '%@openim'   THEN '企业微信'
    WHEN type = 4                   THEN '好友'
    ELSE '其他'
  END AS category
  FROM rcontact WHERE deleteFlag = 0
) GROUP BY category ORDER BY cnt DESC
```

---

## 第五章 分类管理实现建议

### 5.1 枚举定义

```java
public enum ContactCategory {
    FRIEND(4, "好友"),
    GROUP(2, "群聊"),
    OFFICIAL(3, "公众号"),
    SYSTEM(33, "系统号"),
    FILE_HELPER(2049, "文件传输助手"),
    STRANGER(1, "陌生人"),
    OPENIM(4, "企业微信");

    public final int typicalType;
    public final String label;

    ContactCategory(int t, String l) { typicalType = t; label = l; }
}
```

### 5.2 分类判断函数

```java
public static ContactCategory classify(String username, int type, int verifyFlag) {
    if ("filehelper".equals(username))
        return ContactCategory.FILE_HELPER;
    if (username != null && username.endsWith("@chatroom"))
        return ContactCategory.GROUP;
    if (username != null && username.startsWith("gh_"))
        return ContactCategory.OFFICIAL;
    if (type == 33)
        return ContactCategory.SYSTEM;
    if (type == 1)
        return ContactCategory.STRANGER;
    if (username != null && username.endsWith("@openim"))
        return ContactCategory.OPENIM;
    if (type == 4)
        return ContactCategory.FRIEND;
    return ContactCategory.FRIEND; // fallback
}
```

### 5.3 联系人数据模型

```java
public class ContactBean {
    public String  username;        // 唯一ID
    public String  nickname;        // 昵称
    public String  alias;           // 微信号
    public String  conRemark;       // 备注名
    public String  pyInitial;       // 拼音首字母
    public String  quanPin;         // 全拼
    public String  conRemarkPYFull; // 备注全拼
    public String  contactLabelIds; // 标签列表
    public long    createTime;      // 创建时间
    public int     type;            // 原始type值
    public int     verifyFlag;      // 验证标志
    public int     deleteFlag;      // 删除标志
    public ContactCategory category; // 分类

    public String displayName() {
        if (conRemark != null && !conRemark.isEmpty()) return conRemark;
        if (alias != null && !alias.isEmpty()) return alias;
        if (nickname != null && !nickname.isEmpty()) return nickname;
        return username;
    }

    public boolean isFriend()   { return category == ContactCategory.FRIEND; }
    public boolean isGroup()    { return category == ContactCategory.GROUP; }
    public boolean isOfficial() { return category == ContactCategory.OFFICIAL; }
}
```

---

## 第六章 verifyFlag 含义速查

| verifyFlag | 含义 | 出现位置 |
|---|---|---|
| 0 | 普通用户/好友 | 好友/群聊/系统号 |
| 8 | 可能是企业认证 | 未在此版本出现 |
| **24** | **公众号** | **仅 gh_ 开头** |
| 56 | 可能是已认证企业 | 仅 1 行 type=3 |

> ★ `verifyFlag=24` 是公众号的精准标志

---

## 第七章 deleteFlag 说明

| deleteFlag | 含义 |
|---|---|
| 0 | 正常联系人 |
| 1 | 已删除的联系人 |

> 所有查询建议加 `AND deleteFlag = 0`

---

## 第八章 contactLabelIds 标签说明

格式: 逗号分隔的数字字符串，如 `"1,5,12"`，每个数字对应一个标签ID。

标签ID到标签名的映射表: `ContactLabel` (由 `com.tencent.mm.storage.g4` 管理)

**查指定标签的联系人:**
```sql
SELECT * FROM rcontact
WHERE type = 4 AND deleteFlag = 0
  AND (contactLabelIds = '5'
       OR contactLabelIds LIKE '5,%'
       OR contactLabelIds LIKE '%,5'
       OR contactLabelIds LIKE '%,5,%')
```

**标签相关方法 (需反射):**
| 方法 | 作用 |
|---|---|
| `g4.F1(labelName)` → labelId | 标签名 → 标签ID |
| `g4.Q1(labelId)` → labelName | 标签ID → 标签名 |

---

## 第九章 跨版本兼容策略

微信版本升级时，混淆类名会变化，type 编码也可能调整。

### 策略A: username 模式匹配 (最稳)

不依赖 type 值:

```
群聊:    username LIKE '%@chatroom'
公众号:  username LIKE 'gh_%'
系统号:  username IN ('filehelper','qqmail',...)
好友:    上述都排除后的剩余
```

### 策略B: DexKit 动态定位

用 DexKit 搜索使用了 `"EnMicroMsg.db"` 或 `"rcontact"` 字符串的类 → 自动找到核心类。

### 策略C: 新版本先诊断

```sql
-- 查 type 分布
SELECT type, count(*) FROM rcontact GROUP BY type ORDER BY count(*) DESC

-- 抽样确认含义
SELECT username, nickname, type FROM rcontact WHERE type=XXX LIMIT 5
```

---

## 第十章 完整分类分布表

| username格式 | type | verifyFlag | 数量 | 分类 |
|---|---|---|---|---|
| wxid_xxx | 4 | 0 | 13035 | 好友 |
| 自定义微信号 | 4 | 0 | 911 | 好友 |
| @openim | 4 | 0 | 4 | 企业微信 |
| @chatroom | 3 | 0 | 37 | 群聊 |
| @chatroom | 2 | 0 | 28 | 群聊 |
| @chatroom | 2050 | 0 | 2 | 群聊 |
| @chatroom | 2051 | 0 | 1 | 群聊 |
| gh_xxxx | 3 | 24 | 12 | 公众号 |
| 固定英文名 | 33 | 0 | 24 | 系统号 |
| filehelper | 2049 | 0 | 1 | 文件传输助手 |
| wxid_xxx | 1 | 0 | 1 | 陌生人 |
| wxid_xxx | 259 | 0 | 1 | 未分类 |

> 合计 **14107** 行 (另有约667行 deleteFlag=1 或其他)

---

## 第十一章 昵称、备注、微信号、头像获取方法

### 11.1 昵称 — 对方设置的微信名

| 属性 | 值 |
|---|---|
| 数据来源 | `rcontact.nickname` |
| 含义 | 对方自己设置的微信昵称 |
| 能否修改 | ❌ 不能（由对方控制） |

```sql
SELECT nickname FROM rcontact WHERE username = 'wxid_xxx'
```

```java
String nickname = cursor.getString(cursor.getColumnIndex("nickname"));
```

### 11.2 备注名 — 你给对方备注的名字

| 属性 | 值 |
|---|---|
| 数据来源 | `rcontact.conRemark` |
| 含义 | 你给这个联系人设置的备注 |
| 能否修改 | ✅ 能（由你控制） |

```sql
SELECT conRemark FROM rcontact WHERE username = 'wxid_xxx'
```

```java
String conRemark = cursor.getString(cursor.getColumnIndex("conRemark"));
```

**displayName 优先级 (联系人列表展示):**

```
conRemark (备注) > alias (微信号) > nickname (昵称) > username (wxid)
```

```java
public static String getDisplayName(Cursor c) {
    String remark = c.getString(c.getColumnIndex("conRemark"));
    if (remark != null && !remark.isEmpty()) return remark;
    String alias = c.getString(c.getColumnIndex("alias"));
    if (alias != null && !alias.isEmpty()) return alias;
    String nick = c.getString(c.getColumnIndex("nickname"));
    if (nick != null && !nick.isEmpty()) return nick;
    return c.getString(c.getColumnIndex("username"));
}
```

### 11.3 拼音 — 排序和搜索用

| 数据来源 | 字段 | 含义 |
|---|---|---|
| rcontact | `pyInitial` | 昵称拼音首字母 |
| rcontact | `quanPin` | 昵称全拼 |
| rcontact | `conRemarkPYFull` | 备注全拼 |
| rcontact | `conRemarkPYShort` | 备注拼音首字母 |

**排序 SQL (备注拼音优先):**
```sql
ORDER BY CASE WHEN length(conRemarkPYFull) > 0
         THEN upper(conRemarkPYFull)
         ELSE upper(quanPin) END ASC
```

### 11.4 微信号 — 对方设置的微信号

| 属性 | 值 |
|---|---|
| 数据来源 | `rcontact.alias` |
| 含义 | 对方设置的微信号（可用于搜索添加好友） |
| 格式 | 字母+数字+下划线，如 `zhangsan888` |
| 注意 | 不是所有人都设置了微信号，可能为空 |

```sql
SELECT alias FROM rcontact WHERE username = 'wxid_xxx'
```

```java
String alias = cursor.getString(cursor.getColumnIndex("alias"));
```

### 11.5 头像 ★★★

> ⚠️ 头像不存储在 rcontact 表中！以 PNG 文件存储在本机文件系统中。

#### 11.5.1 头像文件路径公式

```
基础目录 = j1.u().h()
         = hm0.b0.h()
         = mp0.b.X() + "MicroMsg/" + MD5("mm" + uin) + "/"

MD5值    = kk.k.g(username.getBytes())
         = MD5(username)

普通头像  = 基础目录 + "avatar/" + MD5[0:2] + "/" + MD5[2:4] + "/user_" + MD5 + ".png"

高清头像  = 基础目录 + "avatar/" + MD5[0:2] + "/" + MD5[2:4] + "/user_hd_" + MD5 + ".png"
```

#### 11.5.2 完整路径示例

对于 `username = "wxid_6b775u983op922"`:

```
MD5("wxid_6b775u983op922") = "7f3a8b2c..."

→ avatar/7f/3a/user_7f3a8b2c....png

→ 完整路径:
  /data/user/0/com.tencent.mm/MicroMsg/
  7b998556beeef3a1552aa84157df99e8/
  avatar/7f/3a/user_7f3a8b2c....png
```

#### 11.5.3 Java 代码

**方法A: 自己拼接路径 (推荐)**

```java
public static String getAvatarPath(String accountDir, String username) {
    String md5 = MD5(username.getBytes());
    return accountDir + "avatar/"
        + md5.substring(0, 2) + "/"
        + md5.substring(2, 4) + "/"
        + "user_" + md5 + ".png";
}

public static String getHdAvatarPath(String accountDir, String username) {
    String md5 = MD5(username.getBytes());
    return accountDir + "avatar/"
        + md5.substring(0, 2) + "/"
        + md5.substring(2, 4) + "/"
        + "user_hd_" + md5 + ".png";
}

// 使用
String accountDir = j1.u().h(); // hm0.b0.h()
String avatarPath = getAvatarPath(accountDir, "wxid_6b775u983op922");
```

**方法B: 反射调用微信 AvatarService**

```java
// com.tencent.mm.modelavatar.z.f(username, isHd, unused) → 文件路径
Class<?> avatarService = cl.loadClass("com.tencent.mm.modelavatar.z");
Object avatarSvc = /* 获取AvatarService实例 */;

String path = (String) avatarService
    .getMethod("f", String.class, boolean.class, boolean.class)
    .invoke(avatarSvc, username, false, false); // 普通头像

String hdPath = (String) avatarService
    .getMethod("f", String.class, boolean.class, boolean.class)
    .invoke(avatarSvc, username, true, false);  // 高清头像
```

#### 11.5.4 加载为 Bitmap

```java
// 方法A: 从文件直接加载
File f = new File(avatarPath);
if (f.exists()) {
    Bitmap bm = BitmapFactory.decodeFile(avatarPath);
}

// 方法B: 用 Glide 加载
Glide.with(context)
    .load(new File(avatarPath))
    .placeholder(R.drawable.default_avatar)
    .into(imageView);

// 方法C: 用微信 AvatarService 直接获取 Bitmap
// com.tencent.mm.modelavatar.z.e(username, tag, isHd) → Bitmap
```

#### 11.5.5 缓存与默认头像

- 微信头像文件已在本地，无需网络请求
- 如果头像还没下载过，文件可能不存在 → 显示默认头像
- 默认头像: `com.tencent.mm.modelavatar.z.h(context)` → Bitmap

#### 11.5.6 showHead 字段

| showHead 值 | 含义 |
|---|---|
| 32 | 显示头像 |
| 其他 | 不显示头像（可能被屏蔽） |

### 11.6 创建时间

| 数据来源 | `rcontact.createTime` |
|---|---|
| 格式 | Unix 毫秒时间戳 |

```java
long ts = cursor.getLong(cursor.getColumnIndex("createTime"));
String date = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
    .format(new Date(ts));
```

### 11.7 完整联系人卡片提取

```sql
SELECT
  username,          -- 唯一ID
  nickname,          -- 昵称
  alias,             -- 微信号
  conRemark,         -- 备注名
  pyInitial,         -- 拼音首字母
  quanPin,           -- 全拼
  conRemarkPYFull,   -- 备注全拼
  type,              -- 类型
  showHead,          -- 头像标志(32=显示)
  contactLabelIds,   -- 标签
  createTime         -- 创建时间戳
FROM rcontact
WHERE type = 4 AND deleteFlag = 0
```

**完整 Java Bean:**

```java
public class ContactCard {
    public String  username;        // wxid
    public String  nickname;        // 昵称
    public String  alias;           // 微信号
    public String  conRemark;       // 备注名
    public String  pyInitial;       // 拼音首字母
    public String  quanPin;         // 全拼
    public String  conRemarkPYFull; // 备注全拼
    public int     type;            // 类型
    public int     showHead;        // 头像标志
    public String  contactLabelIds; // 标签
    public long    createTime;      // 创建时间
    public String  avatarPath;      // 头像文件路径 (需额外计算)

    public String displayName() {
        if (conRemark != null && !conRemark.isEmpty()) return conRemark;
        if (alias != null && !alias.isEmpty()) return alias;
        if (nickname != null && !nickname.isEmpty()) return nickname;
        return username;
    }
}
```

### 11.8 头像相关关键类速查

| 类 | 方法 | 作用 |
|---|---|---|
| `kk.k` | `g(byte[]) → String` | MD5 哈希 |
| `hm0.b0` | `h() → String` | 获取账号目录 |
| `hm0.b0` | `e(int) → String` | uin → MD5 |
| `com.tencent.mm.modelavatar.z` | `f(username, hd, unused) → String` | 获取头像文件路径 |
| `com.tencent.mm.modelavatar.z` | `e(username, tag, hd) → Bitmap` | 直接获取头像 Bitmap |
| `com.tencent.mm.modelavatar.z` | `h(context) → Bitmap` | 获取默认头像 |
| `android.graphics.BitmapFactory` | `decodeFile(path) → Bitmap` | 从文件解码 Bitmap |

---

> 全文完 | 基于微信 8.0.76 实测 | 2026-07-22
