# 微信「红包自动领取 + 金额 TTS 播报」逆向分析报告
> 适用范围：本次逆向基于当前 APK（luckymoney 插件包内类多数保留原名/半混淆，消息存储层已深度混淆）。
> 仅供个人学习/自动化研究使用，请遵守微信用户协议与当地法律法规。

---

## 0. 一句话结论（可直接用的 Hook 链）

```
消息入库 Hook:  com.tencent.mm.storage.f9.Bb/Db/Hb(com.tencent.mm.storage.e9, ...)
   ↓ 从 e9 取 field_content 并用 com.tencent.mm...dx0.r.v(content) 解析出 nativeUrl
   ↓ 识别 nativeUrl 前缀：
      wxpay://c2cbizmessagehandler/hongbao/receivehongbao        → 普通红包
      weixin://openNativeUrl/weixinHB/startreceivebizhbrequest  → 商家红包
   ↓ 构造领取 Scene:
      new com.tencent.mm.plugin.luckymoney.model.n6(1, channelId, sendId, nativeUrl, inWay, "v1.0", sessionUsername)
   ↓ 后台派发（无需 UI）:
      gp0.j1.j().e().h(scene, 0)      // NetSceneQueue.doScene
   ↓ 领取结果:
      hook n6.onGYNetEnd(0, msg, json)  → 金额 = json.optLong("sceneAmount")（单位:分）
      → TTS 播报 sceneAmount / 100.0 元
```

- 金额字段（双重保险）：
  - Scene 字段 `n6.r`（long）= `sceneAmount`（分）
  - 详情模型 `e1.q`（long）= receiveAmount（分），且 `e1.e`=hbStatus / `e1.A`=receiveStatus / `e1.d`=hbType
- 消息实体字段（保留原生名）：`field_type / field_content / field_talker / field_fromUsername / field_msgId / field_msgSvrId / field_createTime / field_isSend / field_status`
- 应用 Context：`com.tencent.mm.sdk.platformtools.a3.a`（静态 Application Context）

---

## 1. 全链路逆向明细

### 1.1 红包聊天消息的识别（官方前台点击链路，也是后台检测的模板）

- 聊天消息实体：`com.tencent.mm.storage.e9`（继承 `dx0.p3` → `im.c8`）
- `im.c8` 保存完整消息字段（Autogen DB 实体）：
  - `getType()I` → 返回 `field_type`
  - `j()Ljava/lang/String;` → 返回 `field_content`（红包 XML 内容）
  - `N0()Ljava/lang/String;` → 返回 `field_talker`（会话 username）
  - `getMsgId()J` → `field_msgId`；`getCreateTime()J` → `field_createTime`
  - 其余：`field_fromUsername / field_toUsername / field_msgSvrId / field_isSend (int) / field_status / field_lvbuffer / field_transContent / field_transBrandWording ...`（共 108 个字段，见附件查明字段列表）
- `e9.N1()`：对 `field_content` 做简单清理后返回（用于取 appmsg XML）

- appmsg XML 解析器：`dx0.r`（构造函数/静态方法 `dx0.r.v(String)`）
  - `v.r.s1` = nativeUrl（重要！`wxpay://.../receivehongbao` / `weixin://openNativeUrl/weixinHB/...`）
  - `v.r.j1` = 外链 url，`v.r.k` = title，`v.r.A1` = billNo，`v.r.r1` = 类型标记（"1003"/"1004"…）
  - `v.r.y(Class)`：取扩展数据（`dx0.d` 信封素材 / `k` 红包元数据 packetId、aboutUrl、source 等）

- 官方判定点：`com.tencent.mm.ui.chatting.viewitems.d4.f0(e9)`
  ```java
  // 从消息取内容并解析
  dx0.r v = dx0.r.v(e9.N1());
  // 普通红包
  if (v.s1.startsWith("wxpay://c2cbizmessagehandler/hongbao/receivehongbao")) { ... }
  // 商家红包
  if (v.s1.startsWith("weixin://openNativeUrl/weixinHB/startreceivebizhbrequest")) { ... }
  ```
  - 普通红包 → 启动 `LuckyMoneyNewReceiveUI`（intent 携带 key_native_url / key_username / key_from_username / key_msgid / key_packet_id…）
  - 商家红包 → 启动 `LuckyMoneyBusiReceiveUI`

### 1.2 普通红包领取：NetSceneReceiveLuckyMoney = `com.tencent.mm.plugin.luckymoney.model.n6` ★核心

- 继承链：`n6 → e6 → q5 → com.tencent.mm.wallet_core.model.d1 → com.tencent.mm.modelbase.m1`（+implements com.tencent.mm.network.l0）
  - `m1` = 所有网络 Scene 的基类（NetSceneBase）
- CGI（`H()Ljava/lang/String;`）：`/cgi-bin/mmpay-bin/receivewxhb`
- 构造函数（已用 smali 逐参核实）：
  ```java
  public n6(int type, int channelId, String sendId, String nativeUrl, int inWay, String ver, String sessionUsername)
  // 字段映射：type→h, channelId→i, sendId→m, nativeUrl→n, inWay→(JSON inWay), ver→(JSON ver), sessionUsername→(JSON group_name+sessionUsername)
  ```
- 请求 JSON（构造时放入 Map 并经 `q5.setRequestData(map)`）：
  `msgType / channelId / sendId / inWay / ver / agreeDuty / nativeUrl(URL 编码) / group_name / sessionUsername`
- 领取点击处的构建代码（`LuckyMoneyNewReceiveUI.onCreate` 约 1330 行）：
  ```java
  int O = Uri.parse(nativeUrl).getQueryParameter("channelid")!=null? Integer.parseInt(...) : 1;
  n6 scene = new n6(1, O, sendId, nativeUrl, getIntent().getIntExtra("key_way",0), "v1.0", sessionUsername);
  doSceneProgress(scene, false);   // 也可直接后台派发，见 1.4
  ```
- 结果回调 `onGYNetEnd(int i, String str, JSONObject jSONObject)`：
  ```java
  this.o = json.optString("sendNick");        // 发送者昵称
  this.q = json.optString("sendHeadImg");     // 头像
  this.r = json.optLong("sceneAmount");       // ★本次抢到的金额（分）
  this.s = json.optInt("hbStatus");           // 0 正常
  this.t = json.optInt("receiveStatus");      // 领取状态
  this.u = json.optString("statusMess");      // 状态文案
  this.w = json.optString("wishing");         // 红包寄语
  this.x = json.optInt("isSender");
  this.y = json.optInt("hbType");
  this.z = json.optString("sendUserName");
  // 等等；i==0 表示网络成功
  ```

### 1.3 其它红包类型（次要）

| 类型 | Scene | CGI | 构造 |
|---|---|---|---|
| 普通红包 | `n6` (NetSceneReceiveLuckyMoney) | /cgi-bin/mmpay-bin/receivewxhb | new n6(1, channelId, sendId, nativeUrl, inWay, "v1.0", username) |
| 商家红包 | `u5` (NetSceneLuckyMoneyBusiBase) | (父类 r5 → /cgi-bin/mmpay-bin/businesshongbao) | new u5(sendId, nativeUrl, way, packageExt) |
| 新年版/union 红包 | `i6` (NetSceneOpenLuckyMoneyUnion) | /cgi-bin/mmpay-bin/unionhb/openunionhb | new i6(更多参数含 nickName/headImg 等) |
| 查询详情 | `x5` (NetSceneLuckyMoneyDetail) | /cgi-bin/mmpay-bin/qrydetailwxhb | 用于已抢后展示详情 |

### 1.4 后台派发 Scene（无需 Activity）★可直接调用

`hy5.b0.e(scene,...)`（WalletNetSceneMgr）最终走：
```java
gp0.j1.j();                    // MMKernel 等价入口（静态单例工厂）
gp0.j1.j().e();                // → com.tencent.mm.modelbase.r1（NetSceneQueue）
q.h(scene, 0);                 // boolean h(m1 scene, int dispatchType) 派发
```
备选路径（等价）：`gp0.j1.j().q().b.h(scene, 0)` （`gp0.y.b` 字段即 r1）
- `com.tencent.mm.modelbase.r1` 其它方法：`g(m1)Z`、`d(m1)`（取消）、`k()`→网络层、`setOnSceneEnd(u0)`…
- 该队列线程安全，可在任意线程（含消息同步线程）调用。
- 场景基类还提供：`scene.setOnSceneEnd(u0 callback)` 注册结果回调（辅助拿结果）。

### 1.5 金额的另一来源（详情模型）

- `i5.d(JSONObject)` 把领取结果 JSON 解析为详情模型 `com.tencent.mm.plugin.luckymoney.model.e1`（LuckyMoneyDetail）
- `LuckyMoneyNewReceiveUI.m7(h6)` 中落库与日志：
  ```java
  e1 e1Var = h6Var.h;          // 场景结果中的详情
  ((ec) t1).field_receiveAmount = e1Var.q;   // 金额（分）
  ((ec) t1).field_receiveStatus = e1Var.A;   // ==2 说明可领取/已成功
  Log.i(..., "amount:" + e1Var.q + ", hbType:" + e1Var.d + ", hbStatus:" + e1Var.e + ", receiveStatus:" + e1Var.A);
  ```
- 结论：抢到金额 = `e1.q`（详情）或 `n6.r`（sceneAmount），均以「分」为单位，做 TTS 时 ÷100。

### 1.6 消息入库 Hook 点（纯后台检测核心）

- 存储类：`com.tencent.mm.storage.f9`（表名 `message`，日志 TAG `MicroMsg.MsgInfoStorage`，方法全部以 e9 为消息实体）
- 插入方法（新消息从同步/Sync 过来都会进这里）：
  - `Bb(com.tencent.mm.storage.e9, Z)J`  ★主插入
  - `Db(com.tencent.mm.storage.e9)J`
  - `Hb(com.tencent.mm.storage.e9)J`
  - `Ic(J, com.tencent.mm.storage.e9)I` / `Qc(J, com.tencent.mm.storage.e9, Z)I`（更新/批量）
- 事件（备选）：`com.tencent.mm.autogen.events.NotifyNewMsgEvent`（仅带新消息条数）、`MsgInsertWithNewContactEvent`（带 e9），均通过 IEvent 系统派发，可用 `IListener` 订阅或直接 hook 生产方。

---

## 2. 独立 Xposed 模块实现方案（纯后台，Java）

> 你的模块是独立 Java/Xposed，不依赖 LSPilot 插件框架。以下为模块内主要 Hook 骨架。

### 2.1 需要 Hook 的位置
```java
// 1) 消息入库 → 检测红包
XC_MethodHook 存储 insert:
  className: "com.tencent.mm.storage.f9"
  methodNames: ["Bb", "Db", "Hb"]   // 按 DexKit 解析结果

// 2) 领取结果 → TTS 金额播报（最稳：直接解析 JSON 参数，不依赖字段名）
XC_MethodHook 领取结果:
  className: "com.tencent.mm.plugin.luckymoney.model.n6"
  methodName: "onGYNetEnd"
  params: int, String, org.json.JSONObject
  afterHooked: 若 firstArg==0 且 json.optLong("sceneAmount")>0 → 播报
```
可选辅助 Hook：
- `LuckyMoneyNewReceiveUI.m7(h6)`：官方成功跳详情/落库点（catch 金额 e1.q），适合做二次保障。
- `gp0.j1.j().e().h(m1,int)`：如需要监控所有 Scene 也可以挂。

### 2.2 后台领取核心代码骨架
```java
// 线程：消息同步线程即可；内部队列线程安全
void tryGrabInBackground(Object e9) throws Exception {
    Class<?> cE9 = e9.getClass();
    String content = (String) cE9.getMethod("j").invoke(e9);      // field_content
    if (content == null) return;
    // 只处理"红包" appmsg（快速字符串判断，官方也是这么判的）
    if (!content.contains("wxpay://c2cbizmessagehandler/hongbao")
        && !content.contains("weixin://openNativeUrl/weixinHB")) return;

    // 解析 appmsg（用 dx0.r.v）
    Class<?> cR = findClass("dx0.r");                       // DexKit 解析
    Object r = cR.getMethod("v", String.class).invoke(null, content);
    if (r == null) return;
    String nativeUrl = (String) getField(cR, "s1", r);
    if (nativeUrl == null || nativeUrl.isEmpty()) return;
    // 只抢普通红包（商家/union 走各自 Scene，可按需扩展）
    if (!nativeUrl.startsWith("wxpay://c2cbizmessagehandler/hongbao/receivehongbao")) return;

    Uri u = Uri.parse(nativeUrl);
    String sendId  = u.getQueryParameter("sendid");
    String channel = u.getQueryParameter("channelid");
    int channelId  = (channel != null && !channel.isEmpty()) ? Integer.parseInt(channel) : 1;
    String session = (String) cE9.getMethod("N0").invoke(e9);  // field_talker
    if (sendId == null || sendId.isEmpty()) return;

    // 防重：sendId 集合 / 时间窗
    if (!dedup.add(sendId)) return;

    // 构造领取 Scene
    Class<?> cN6 = findClassByCgi("/cgi-bin/mmpay-bin/receivewxhb"); // DexKit
    Object scene = cN6.getConstructor(int.class,int.class,String.class,String.class,int.class,String.class,String.class)
                      .newInstance(1, channelId, sendId, nativeUrl, 1, "v1.0",
                                   session == null ? "" : session);
    // 派发
    Class<?> cKernel = findKernel();                 // gp0.j1
    Object kernel  = cKernel.getMethod("j").invoke(null);
    Object queue   = cKernel.getMethod("e").invoke(kernel);      // com.tencent.mm.modelbase.r1
    queue.getClass().getMethod("h", m1Class, int.class).invoke(queue, scene, 0);
}

// 在 n6.onGYNetEnd afterHooked 中：
void onReceived(Object scene, int errType, int errCode, JSONObject json) {
    if (errType == 0 && errCode == 0 && json != null) {
        long amountFen = json.optLong("sceneAmount");   // 或读 scene.r
        if (amountFen > 0) {
            String yuan = String.format(Locale.CHINA, "%.2f", amountFen / 100.0);
            speak("收到红包 " + yuan + " 元");           // 调用你自己的 TTS
        }
    }
}
```

### 2.3 TTS 播报接入点
- 播报内容建议：`"收到红包 %.2f 元"`，如需播报发送人昵称可读 `n6.o (sendNick)`。
- 若在红包被「抢到」（i==0 且 sceneAmount>0）才播报；若返回 hbStatus 异常/已领完（sceneAmount==0 或 json 含错误码）不播报。
- 你的模块已有 TTS，直接在此回调调用即可；注意不要在主线程卡顿，TTS 初始化放模块 Application/首包。

### 2.4 工程细节
- 模块入口：`IXposedHookLoadPackage`，`handleLoadPackage` 判 `packageName.equals("com.tencent.mm")`。
- Context：`(Context) Class.forName("com.tencent.mm.sdk.platformtools.a3").getField("a").get(null)` 拿 Application Context。
- 何时 hook：建议 `Application` attach/主进程起来后再 hook 存储类（用 `XC_InitZygote` 不需要，普通模块在 loadPackage 里 hook 即可，存储类在主进程加载）。
- 权限：微信钱包场景需要已登录 + 可用，直接调用与点击是一致的后端 CGI，无额外权限。

---

## 3. DexKit 动态适配方案（后续微信版本）

该微信版本消息层已深度混淆，强烈建议按「字符串锚点 + 签名锚点 + 行为锚点」用 DexKit 在运行时解析，全部缓存到静态字段，避免硬编码。

### 3.1 每个锚点的 DexKit 搜索条件（DexKitBridge）

| 目标 | DexKit 条件 | 说明 |
|---|---|---|
| 领取 Scene（原来 n6） | `findMethod().usingStrings("sceneAmount","receiveStatus","MicroMsg.NetSceneReceiveLuckyMoney")` 或 `findClass().usingStrings("/cgi-bin/mmpay-bin/receivewxhb")` | onGYNetEnd 所在类；再找 ctor `(int,int,String,String,int,String,String)` |
| union Scene（i6） | `findClass().usingStrings("/cgi-bin/mmpay-bin/unionhb/openunionhb")` | |
| 商家 Scene（u5） | `findClass().usingStrings("/cgi-bin/mmpay-bin/businesshongbao")` | |
| 查询详情（x5） | `findClass().usingStrings("/cgi-bin/mmpay-bin/qrydetailwxhb")` | |
| 消息实体（im.c8） | `findClass().fields(contains("field_content","field_type","field_talker","field_msgId"))` 或 methods `getType()/setType(int)` | 字段原生名是强锚点 |
| e9 包装类 | 继承消息实体且可 getMsgId | 可跳过，直接用实体父类 |
| 消息存储（f9） | `findClass().usingStrings("CREATE TABLE IF NOT EXISTS message","MicroMsg.MsgInfoStorage")`；插入方法=签名为 `(消息实体,boolean)J` 或 `(消息实体)J` 的方法 | |
| appmsg 解析（dx0.r） | `findMethod().usingStrings("weixin://openNativeUrl/weixinHB/startreceivebizhbrequest","wxpay://c2cbizmessagehandler/hongbao/receivehongbao")` → 用该字符串的类 | 然后找 `v(String)` 静态方法 |
| 场景队列（r1） | `findMethod().params("com.tencent.mm.modelbase.m1",int).returnType(boolean).name("h")` 所在类；或 `findClass().usingStrings("MicroMsg.NetsceneQueue")` | |
| MMKernel（gp0.j1） | `findMethod().name("j").returnType(class).static` 且类上有 `e()` 返回队列类 | 或 `usingStrings("MMKernel")` |
| 接收 UI | `findClass().usingStrings("key_native_url","key_sendid","key_way")` 且继承 MMActivity | 用于捕捉 UI 链路调试 |
| 金额详情（e1） | 类实现 `com.tencent.mm.protobuf.f` + 字段含 long；或用接收 UI 的 `amount:` 日志字符串定位 m7 | |

### 3.2 运行时解析顺序与缓存
1. 模块 Application/首包时 `DexKitBridge.create(apkPath)`（或对已加载 dex 走内存 bridge）。
2. 按 3.1 依次解析，全部 `single()`/`first()`，结果存 `static Class<?>` + Method/Field 引用。
3. 解析失败打日志 + 降级（例如某些类型用内容前缀匹配代替类型判断）。
4. 每个版本发布前用「检索自检页」跑一遍，确认全部锚点命中。

### 3.3 字段变更兜底
- 金额优先读 JSON：`onGYNetEnd` 的参数 `JSONObject.optLong("sceneAmount")` 永不变（服务端协议字段），这是最抗混淆的方案。
- 消息字段用原生名 `field_*`（微信自动生成保持多年未变）。
- 场景构造参数顺序用 DexKit 精确匹配 ctor 签名锁定。

---

## 4. 需要二次审查确认的点（本次已核对）
1. ✅ `n6` 构造参数 7 个及字段/JSON 映射（smali 逐参核对）
2. ✅ CGI `receivewxhb`、`businesshongbao`、`unionhb/openunionhb`（H() 方法核实）
3. ✅ 金额字段：`n6.r`=sceneAmount、详情 `e1.q`=receiveAmount（分）
4. ✅ 派发链：`gp0.j1.j().e().h(scene,0)`（smali 核实）
5. ✅ 消息实体字段名与 getter（`im.c8` 108 字段 + getType/j/N0 的 smali 核实）
6. ✅ 官方红包判定：`dx0.r.v(content).s1` 前缀（d4.f0 源码核实）
7. ⚠️ 商家/union 红包的具体防抢差异建议按 3.1 扩展（本次给出 u5/i6 入口）

---

## 5. 注意事项
- **风控**：自动抢属自动化操作，高频/异常频率可能触发微信安全策略；务必加「随机延时 + 频率上限 + 开关」。
- **金额合法性**：只播报成功抢到的金额（sceneAmount>0 且 errCode==0）。
- **防重复**：sendId 去重集合 + 时间窗口 + 记录已抢（可用 `lj0.a3.ij().t1(nativeUrl)` 查询本地红包库 `ec` 判断 field_receiveAmount>0）。
- **线程**：n6.onGYNetEnd 在 IO/回调线程，TTS 调用建议 post 到主线程。
- **兼容性**：所有类名/方法名按 DexKit 动态解析为准，本报告类名是当前版本的硬编码参考值。

---

## 附录 A：关键类速查表
```
红包识别：    com.tencent.mm.ui.chatting.viewitems.d4.f0(e9)（官方模板）
appmsg 解析： dx0.r.v(String) → dx0.r（s1=nativeUrl）
领取 Scene：   com.tencent.mm.plugin.luckymoney.model.n6（receivewxhb）
结果解析：     n6.onGYNetEnd(int,String,JSONObject) → r=sceneAmount
详情模型：     com.tencent.mm.plugin.luckymoney.model.e1（q=amount/分, A=receiveStatus）
派发队列：     com.tencent.mm.modelbase.r1.h(m1,int)
内核入口：     gp0.j1.j() → j1.e()=r1
消息实体：     com.tencent.mm.storage.e9 → im.c8（field_* 字段）
消息存储：     com.tencent.mm.storage.f9（Bb/Db/Hb 插入）
应用 Context： com.tencent.mm.sdk.platformtools.a3.a
```

## 附录 B：本次分析使用的关键工具记录
- search_classes / find_class：定位 luckymoney 包与各 Scene
- view_strings：确认 CGI、JSON key、log TAG（NetSceneReceiveLuckyMoney 等）
- class_hierarchy：确认继承链 n6→e6→q5→d1→m1、e9→p3→c8
- get_method_smali：逐参核对 n6 构造、onGYNetEnd、dispatch 链、消息 getter
- decompile_class/method：d4.f0 红包判定、LuckyMoneyNewReceiveUI.onCreate/m7 领取与金额
- find_class_usage：确认 n6 被各接收 UI 持有
```
