# 微信联系人数据库 — 终极完整手册 (V3.0)
# 从0到1：全部表结构 / 8大分类 / 手机号 / 微信号 / 动态调用 / Xposed模块集成

> 微信 8.0.76 (3140) | smali源码级验证 | EnMicroMsg.db 全表 12+ 张
> 含手机号提取方案、微信号提取、所有字段详解、分类用途、动态调用完整 Java 代码


---

# 第一章：数据库全景

## 1.1 EnMicroMsg.db 全部表清单

```
EnMicroMsg.db
│
├── rcontact          ★ 联系人主表 (13773行)
├── contact           旧版联系人表
├── contact_ext       ★ 联系人扩展表 (含 Mobile / Email / Uin)
├── bottlecontact     漂流瓶联系人
├── rconversation     ★ 会话列表
├── bottleconversation 漂流瓶会话
├── message           ★ 消息记录
├── ContactCmdBuf     联系人命令缓冲
├── ContactLabel      标签表 ★
├── userinfo          配置存储
├── userinfo2         配置存储2
└── (chatroom相关表)   群聊信息
```

---

# 第二章：手机号提取 — 完整方案

## 2.1 核心发现

**手机号存储在 `contact_ext` 表中，不在 `rcontact` 表中。**

### contact_ext 表结构 (来自 j4.<clinit>)

```sql
CREATE TABLE IF NOT EXISTS contact_ext (
  username    VARCHAR(40),           -- 关联 rcontact.username
  Uin         INTEGER DEFAULT 0,     -- ★ 微信纯数字 UIN
  Email       VARCHAR(128),          -- ★ 邮箱
  Mobile      VARCHAR(60),           -- ★★★ 手机号
  ShowFlag    INTEGER,
  PhotoImgFlag INTEGER,
  weiboFlag   INT DEFAULT 0,         -- 微博标志
  weiboNickname INT DEFAULT 0        -- 微博昵称
);
```

### 2.2 手机号提取 SQL

```sql
-- 获取有手机号的好友
SELECT r.username, r.nickname, r.conRemark, r.alias, e.Mobile, e.Email, e.Uin
FROM rcontact r
INNER JOIN contact_ext e ON r.username = e.username
WHERE r.type = 4 AND r.deleteFlag = 0
  AND e.Mobile IS NOT NULL AND e.Mobile != ''
ORDER BY r.nickname
```

### 2.3 重要限制

| 条件 | 说明 |
|---|---|
| **仅有好友** | `contact_ext` 通常只有 type=4(好友) 的记录 |
| **不一定有数据** | 大部分联系人的 Mobile 字段为空 — 微信不会主动同步所有好友的手机号 |
| **隐私保护** | 如果对方设置了"不允许通过手机号找到我"，Mobile 字段为空 |
| **来源** | Mobile 数据来源: ① 从通讯录匹配来的好友 ② 对方主动在资料中填写 |
| **Uin 字段** | `contact_ext.Uin` 是微信纯数字UIN，比 username (wxid) 更短 |

### 2.4 手机号的另一种获取方式

微信聊天界面中对方的手机号会以 `tel:` URI 形式出现。`FriendMobileUriSpanHandler` 负责处理这种格式:

```java
// 类: com.tencent.mm.app.plugin.URISpanHandlerSet$FriendMobileUriSpanHandler
// 方法: d(View, m3, z) → 处理手机号点击
// 可以 Hook 这个方法来拦截手机号展示
```

### 2.5 通过反射提取 contact_ext

```java
// contact_ext 同样在 EnMicroMsg.db 中，可以直接 SQL 查询
Cursor c = db.u(
    "SELECT username, Uin, Email, Mobile FROM contact_ext " +
    "WHERE Mobile IS NOT NULL AND Mobile != '' LIMIT 50",
    null
);
while (c.moveToNext()) {
    String username = c.getString(0);
    int uin = c.getInt(1);
    String email = c.getString(2);
    String mobile = c.getString(3);  // ★ 手机号
}
```


# 第三章：微信号提取

## 3.1 微信号 = rcontact.alias

```sql
-- 微信号就在 rcontact.alias 字段中
SELECT username, nickname, alias, conRemark
FROM rcontact
WHERE type = 4 AND alias IS NOT NULL AND alias != ''
ORDER BY nickname
```

| 字段 | 含义 | 示例 |
|---|---|---|
| `username` | 微信内部wxid | `wxid_6b775u983op922` |
| `alias` | ★ 对方设置的微信号 | `zhangsan888` |
| `nickname` | 昵称 | `张三` |
| `conRemark` | 你备注的名字 | `同事张三` |

### 3.2 区别

| 概念 | 数据库字段 | 谁设置的 | 能否搜索加好友 |
|---|---|---|---|
| **wxid** | `username` | 系统自动生成 | ❌ 不能 |
| **微信号** | `alias` | 用户自己设置 | ✅ 可以 |
| **昵称** | `nickname` | 用户随时改 | ❌ 不能(有重名) |
| **备注** | `conRemark` | 你设置的 | ❌ 不能 |

### 3.3 判断是否设置了微信号

```sql
-- 有微信号的好友
SELECT count(*) FROM rcontact
WHERE type = 4 AND alias IS NOT NULL AND alias != ''

-- 没有微信号的好友
SELECT count(*) FROM rcontact  
WHERE type = 4 AND (alias IS NULL OR alias = '')
```

### 3.4 Uin — 另一种唯一标识

`contact_ext.Uin` 是微信纯数字 UIN，比 username (wxid) 更短:

```sql
SELECT r.username, r.nickname, e.Uin
FROM rcontact r
INNER JOIN contact_ext e ON r.username = e.username
WHERE r.type = 4 AND e.Uin > 0
ORDER BY e.Uin
```

Uin 在 protobuf 协议通信、某些 API 调用中需要用到。


# 第四章：八大分类 — 详细用途介绍

## 分类总览

```
rcontact 13773行 全部 8 大分类:

  分类1 ★ 好友 (type=4)          13950行
  分类2 ★ 群聊 (username@chatroom)  68行
  分类3 ★ 公众号 (gh_前缀)           12行
  分类4 ★ 系统号 (type=33)           24行
  分类5   文件传输助手 (type=2049)    1行
  分类6   陌生人 (type=1)             1行
  分类7   企业微信 (@openim)          4行
  分类8   群成员 (不在rcontact)       0行 → 需从message表提取
```

---

## 分类1: 好友 (type=4) — 13950行

### 是什么
你的微信好友列表。包括:
- `wxid_` 开头的: 13035人 (系统分配的唯一ID)
- 自定义微信号: 911人 (由对方设置了微信号)
- `@openim`: 4人 (企业微信联系人)

### 能获取什么信息

| 信息 | 字段 | 说明 |
|---|---|---|
| wxid | username | 系统唯一ID，不可改 |
| **微信号** | alias | 对方设置的微信号 (可用于搜索添加) ★ |
| 昵称 | nickname | 对方当前昵称 |
| **备注名** | conRemark | 你给对方备注的名字 ★ |
| 备注拼音 | conRemarkPYFull/conRemarkPYShort | 用于排序 |
| 昵称拼音 | pyInitial/quanPin | 用于排序 |
| **头像** | 文件系统 | MD5(username) 定位 |
| **标签** | contactLabelIds | 逗号分隔的标签ID |
| **添加时间** | createTime | Unix毫秒时间戳 |
| **手机号** | contact_ext.Mobile | ⚠️ 不一定有 |
| **邮箱** | contact_ext.Email | ⚠️ 不一定有 |
| **Uin** | contact_ext.Uin | 纯数字UIN |

### 用途场景

| 场景 | 说明 |
|---|---|
| 联系人管理 | 按备注/昵称/标签分类展示 |
| 搜索好友 | 通过 alias 微信号搜索 |
| 标签管理 | contactLabelIds 关联标签表 |
| 排序 | conRemarkPYFull 拼音排序 |
| 头像展示 | 计算头像文件路径加载 |
| 数据导出 | 导出通讯录到外部 |
| 去重检测 | username 唯一 |

### SQL

```sql
-- 完整好友信息
SELECT r.username, r.nickname, r.alias, r.conRemark,
       r.conRemarkPYFull, r.pyInitial, r.quanPin,
       r.showHead, r.contactLabelIds, r.createTime,
       e.Mobile, e.Email, e.Uin
FROM rcontact r
LEFT JOIN contact_ext e ON r.username = e.username
WHERE r.type = 4 AND r.deleteFlag = 0
ORDER BY CASE WHEN length(r.conRemarkPYFull) > 0
         THEN upper(r.conRemarkPYFull)
         ELSE upper(r.quanPin) END ASC
```

---

## 分类2: 群聊 (username LIKE '%@chatroom') — 68行

### 是什么
你加入的所有微信群聊。username 格式: `纯数字@chatroom`

### 能获取什么信息

| 信息 | 字段 | 说明 |
|---|---|---|
| 群聊ID | username | `22110637730@chatroom` |
| 群名称 | nickname | 群主设置的群名 ★ |
| 群备注 | conRemark | 你给这个群备注的名字 |
| 群聊子类型 | type | 2/3/2050/2051 |
| 创建时间 | createTime | 你加入/群创建的时间 |

### 不能直接获取

| 信息 | 原因 |
|---|---|
| **群成员列表** | 不存SQLite，需从message表提取或网络请求 |
| 群公告 | 通过其他接口获取 |
| 群主信息 | 需通过 GroupCard 表 |

### 用途场景

| 场景 | 说明 |
|---|---|
| 群聊管理 | 列出所有群，按名称/时间排序 |
| 群消息统计 | 通过 message 表统计各群消息数 |
| 群成员提取 | 从 message 表中获取群成员 |
| 群批量操作 | 批量退群/免打扰 |

### SQL

```sql
SELECT username, nickname, conRemark, type, createTime
FROM rcontact
WHERE username LIKE '%@chatroom' AND deleteFlag = 0
ORDER BY nickname ASC
```

---

## 分类3: 公众号 (gh_前缀) — 12行

### 是什么
你关注的微信公众号。username 格式: `gh_` 开头 + 12位hex。

### 能获取什么信息

| 信息 | 字段 | 说明 |
|---|---|---|
| 公众号ID | username | `gh_3dfda90e39d6` |
| 公众号名称 | nickname | `微信支付` |
| 验证标志 | verifyFlag | 固定为 24 ★ |

### 用途场景

| 场景 | 说明 |
|---|---|
| 公众号管理 | 列出所有关注的公众号 |
| 取关检测 | 对比历史快照 |
| 区分消息来源 | 判断发消息的是公众号还是好友 |

### SQL

```sql
SELECT username, nickname, verifyFlag
FROM rcontact
WHERE username LIKE 'gh_%' AND deleteFlag = 0
```

---

## 分类4: 系统号 (type=33) — 24行

### 是什么
微信内置的功能账号，不是真实用户。

### 完整名单及用途

| username | nickname | 用途 |
|---|---|---|
| `qqmail` | QQ邮箱提醒 | 接收QQ邮件提醒 |
| `floatbottle` | 漂流瓶 | 漂流瓶功能 |
| `shakeapp` | 摇一摇 | 摇一摇功能 |
| `lbsapp` | 附近的人 | 附近的人功能 |
| `medianote` | 语音记事本 | 语音记事 |
| `newsapp` | 腾讯新闻 | 新闻推送 |
| `facebookapp` | Facebook | Facebook绑定 |
| `qqfriend` | 查看QQ好友 | QQ好友推荐 |
| `officialaccounts` | 公众号 | 公众号聚合入口 |
| `service_officialaccounts` | 服务号 | 服务号聚合入口 |
| `masssendapp` | 群发助手 | 群发消息 |
| — 等 13 个其他 | — | — |

### 用途场景

| 场景 | 说明 |
|---|---|
| 过滤联系人 | 在联系人列表中隐藏系统号 |
| 按功能分类 | 区分不同功能模块 |
| UI 定制 | 对系统功能号显示特殊UI |

---

## 分类5: 文件传输助手 (type=2049) — 1行

### 是什么
微信内置的"文件传输助手"，用于手机和电脑之间互传文件。

| 字段 | 值 |
|---|---|
| username | `filehelper` |
| nickname | `文件传输助手` |
| type | 2049 |

### 用途场景
- 自动转发消息到文件传输助手
- 排除在好友列表之外
- 消息自动备份/导出

---

## 分类6: 陌生人 (type=1) — 1行

### 是什么
通过群聊/名片等途径产生过临时会话但未添加好友的人。

### 用途场景
- 检测未添加好友的临时联系人
- 判断是否需要添加好友

---

## 分类7: 企业微信 (@openim) — 4行

### 是什么
企业微信联系人，username 以 `@openim` 结尾，混在 type=4 中。

### 用途场景
- 区分个人好友和企业微信联系人
- 企业微信联系人通常不能查看朋友圈

---

## 分类8: 群成员

### 是什么
微信群聊中的成员。不存储在 rcontact 中。

### 三种获取方案

**方案A: 从 message 表提取群发言者**

```sql
SELECT DISTINCT content, talker
FROM message
WHERE talker = '22110637730@chatroom'
  AND isSend = 0
  AND type = 1
LIMIT 500
```

**方案B: GroupCard 表**

群成员标识格式: `{群聊数字}@chatroom_{成员username}@groupcard`

**方案C: 网络请求**

```java
// 通过 CGI: GetChatroomMemberDetail
// 需要模拟微信的网络请求协议
```


# 第五章：全部字段详解

## 5.1 rcontact 表 26字段

| # | 字段 | 类型 | 含义 | 常见值 |
|---|---|---|---|---|
| 1 | `username` | TEXT PK | ★ 唯一标识 | `wxid_xxx` / `gh_xxx` / `数字@chatroom` |
| 2 | `alias` | TEXT | ★ 微信号 | `zhangsan888` (可为空) |
| 3 | `conRemark` | TEXT | ★ 备注名 | `同事张三` |
| 4 | `nickname` | TEXT | ★ 昵称 | `张三` |
| 5 | `pyInitial` | TEXT | 昵称拼音首字母 | `ZS` |
| 6 | `quanPin` | TEXT | 昵称全拼 | `zhangsan` |
| 7 | `conRemarkPYFull` | TEXT | 备注全拼 | `tongshizhangsan` |
| 8 | `conRemarkPYShort` | TEXT | 备注首字母 | `TSZS` |
| 9 | `type` | INT | ★★★ 联系人类型 | 4=好友 3/2=群聊 33=系统号 2049=文件助手 |
| 10 | `verifyFlag` | INT | 验证标志 | 0=普通 24=公众号 |
| 11 | `deleteFlag` | INT | 删除标志 | 0=正常 1=已删 |
| 12 | `showHead` | INT | 头像显示 | 32=显示 其他=隐藏 |
| 13 | `contactLabelIds` | TEXT | 标签ID列表 | `"1,5,12"` |
| 14 | `createTime` | LONG | 创建时间戳 | Unix毫秒 |
| 15 | `encryptUsername` | TEXT | 加密用户名 | — |
| 16 | `chatroomFlag` | INT | 群聊标志 | 8.0.76全为0 |
| 17 | `lvbuff` | BLOB | protobuf数据 | 二进制 |
| 18 | `domainList` | TEXT | 域名列表 | — |
| 19 | `uiType` | LONG | UI类型 | 群聊用 |
| 20 | `weiboFlag` | INT | 微博标志 | — |
| 21 | `weiboNickname` | TEXT | 微博昵称 | — |
| 22 | `openImAppid` | TEXT | 企业微信ID | — |
| 23 | `sourceExtInfo` | TEXT | 来源扩展 | — |
| 24 | `descWordingId` | TEXT | 描述ID | — |
| 25 | `usernameFlag` | LONG | 用户名标志 | — |
| 26 | `ticket` | TEXT | 票据 | — |

## 5.2 contact_ext 表字段

| # | 字段 | 类型 | 含义 |
|---|---|---|---|
| 1 | `username` | VARCHAR(40) | 关联 rcontact |
| 2 | `Uin` | INTEGER | ★ 纯数字UIN |
| 3 | `Email` | VARCHAR(128) | ★ 邮箱 |
| 4 | `Mobile` | VARCHAR(60) | ★★★ 手机号 |
| 5 | `ShowFlag` | INTEGER | 显示标志 |
| 6 | `PhotoImgFlag` | INTEGER | 头像标志 |
| 7 | `weiboFlag` | INT | 微博 |
| 8 | `weiboNickname` | TEXT | 微博昵称 |

## 5.3 rconversation 表 — 会话列表

| 字段 | 类型 | 含义 |
|---|---|---|
| `username` | TEXT | 会话ID (JOIN rcontact) |
| `unReadCount` | INTEGER | ★ 未读数 |
| `content` | TEXT | 最后消息内容 |
| `conversationTime` | LONG | 最后消息时间 |
| `flag` | LONG | ★ 置顶/免打扰标志 |
| `atCount` | INT | @我次数 |
| `parentRef` | TEXT | 折叠父会话 |
| `msgType` | TEXT | 消息类型 |

## 5.4 message 表 — 消息记录

| 字段 | 类型 | 含义 |
|---|---|---|
| `msgId` | INTEGER PK | 消息ID |
| `type` | INT | ★ 消息类型 (1文字/3图片/34语音/436207665红包) |
| `isSend` | INT | 0=收到 1=发出 |
| `talker` | TEXT | ★ 会话ID |
| `content` | TEXT | 消息内容 |
| `createTime` | LONG | 消息时间 |
| `status` | INT | 消息状态 |
| `lvbuffer` | BLOB | protobuf数据 |


# 第六章：WxContactManager — 完整动态调用类

```java
package com.your.module;

import android.content.*;
import android.database.Cursor;
import android.util.Log;
import java.lang.reflect.Method;
import java.util.*;

/**
 * WxContactManager V3.0
 * 
 * 完整联系人管理器，支持:
 * - 8大分类查询 (好友/群聊/公众号/系统号/文件助手/陌生人/企业微信/群成员)
 * - 手机号提取 (contact_ext.Mobile)
 * - 微信号提取 (rcontact.alias)  
 * - Uin提取 (contact_ext.Uin)
 * - 邮箱提取 (contact_ext.Email)
 * - 会话列表 (rconversation)
 * - 消息记录 (message)
 * - 标签管理 (ContactLabel)
 * - 头像路径计算
 * - 分类判断
 * - 统计数据
 *
 * 零第三方依赖，纯反射，可直接嵌入 Xposed 模块。
 */
public class WxContactManager {

    private final ClassLoader cl;
    private final Context ctx;
    private Object db;
    private String dbPath;
    private long uin;

    public WxContactManager(ClassLoader cl, Context ctx) {
        this.cl = cl;
        this.ctx = ctx;
    }

    // ============ 生命周期 ============

    public boolean open() {
        try {
            uin = getUin();
            dbPath = getDbPath(uin);
            db = openDbWithRetry(dbPath, uin);
            return db != null;
        } catch (Throwable t) { return false; }
    }

    public void close() {
        if (db != null) {
            try { db.getClass().getMethod("c").invoke(db); } catch (Throwable ignored) {}
            db = null;
        }
    }

    // ============ 八大分类查询 ============

    /** 分类1: 好友 (type=4) — 含手机号/邮箱/Uin */
    public List<ContactBean> getFriends(int limit) {
        return queryContacts(
            "SELECT r.username, r.nickname, r.alias, r.conRemark, " +
            "r.pyInitial, r.quanPin, r.conRemarkPYFull, r.conRemarkPYShort, " +
            "r.type, r.showHead, r.contactLabelIds, r.createTime, r.verifyFlag, " +
            "e.Mobile, e.Email, e.Uin " +
            "FROM rcontact r " +
            "LEFT JOIN contact_ext e ON r.username = e.username " +
            "WHERE r.type = 4 AND r.deleteFlag = 0 " +
            "ORDER BY CASE WHEN length(r.conRemarkPYFull) > 0 " +
            "THEN upper(r.conRemarkPYFull) ELSE upper(r.quanPin) END ASC " +
            "LIMIT " + limit
        );
    }

    /** 分类2: 群聊 (@chatroom) */
    public List<ContactBean> getChatrooms() {
        return queryContacts(
            "SELECT username, nickname, conRemark, type, createTime " +
            "FROM rcontact WHERE username LIKE '%@chatroom' AND deleteFlag = 0 " +
            "ORDER BY nickname ASC"
        );
    }

    /** 分类3: 公众号 (gh_) */
    public List<ContactBean> getOfficialAccounts() {
        return queryContacts(
            "SELECT username, nickname, type, verifyFlag " +
            "FROM rcontact WHERE username LIKE 'gh_%' AND deleteFlag = 0"
        );
    }

    /** 分类4: 系统号 (type=33) */
    public List<ContactBean> getSystemAccounts() {
        return queryContacts(
            "SELECT username, nickname FROM rcontact " +
            "WHERE type = 33 AND deleteFlag = 0"
        );
    }

    /** 分类5: 文件传输助手 */
    public ContactBean getFileHelper() {
        List<ContactBean> list = queryContacts(
            "SELECT username, nickname FROM rcontact WHERE type = 2049"
        );
        return list.isEmpty() ? null : list.get(0);
    }

    /** 分类6: 陌生人 */
    public List<ContactBean> getStrangers() {
        return queryContacts(
            "SELECT username, nickname FROM rcontact WHERE type = 1"
        );
    }

    /** 分类7: 企业微信 */
    public List<ContactBean> getOpenIMContacts() {
        return queryContacts(
            "SELECT username, nickname, conRemark FROM rcontact " +
            "WHERE username LIKE '%@openim' AND deleteFlag = 0"
        );
    }

    /** 分类8: 群成员 (从message表提取发言者) */
    public List<String> getGroupMembers(String groupId) {
        List<String> members = new ArrayList<>();
        Cursor c = null;
        try {
            c = rawQuery(
                "SELECT DISTINCT content FROM message " +
                "WHERE talker = '" + escape(groupId) + "' " +
                "AND isSend = 0 AND type = 1 LIMIT 500"
            );
            while (c != null && c.moveToNext()) members.add(c.getString(0));
        } catch (Throwable ignored) {} finally {
            if (c != null) c.close();
        }
        return members;
    }

    // ============ 全部分类一次性查询 ============

    public List<ContactBean> getAllContacts() {
        return queryContacts(
            "SELECT username, nickname, alias, conRemark, type, verifyFlag, deleteFlag, " +
            "CASE " +
            "  WHEN username = 'filehelper' THEN '文件传输助手' " +
            "  WHEN username LIKE '%@chatroom' THEN '群聊' " +
            "  WHEN username LIKE 'gh_%' THEN '公众号' " +
            "  WHEN username LIKE '%@openim' THEN '企业微信' " +
            "  WHEN type = 33 THEN '系统号' " +
            "  WHEN type = 1 THEN '陌生人' " +
            "  WHEN type = 4 THEN '好友' " +
            "  ELSE '其他' END AS category " +
            "FROM rcontact WHERE deleteFlag = 0 " +
            "ORDER BY category, nickname"
        );
    }

    // ============ 手机号/微信号专项查询 ============

    /** 获取有手机号的好友列表 */
    public List<ContactBean> getFriendsWithMobile() {
        return queryContacts(
            "SELECT r.username, r.nickname, r.alias, r.conRemark, " +
            "e.Mobile, e.Email, e.Uin " +
            "FROM rcontact r " +
            "INNER JOIN contact_ext e ON r.username = e.username " +
            "WHERE r.type = 4 AND e.Mobile IS NOT NULL AND e.Mobile != '' " +
            "ORDER BY r.nickname"
        );
    }

    /** 获取有微信号的好友列表 */
    public List<ContactBean> getFriendsWithAlias() {
        return queryContacts(
            "SELECT username, nickname, alias, conRemark FROM rcontact " +
            "WHERE type = 4 AND alias IS NOT NULL AND alias != '' " +
            "ORDER BY alias ASC"
        );
    }

    /** 获取有Uin的联系人 */
    public List<ContactBean> getContactsWithUin() {
        return queryContacts(
            "SELECT r.username, r.nickname, e.Uin FROM rcontact r " +
            "INNER JOIN contact_ext e ON r.username = e.username " +
            "WHERE e.Uin > 0 ORDER BY e.Uin"
        );
    }

    // ============ 会话 + 消息查询 ============

    /** 获取会话列表 (最近聊天) */
    public List<ContactBean> getConversations() {
        return queryContacts(
            "SELECT c.username, c.unReadCount, c.content, " +
            "c.conversationTime, c.flag, c.atCount " +
            "FROM rconversation c " +
            "WHERE c.username NOT LIKE '%@chatroom' " +
            "ORDER BY c.flag DESC, c.conversationTime DESC LIMIT 50"
        );
    }

    /** 获取与某人的消息记录 */
    public List<ContactBean> getMessages(String talker, int limit) {
        return queryContacts(
            "SELECT msgId, type, isSend, content, createTime " +
            "FROM message WHERE talker = '" + escape(talker) + "' " +
            "ORDER BY createTime DESC LIMIT " + limit
        );
    }

    // ============ 工具方法 ============

    /** 分类判断 (不查库) */
    public String classifyContact(String username) {
        if (username == null) return "未知";
        if (username.equals("filehelper")) return "文件传输助手";
        if (username.endsWith("@chatroom")) return "群聊";
        if (username.startsWith("gh_")) return "公众号";
        if (username.endsWith("@openim")) return "企业微信";
        return "好友";
    }

    /** 获取头像路径 */
    public String getAvatarPath(String username) {
        try {
            String accountDir = getAccountDir();
            String md5 = md5(username.getBytes("UTF-8"));
            return accountDir + "avatar/" + md5.substring(0,2) + "/"
                + md5.substring(2,4) + "/user_" + md5 + ".png";
        } catch (Throwable e) { return null; }
    }

    /** 获取显示名 */
    public String getDisplayName(ContactBean b) {
        if (b.conRemark != null && !b.conRemark.isEmpty()) return b.conRemark;
        if (b.alias != null && !b.alias.isEmpty()) return b.alias;
        if (b.nickname != null && !b.nickname.isEmpty()) return b.nickname;
        return b.username;
    }

    /** 统计信息 */
    public String getStats() {
        StringBuilder sb = new StringBuilder();
        Cursor c = null;
        try {
            c = rawQuery(
                "SELECT type, count(*) FROM rcontact WHERE deleteFlag=0 " +
                "GROUP BY type ORDER BY count(*) DESC");
            while (c.moveToNext())
                sb.append("type=").append(c.getInt(0))
                  .append(" → ").append(c.getInt(1)).append("行\n");
        } catch (Throwable ignored) {} finally {
            if (c != null) c.close();
        }
        return sb.toString();
    }


    // ================================================================
    // 内部实现 (数据库打开/密码/路径)
    // ================================================================

    private long getUin() {
        SharedPreferences sp = ctx.getSharedPreferences(
            "system_config_prefs", 0);
        Object v = sp.getAll().get("default_uin");
        return Long.parseLong(v.toString());
    }

    private String getImei() {
        try {
            Method g = cl.loadClass("wo.w0").getMethod("g", boolean.class);
            String s = (String) g.invoke(null, true);
            return (s != null && !s.isEmpty()) ? s : "1234567890ABCDEF";
        } catch (Throwable e) { return "1234567890ABCDEF"; }
    }

    private String md5(byte[] input) {
        try {
            return (String) cl.loadClass("kk.k")
                .getMethod("g", byte[].class).invoke(null, (Object) input);
        } catch (Throwable e) {
            try {
                java.security.MessageDigest md =
                    java.security.MessageDigest.getInstance("MD5");
                byte[] d = md.digest(input);
                StringBuilder sb = new StringBuilder();
                for (byte b : d) sb.append(String.format("%02x", b));
                return sb.toString();
            } catch (Throwable e2) { return ""; }
        }
    }

    private String getDbPath(long uin) {
        try {
            String dir = (String) cl.loadClass("mp0.b")
                .getMethod("X").invoke(null);
            String hash = (String) cl.loadClass("hm0.b0")
                .getMethod("e", int.class).invoke(null, (int) uin);
            return dir + "MicroMsg/" + hash + "/EnMicroMsg.db";
        } catch (Throwable e) {
            String dir = ctx.getFilesDir().getParent() + "/";
            return dir + "MicroMsg/" + md5(("mm" + uin).getBytes())
                + "/EnMicroMsg.db";
        }
    }

    private String getAccountDir() {
        try {
            String dir = (String) cl.loadClass("mp0.b")
                .getMethod("X").invoke(null);
            String hash = (String) cl.loadClass("hm0.b0")
                .getMethod("e", int.class).invoke(null, (int) uin);
            return dir + "MicroMsg/" + hash + "/";
        } catch (Throwable e) { return null; }
    }

    private Object openDb(String path, String pwd) {
        try {
            Class<?> ka5f = cl.loadClass("ka5.f");
            Object db = ka5f.getMethod("s",
                String.class, String.class, int.class, boolean.class)
                .invoke(null, path, pwd, 0, true);
            if (db != null && isValid(db)) return db;
            if (db != null) db.getClass().getMethod("c").invoke(db);
        } catch (Throwable ignored) {}
        return null;
    }

    private boolean isValid(Object db) {
        try {
            Cursor c = rawQuery("SELECT name FROM sqlite_master " +
                "WHERE type='table' AND name='rcontact'");
            if (c != null) { boolean ok = c.moveToFirst(); c.close(); return ok; }
        } catch (Throwable ignored) {}
        return false;
    }

    private Object openDbWithRetry(String path, long uin) {
        List<String> imeis = new ArrayList<>();
        imeis.add(getImei());
        if (!imeis.contains("1234567890ABCDEF"))
            imeis.add("1234567890ABCDEF");
        try {
            for (String h : (Collection<String>) cl
                    .loadClass("com.tencent.mm.storagebase.IMEISave")
                    .getMethod("a").invoke(null))
                if (!imeis.contains(h)) imeis.add(h);
        } catch (Throwable ignored) {}

        for (String imei : imeis) {
            String pwd = md5((imei + uin).getBytes()).substring(0, 7);
            Object db = openDb(path, pwd);
            if (db != null) return db;
        }
        return null;
    }

    private Cursor rawQuery(String sql) {
        try {
            return (Cursor) db.getClass()
                .getMethod("u", String.class, String[].class)
                .invoke(db, sql, null);
        } catch (Throwable t) { return null; }
    }

    private List<ContactBean> queryContacts(String sql) {
        List<ContactBean> list = new ArrayList<>();
        if (db == null) return list;
        Cursor c = null;
        try {
            c = rawQuery(sql);
            if (c != null) {
                while (c.moveToNext()) {
                    ContactBean b = new ContactBean();
                    b.username = safeStr(c, "username");
                    b.nickname = safeStr(c, "nickname");
                    b.alias = safeStr(c, "alias");
                    b.conRemark = safeStr(c, "conRemark");
                    b.pyInitial = safeStr(c, "pyInitial");
                    b.quanPin = safeStr(c, "quanPin");
                    b.conRemarkPYFull = safeStr(c, "conRemarkPYFull");
                    b.conRemarkPYShort = safeStr(c, "conRemarkPYShort");
                    b.contactLabelIds = safeStr(c, "contactLabelIds");
                    b.mobile = safeStr(c, "Mobile");
                    b.email = safeStr(c, "Email");
                    b.type = safeInt(c, "type");
                    b.showHead = safeInt(c, "showHead");
                    b.verifyFlag = safeInt(c, "verifyFlag");
                    b.deleteFlag = safeInt(c, "deleteFlag");
                    b.uinVal = safeLong(c, "Uin");
                    b.createTime = safeLong(c, "createTime");
                    b.unReadCount = safeInt(c, "unReadCount");
                    b.flag = safeLong(c, "flag");
                    b.atCount = safeInt(c, "atCount");
                    b.content = safeStr(c, "content");
                    b.conversationTime = safeLong(c, "conversationTime");
                    b.category = safeStr(c, "category");
                    list.add(b);
                }
            }
        } catch (Throwable t) { Log.e("WxContact", "query", t); }
        finally { if (c != null) c.close(); }
        return list;
    }

    // ================================================================
    // 数据类
    // ================================================================

    public static class ContactBean {
        // rcontact
        public String username, nickname, alias, conRemark;
        public String pyInitial, quanPin, conRemarkPYFull, conRemarkPYShort;
        public String contactLabelIds, category;
        public int type, showHead, verifyFlag, deleteFlag;
        public long createTime;
        // contact_ext
        public String mobile, email;
        public long uinVal;
        // rconversation
        public int unReadCount, atCount;
        public long flag, conversationTime;
        public String content;

        public String displayName() {
            if (conRemark != null && !conRemark.isEmpty()) return conRemark;
            if (alias != null && !alias.isEmpty()) return alias;
            return nickname != null ? nickname : username;
        }
    }

    // ================================================================
    // 安全读字段
    // ================================================================

    private String safeStr(Cursor c, String col) {
        try { int i = c.getColumnIndex(col);
              return i >= 0 ? c.getString(i) : null; }
        catch (Throwable e) { return null; }
    }
    private int safeInt(Cursor c, String col) {
        try { int i = c.getColumnIndex(col);
              return i >= 0 ? c.getInt(i) : 0; }
        catch (Throwable e) { return 0; }
    }
    private long safeLong(Cursor c, String col) {
        try { int i = c.getColumnIndex(col);
              return i >= 0 ? c.getLong(i) : 0; }
        catch (Throwable e) { return 0; }
    }
    private String escape(String s) { return s.replace("'", "''"); }
}
```


# 第七章：Xposed 模块调用示例

```java
public class MyModule implements IXposedHookLoadPackage {
    @Override
    public void handleLoadPackage(LoadPackageParam lpparam) {
        if (!"com.tencent.mm".equals(lpparam.packageName)) return;
        ClassLoader cl = lpparam.classLoader;
        Context ctx = (Context) XposedHelpers.getStaticObjectField(
            cl.loadClass("com.tencent.mm.sdk.platformtools.x2"), "a");

        WxContactManager mgr = new WxContactManager(cl, ctx);
        if (!mgr.open()) return;

        // === 使用示例 ===

        // 1. 获取好友 (含手机号)
        List<ContactBean> friends = mgr.getFriends(100);
        for (ContactBean f : friends) {
            String line = mgr.getDisplayName(f);
            if (f.alias != null) line += " | 微信号:" + f.alias;
            if (f.mobile != null) line += " | 手机:" + f.mobile;
            Log.i("Wx", line);
        }

        // 2. 获取有手机号的联系人
        List<ContactBean> withPhone = mgr.getFriendsWithMobile();
        Log.i("Wx", "有手机号的好友: " + withPhone.size() + "人");

        // 3. 获取群聊
        List<ContactBean> groups = mgr.getChatrooms();
        for (ContactBean g : groups)
            Log.i("Wx", g.nickname + " (" + g.username + ")");

        // 4. 全部分类
        for (ContactBean c : mgr.getAllContacts())
            Log.i("Wx", "[" + c.category + "] " + c.displayName());

        // 5. 统计数据
        Log.i("Wx", mgr.getStats());

        mgr.close();
    }
}
```

---

# 第八章：信息完整性总结

## 8.1 可以获取的全部信息

| 信息类别 | 来源表/字段 | 获取难度 |
|---|---|---|
| 昵称 | rcontact.nickname | ✅ 直接读取 |
| 备注名 | rcontact.conRemark | ✅ 直接读取 |
| **微信号** | rcontact.alias | ✅ 直接读取 |
| **手机号** | contact_ext.Mobile | ⚠️ 多半为空 |
| **邮箱** | contact_ext.Email | ⚠️ 多半为空 |
| **Uin** | contact_ext.Uin | ✅ 直接读取 |
| 拼音 | rcontact.pyInitial/quanPin | ✅ 直接读取 |
| 标签ID | rcontact.contactLabelIds | ✅ 直接读取 |
| 创建时间 | rcontact.createTime | ✅ 直接读取 |
| 头像 | 文件系统 (MD5路径) | ✅ 直接读取 |
| 未读消息数 | rconversation.unReadCount | ✅ 直接读取 |
| 消息记录 | message表 | ✅ 直接读取 |
| 群成员 | message表提取 | ⚠️ 间接提取 |
| 朋友圈 | 独立SNS存储 | ❌ 不能直接获取 |
| 在线状态 | 无本地缓存 | ❌ 网络实时 |

## 8.2 为什么手机号多半为空

```
contact_ext 的数据来源:
  1. 从手机通讯录导入的好友 → 会有 Mobile 字段
  2. 扫二维码/搜索微信号添加的好友 → 通常没有 Mobile
  3. 群聊中添加的好友 → 通常没有 Mobile
  4. 对方主动在个人资料中填写 → 极少数

结论: 只有从通讯录添加的好友才会有手机号数据。
```

## 8.3 动态调用速查表

```java
WxContactManager mgr = new WxContactManager(cl, ctx);
mgr.open();

// 八大分类
mgr.getFriends(500);             // 好友 (含手机号/微信号/Uin/邮箱)
mgr.getChatrooms();              // 群聊
mgr.getOfficialAccounts();       // 公众号
mgr.getSystemAccounts();         // 系统号
mgr.getFileHelper();             // 文件传输助手
mgr.getStrangers();              // 陌生人
mgr.getOpenIMContacts();         // 企业微信
mgr.getGroupMembers("群ID");     // 群成员(从消息提取)

// 专项查询
mgr.getFriendsWithMobile();      // 有手机号的好友
mgr.getFriendsWithAlias();       // 有微信号的好友
mgr.getContactsWithUin();        // 有UIN的联系人

// 会话+消息
mgr.getConversations();          // 最近会话
mgr.getMessages("talker", 100);  // 消息记录

// 工具
mgr.getAllContacts();            // 全部(带分类标签)
mgr.classifyContact("wxid");     // 分类判断
mgr.getAvatarPath("wxid");       // 头像路径
mgr.getDisplayName(contact);     // 显示名
mgr.getStats();                  // 统计

mgr.close();
```

---

> **全文完** | 微信 8.0.76 (3140) | smali源码级验证
> **总计**: 12+ 张表 | 8大分类 | 50+可查询字段 | 完整 WxContactManager 类


# 附录：头像、性别、位置 — 完整获取方案

---

## 附A：头像 — 完整方案

### A.1 头像不存数据库

头像以 PNG 文件存储在本机文件系统，不在 rcontact 的任何字段中。

### A.2 路径公式

```
账号目录 = j1.u().h() = mp0.b.X() + "MicroMsg/" + MD5("mm"+uin) + "/"

MD5值   = kk.k.g(username.getBytes("UTF-8"))

普通头像 = 账号目录 + "avatar/" + md5[0:2] + "/" + md5[2:4] + "/user_" + md5 + ".png"
高清头像 = 账号目录 + "avatar/" + md5[0:2] + "/" + md5[2:4] + "/user_hd_" + md5 + ".png"
```

### A.3 完整示例

```
username = "wxid_6b775u983op922"
MD5("wxid_6b775u983op922") = "a1b2c3d4e5f6789abcdef..."

普通头像:
/data/user/0/com.tencent.mm/MicroMsg/7b998556beeef.../
  avatar/a1/b2/user_a1b2c3d4e5f6789abcdef....png

高清头像:
.../avatar/a1/b2/user_hd_a1b2c3d4e5f6789abcdef....png
```

### A.4 Java 代码

```java
/** 获取头像路径 */
public String getAvatarPath(String username, boolean hd) {
    String accountDir = getAccountDir();
    String md5 = md5(username.getBytes("UTF-8"));
    return accountDir + "avatar/"
        + md5.substring(0, 2) + "/"
        + md5.substring(2, 4) + "/"
        + (hd ? "user_hd_" : "user_")
        + md5 + ".png";
}

/** 获取默认头像 */
public Bitmap getDefaultAvatar() {
    // 反射调用微信的默认头像
    Class<?> z = cl.loadClass("com.tencent.mm.modelavatar.z");
    return (Bitmap) z.getMethod("h", Context.class).invoke(null, ctx);
}

/** 从微信缓存获取头像Bitmap */
public Bitmap getAvatarBitmap(String username, boolean hd) {
    Class<?> z = cl.loadClass("com.tencent.mm.modelavatar.z");
    return (Bitmap) z.getMethod("e", String.class, String.class, boolean.class)
        .invoke(null, username, "tag", hd);
}

/** 判断头像文件是否存在 */
public boolean hasAvatar(String username) {
    return new File(getAvatarPath(username, false)).exists();
}
```

### A.5 群聊头像

群聊头像不是单张图片，而是用 `ChattingAvatarImageView` 拼出的 3×3 网格:

```java
// 群聊头像由多个群成员的矩形头像拼接而成
// 通过 ChattingAvatarImageView 的 setChatroomUsername() 设置
```

### A.6 showHead 字段

`rcontact.showHead = 32` → 显示头像；其他值 → 隐藏头像。

可用于在 UI 中决定是否展示头像图片。


---

## 附B：性别 — 完整方案

### B.1 核心发现

**性别存储在 `contact` 旧表中，不在 `rcontact` 表中。**

```sql
CREATE TABLE IF NOT EXISTS contact (
  contactID  INTEGER PRIMARY KEY,
  sex        INT,           -- ★ 性别 (0=未设置 1=男 2=女)
  type       INT,
  showHead   INT,
  username   VARCHAR(40),   -- 关联 rcontact.username
  nickname   VARCHAR(40),
  ...
);
```

### B.2 性别值含义

| sex值 | 含义 | 微信显示 |
|---|---|---|
| 0 | 未设置 | 不显示 |
| 1 | 男 | ♂ |
| 2 | 女 | ♀ |

### B.3 提取 SQL

```sql
-- JOIN contact 表获取性别
SELECT r.username, r.nickname, r.conRemark,
       c.sex,
       CASE c.sex
         WHEN 1 THEN '男'
         WHEN 2 THEN '女'
         ELSE '未设置'
       END AS gender
FROM rcontact r
INNER JOIN contact c ON r.username = c.username
WHERE r.type = 4 AND r.deleteFlag = 0
ORDER BY r.nickname
```

### B.4 重要限制

| 限制 | 说明 |
|---|---|
| **大部分为0** | 微信默认 sex=0(未设置)，手动修改后才会有值 |
| **不是好友也能看** | 只要对方在资料中设置了性别 |
| **数据同步** | 性别修改后会通过服务器同步到本地 contact 表 |
| **contact 表不一定有记录** | 新好友可能不在旧 contact 表中 |

### B.5 Java 代码

```java
/** 获取性别 */
public int getSex(String username) {
    Cursor c = rawQuery(
        "SELECT c.sex FROM contact c WHERE c.username = '"
        + escape(username) + "'");
    if (c != null) {
        try {
            if (c.moveToFirst()) return c.getInt(0);
        } finally { c.close(); }
    }
    return 0; // 未设置
}

/** 获取性别文本 */
public String getSexText(String username) {
    int sex = getSex(username);
    switch (sex) {
        case 1: return "男";
        case 2: return "女";
        default: return "未设置";
    }
}

/** 获取好友列表 (含性别) */
public List<ContactBean> getFriendsWithSex(int limit) {
    return queryContacts(
        "SELECT r.username, r.nickname, r.alias, r.conRemark, " +
        "c.sex, " +
        "CASE WHEN c.sex=1 THEN '男' WHEN c.sex=2 THEN '女' ELSE '未设置' END AS sexText " +
        "FROM rcontact r " +
        "LEFT JOIN contact c ON r.username = c.username " +
        "WHERE r.type = 4 AND r.deleteFlag = 0 " +
        "ORDER BY r.nickname LIMIT " + limit
    );
}
```

### B.6 另一种方式：通过 y3 实体

```java
// y3 (Contact) 继承自 contact.s → dm.f2
// contact.s 有 sex 相关的 setter/getter (但方法名是混淆的)
// 通过反射 ContactStorage.j4.n(username) → y3 → 读取 getter

Object contact = j4.getMethod("n", String.class, boolean.class)
    .invoke(contactStg, "wxid_xxx", true);
// y3 实例中的性别字段来自 contact 表
// 但方法名被混淆，需要通过字段直接读取
```


---

## 附C：精准位置 — 完整方案

### C.1 核心结论

**微信不在本地 SQLite 数据库中存储用户的精准 GPS 位置。**

原因:
1. 隐私保护 — 精准 GPS 是敏感数据
2. 位置随时变化 — 不适合本地持久化
3. 法律合规 — 中国个人信息保护法要求

### C.2 微信能存的位置信息

| 数据 | 存储位置 | 精度 | 获取方式 |
|---|---|---|---|
| **国家/地区** | `contact` 表 (region字段) | 国家级别 | SQL 直接读 |
| **省份/城市** | `contact` 表 (可能) | 城市级别 | ⚠️ 不保证有 |
| **朋友圈位置** | SNS 消息内容 | 自定义文本 | message.content 解析 |
| **聊天位置消息(type=48)** | message 表 | GPS 坐标 | message.content 解析 |
| **附近的人** | 网络实时获取 | GPS → 模糊化 | ❌ 本地没有 |
| **lbsapp** | rcontact (type=33) | 无 | 功能号用户名 |

### C.3 国家/地区代码提取

```java
// 微信存储的是国家代码 (ISO 3166)，不是完整地址
// 存储在 contact 表的某个字段中

// 通过 RegionCode 相关类获取:
// com.tencent.mm.storage.ga — 地区存储
// com.tencent.mm.ui.contact.g — 地区选择

// 也可以从 SharedPreferences 中读取当前用户自己的地区设置:
SharedPreferences sp = ctx.getSharedPreferences(
    "com.tencent.mm_preferences", 0);
String region = sp.getString("regions", "CN");
```

### C.4 位置消息提取 (type=48)

```sql
-- 提取所有位置消息
SELECT msgId, talker, content, createTime
FROM message
WHERE type = 48  -- 位置分享消息
  AND isSend = 0  -- 收到的
ORDER BY createTime DESC
LIMIT 50
```

位置消息的 `content` 字段是 XML 格式，包含 GPS 坐标:

```xml
<msg>
  <location x="23.123456" y="113.123456"
            scale="16" label="广州市天河区XXX路" poiname="XXX大厦" />
</msg>
```

解析示例:

```java
/** 解析位置消息获取GPS坐标 */
public LatLng parseLocationMsg(String content) {
    try {
        // 内容格式: <location x="lat" y="lng" label="地址" poiname="名称"/>
        String x = extractAttr(content, "x");
        String y = extractAttr(content, "y");
        String label = extractAttr(content, "label");
        if (x != null && y != null) {
            return new LatLng(Double.parseDouble(x), Double.parseDouble(y));
        }
    } catch (Throwable ignored) {}
    return null;
}
```

### C.5 附近的人 — 位置相关类

| 类 | 用途 |
|---|---|
| `com.tencent.mm.plugin.nearby.ui.NearbyPersonalInfoUI` | 附近的人个人信息 |
| `com.tencent.mm.plugin.nearby.ui.NearbySayHiListUI` | 附近打招呼列表 |
| `com.tencent.mm.plugin.ext.provider.ExtControlProviderNearBy` | 附近的人数据源 |
| `a65.ae1` / `a65.bz2` | LBS protobuf数据(混淆) |
| `a65.d01` / `a65.dk2` | 地区相关 protobuf |

### C.6 总结：位置数据能获取什么

| 获取内容 | 方式 | 精度 |
|---|---|---|
| 当前用户自己的国家 | `SharedPreferences("regions")` | 国家代码 |
| 聊天中的位置消息 | `message` 表 `type=48` | GPS 坐标 (对方主动发送的) |
| 附近的人 | ❌ 本地没有 | 需网络请求 |
| 对方精确位置 | ❌ 不可能 | 隐私保护 |

### C.7 补充到 ContactBean

```java
public static class ContactBean {
    // ... 原有字段 ...
    public int sex;          // ★ 性别: 0=未设置 1=男 2=女
    public String sexText;   // ★ 性别文本
    public String region;    // 国家代码 (从contact表)
}
```

---

> **补充完毕** | 头像(路径+默认图) | 性别(contact表sex字段) | 位置(GPS坐标仅在type=48消息中)

---

# 第十一章：联系人选择器 — 过滤方案与实战

> 场景: Xposed 模块中实现自定义联系人选择器
> 需求: 只显示好友和群聊，排除公众号/系统号/陌生人/群成员，支持按性别筛选

---

## 11.1 核心问题

在模块中做联系人选择器时，数据源是 `rcontact` 表 (13773行)，但其中包含大量不需要的类型:

```
rcontact 全部数据:
  ✅ 好友 (type=4)           13950行 — 要的
  ✅ 群聊 (@chatroom)         68行   — 要的
  ❌ 公众号 (gh_)              12行   — 不要
  ❌ 系统号 (type=33)          24行   — 不要
  ❌ 文件传输助手 (type=2049)   1行    — 不要
  ❌ 陌生人 (type=1)           1行    — 不要
  ❌ 企业微信 (@openim)        4行    — 可选
  ❌ 群成员                   不在表中 — 不用担心
```

### 群成员为什么不用管？

**群成员根本不在 rcontact 表中。** rcontact 里的 `@chatroom` row 是群聊本身（群名称+群ID），不是群成员列表。所以"过滤掉非好友群成员"这个需求本质上只要不加群成员到数据源就不会出现。

---

## 11.2 最简单的过滤 SQL

```sql
-- 只取好友 + 群聊
SELECT username, nickname, alias, conRemark, type, createTime,
  CASE WHEN type=4 THEN '好友'
       WHEN username LIKE '%@chatroom' THEN '群聊'
       ELSE '其他' END AS category
FROM rcontact
WHERE deleteFlag = 0
  AND (
    type = 4                            -- 好友
    OR username LIKE '%@chatroom'       -- 群聊
  )
ORDER BY
  CASE WHEN type=4 THEN 0 ELSE 1 END,    -- 好友排前面
  CASE WHEN length(conRemarkPYFull) > 0
       THEN upper(conRemarkPYFull)
       ELSE upper(quanPin) END ASC
```

### 核心过滤条件解释

| 条件 | 包含 | 排除 |
|---|---|---|
| `type = 4` | 所有好友 (13950) | 公众号(gh_)、系统号(type=33)、文件助手(type=2049) |
| `username LIKE '%@chatroom'` | 所有群聊 (68) | 陌生人(type=1)、企业微信(@openim) |
| `deleteFlag = 0` | 正常联系人 | 已删除 |

**一行代码搞定**: `WHERE deleteFlag=0 AND (type=4 OR username LIKE '%@chatroom')`

---

## 11.3 Java 方法实现

```java
/**
 * ContactSelector — 联系人选择器完整实现
 * 嵌入 WxContactManager 中
 */
public class WxContactManager {

    // ... (前面的方法不变) ...

    // ================================================================
    // 11.3.1 基础选择器: 好友+群聊
    // ================================================================

    /** 获取联系人选择器数据 (好友+群聊) */
    public List<ContactBean> getSelectableContacts(int limit) {
        return queryContacts(
            "SELECT username, nickname, alias, conRemark, type, " +
            "CASE WHEN type=4 THEN '好友' ELSE '群聊' END AS category " +
            "FROM rcontact WHERE deleteFlag = 0 " +
            "AND (type = 4 OR username LIKE '%@chatroom') " +
            "ORDER BY CASE WHEN type=4 THEN 0 ELSE 1 END, " +
            "  CASE WHEN length(conRemarkPYFull) > 0 " +
            "  THEN upper(conRemarkPYFull) ELSE upper(quanPin) END ASC " +
            "LIMIT " + limit
        );
    }

    // ================================================================
    // 11.3.2 按性别筛选
    //
    // 性别存储在 contact 表的 sex 字段:
    //   0 = 未设置 (默认)
    //   1 = 男
    //   2 = 女
    //
    // 注意: 用 LEFT JOIN，因为大部分联系人在 contact 表中没有记录
    //       COALESCE/IFNULL 处理 NULL → 0
    // ================================================================

    /**
     * 按性别筛选联系人
     * @param sex  0=全部  1=男  2=女  3=未设置(含NULL)
     */
    public List<ContactBean> getContactsBySex(int sex, int limit) {
        String sexCondition = "";
        if (sex == 1) {
            sexCondition = "AND c.sex = 1";
        } else if (sex == 2) {
            sexCondition = "AND c.sex = 2";
        } else if (sex == 3) {
            sexCondition = "AND (c.sex IS NULL OR c.sex = 0)";
        }
        // sex == 0 不加条件 (全部)

        return queryContacts(
            "SELECT r.username, r.nickname, r.alias, r.conRemark, r.type, " +
            "COALESCE(c.sex, 0) AS sex, " +
            "CASE WHEN COALESCE(c.sex,0)=1 THEN '男' " +
            "     WHEN COALESCE(c.sex,0)=2 THEN '女' ELSE '未设置' END AS sexText, " +
            "CASE WHEN r.type=4 THEN '好友' ELSE '群聊' END AS category " +
            "FROM rcontact r " +
            "LEFT JOIN contact c ON r.username = c.username " +
            "WHERE r.deleteFlag = 0 " +
            "AND (r.type = 4 OR r.username LIKE '%@chatroom') " +
            sexCondition + " " +
            "ORDER BY r.nickname LIMIT " + limit
        );
    }

    /** 只获取男性联系人 */
    public List<ContactBean> getMaleContacts(int limit) {
        return getContactsBySex(1, limit);
    }

    /** 只获取女性联系人 */
    public List<ContactBean> getFemaleContacts(int limit) {
        return getContactsBySex(2, limit);
    }

    // ================================================================
    // 11.3.3 按关键词搜索 (支持昵称/备注/微信号/wxid)
    // ================================================================

    /**
     * 搜索联系人
     * @param keyword 搜索关键词 (昵称/备注/微信号/wxid)
     */
    public List<ContactBean> searchContacts(String keyword, int limit) {
        if (keyword == null || keyword.isEmpty()) return getSelectableContacts(limit);

        String escaped = escape(keyword);
        return queryContacts(
            "SELECT username, nickname, alias, conRemark, type, " +
            "CASE WHEN type=4 THEN '好友' ELSE '群聊' END AS category " +
            "FROM rcontact WHERE deleteFlag = 0 " +
            "AND (type = 4 OR username LIKE '%@chatroom') " +
            "AND (nickname LIKE '%" + escaped + "%' " +
            "  OR conRemark LIKE '%" + escaped + "%' " +
            "  OR alias LIKE '%" + escaped + "%' " +
            "  OR pyInitial LIKE '%" + escaped.toLowerCase() + "%' " +
            "  OR quanPin LIKE '%" + escaped.toLowerCase() + "%' " +
            "  OR username LIKE '%" + escaped + "%') " +
            "ORDER BY CASE WHEN type=4 THEN 0 ELSE 1 END, " +
            "  CASE WHEN conRemark LIKE '%" + escaped + "%' THEN 0 " +
            "       WHEN nickname LIKE '%" + escaped + "%' THEN 1 " +
            "       WHEN alias LIKE '%" + escaped + "%' THEN 2 ELSE 3 END, " +
            "  nickname ASC LIMIT " + limit
        );
    }

    // ================================================================
    // 11.3.4 组合筛选: 性别 + 关键词 + 分页
    // ================================================================

    /**
     * 完整的联系人筛选器 (组合查询)
     *
     * @param sex     性别: 0=全部 1=男 2=女 3=未设置
     * @param keyword 搜索关键词 (null=不搜索)
     * @param type    类型: 0=全部 1=好友 2=群聊
     * @param offset  分页偏移
     * @param limit   每页数量
     */
    public List<ContactBean> filterContacts(int sex, String keyword,
            int type, int offset, int limit) {
        StringBuilder sql = new StringBuilder();
        sql.append("SELECT r.username, r.nickname, r.alias, r.conRemark, r.type, ")
           .append("COALESCE(c.sex,0) AS sex, ")
           .append("CASE WHEN COALESCE(c.sex,0)=1 THEN '男' ")
           .append("     WHEN COALESCE(c.sex,0)=2 THEN '女' ELSE '未设置' END AS sexText, ")
           .append("CASE WHEN r.type=4 THEN '好友' ELSE '群聊' END AS category ")
           .append("FROM rcontact r ")
           .append("LEFT JOIN contact c ON r.username = c.username ")
           .append("WHERE r.deleteFlag = 0 AND (r.type = 4 OR r.username LIKE '%@chatroom') ");

        // 类型过滤
        if (type == 1)       sql.append("AND r.type = 4 ");
        else if (type == 2)  sql.append("AND r.username LIKE '%@chatroom' ");

        // 性别过滤
        if (sex == 1)        sql.append("AND c.sex = 1 ");
        else if (sex == 2)   sql.append("AND c.sex = 2 ");
        else if (sex == 3)   sql.append("AND (c.sex IS NULL OR c.sex = 0) ");

        // 关键词搜索
        if (keyword != null && !keyword.isEmpty()) {
            String kw = escape(keyword);
            sql.append("AND (r.nickname LIKE '%").append(kw)
               .append("%' OR r.conRemark LIKE '%").append(kw)
               .append("%' OR r.alias LIKE '%").append(kw)
               .append("%' OR r.pyInitial LIKE '%").append(kw.toLowerCase())
               .append("%' OR r.quanPin LIKE '%").append(kw.toLowerCase())
               .append("%' OR r.username LIKE '%").append(kw)
               .append("%') ");
        }

        sql.append("ORDER BY r.type, r.nickname LIMIT ")
           .append(limit).append(" OFFSET ").append(offset);

        return queryContacts(sql.toString());
    }

    // ================================================================
    // 11.3.5 统计各分类数量 (用于 UI 显示 Tab 数量)
    // ================================================================

    /** 获取选择器统计 (好友数/群聊数/男/女) */
    public SelectorStats getSelectorStats() {
        SelectorStats stats = new SelectorStats();

        // 总数
        Cursor c = rawQuery(
            "SELECT count(*) FROM rcontact " +
            "WHERE deleteFlag=0 AND (type=4 OR username LIKE '%@chatroom')");
        if (c != null) { try { if (c.moveToFirst()) stats.total = c.getInt(0); }
            finally { c.close(); } }

        // 好友数
        c = rawQuery("SELECT count(*) FROM rcontact WHERE type=4 AND deleteFlag=0");
        if (c != null) { try { if (c.moveToFirst()) stats.friends = c.getInt(0); }
            finally { c.close(); } }

        // 群聊数
        c = rawQuery(
            "SELECT count(*) FROM rcontact " +
            "WHERE username LIKE '%@chatroom' AND deleteFlag=0");
        if (c != null) { try { if (c.moveToFirst()) stats.groups = c.getInt(0); }
            finally { c.close(); } }

        // 男性数 (JOIN contact)
        c = rawQuery(
            "SELECT count(*) FROM rcontact r " +
            "INNER JOIN contact c ON r.username=c.username " +
            "WHERE r.deleteFlag=0 AND (r.type=4 OR r.username LIKE '%@chatroom') " +
            "AND c.sex=1");
        if (c != null) { try { if (c.moveToFirst()) stats.males = c.getInt(0); }
            finally { c.close(); } }

        // 女性数
        c = rawQuery(
            "SELECT count(*) FROM rcontact r " +
            "INNER JOIN contact c ON r.username=c.username " +
            "WHERE r.deleteFlag=0 AND (r.type=4 OR r.username LIKE '%@chatroom') " +
            "AND c.sex=2");
        if (c != null) { try { if (c.moveToFirst()) stats.females = c.getInt(0); }
            finally { c.close(); } }

        return stats;
    }

    /** 统计信息 Bean */
    public static class SelectorStats {
        public int total, friends, groups, males, females;
        @Override
        public String toString() {
            return "共" + total + "个 (好友:" + friends +
                   " 群聊:" + groups + " 男:" + males + " 女:" + females + ")";
        }
    }
}
```

---

## 11.4 使用示例

### 11.4.1 基础联系人选择器

```java
WxContactManager mgr = new WxContactManager(cl, ctx);
mgr.open();

// 获取所有可选择联系人 (好友+群聊)
List<ContactBean> contacts = mgr.getSelectableContacts(500);
for (ContactBean b : contacts) {
    Log.i("Picker", "[" + b.category + "] " + mgr.getDisplayName(b)
        + " (" + b.username + ")");
}
```

### 11.4.2 按性别筛选

```java
// 只显示男性联系人
List<ContactBean> males = mgr.getMaleContacts(200);
Log.i("Picker", "男性: " + males.size() + "人");

// 只显示女性联系人
List<ContactBean> females = mgr.getFemaleContacts(200);
Log.i("Picker", "女性: " + females.size() + "人");

// 未设置性别的
List<ContactBean> unknown = mgr.getContactsBySex(3, 200);
Log.i("Picker", "未设置: " + unknown.size() + "人");
```

### 11.4.3 搜索联系人

```java
// 搜索含"张"的联系人
List<ContactBean> results = mgr.searchContacts("张", 50);
for (ContactBean b : results) {
    Log.i("Search", b.displayName() + " [" + b.category + "]");
}
```

### 11.4.4 复合筛选 (带分页)

```java
// 第1页: 女性 + 好友 + 每页20条
List<ContactBean> page1 = mgr.filterContacts(
    2,      // sex: 女
    null,   // keyword: 不搜索
    1,      // type: 好友
    0,      // offset
    20      // limit
);

// 第2页
List<ContactBean> page2 = mgr.filterContacts(2, null, 1, 20, 20);
```

### 11.4.5 显示统计信息

```java
SelectorStats stats = mgr.getSelectorStats();
Log.i("Stats", stats.toString());
// 输出: 共14018个 (好友:13950 群聊:68 男:xxx 女:xxx)
```

---

## 11.5 数据流图

```
┌─────────────────────────────────────────────────┐
│            rcontact 表 (13773行)                  │
│                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 好友      │  │ 群聊      │  │ 公众号     │       │
│  │ type=4    │  │ @chatroom│  │ gh_       │       │
│  │ 13950行   │  │ 68行      │  │ 12行       │      │
│  └─────┬────┘  └─────┬────┘  └─────┬─────┘       │
│        │             │              │ (排除)       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 系统号    │  │ 文件助手  │  │ 陌生人    │        │
│  │ type=33   │  │ type=2049│  │ type=1    │        │
│  │ 24行      │  │ 1行       │  │ 1行       │        │
│  └─────┬────┘  └─────┬────┘  └─────┬─────┘       │
│        │             │              │              │
│      (排除)        (排除)         (排除)           │
│                                                   │
│  ┌─────────────────────────────────────────┐      │
│  │         WHERE (type=4                    │      │
│  │          OR username LIKE '%@chatroom') │      │
│  │         AND deleteFlag=0                │      │
│  └───────────────────┬─────────────────────┘      │
│                      │                              │
│               ┌──────┴──────┐                       │
│               │  选择器数据   │                       │
│               │  好友+群聊    │                       │
│               │  ~14018行    │                       │
│               └──────┬──────┘                       │
│                      │                              │
│            ┌─────────┴─────────┐                     │
│            │   JOIN contact    │                     │
│            │   获取 sex 字段    │                     │
│            └─────────┬────────┘                     │
│                      │                              │
│        ┌─────────────┼─────────────┐                │
│        │             │             │                │
│   sex=1(男)     sex=2(女)    sex=0/NULL(未设置)     │
└─────────────────────────────────────────────────┘
```

---

## 11.6 关键点总结

| 问题 | 答案 |
|---|---|
| **如何过滤掉非好友/群聊？** | `WHERE (type=4 OR username LIKE '%@chatroom')` — 一行条件搞定所有排除 |
| **群成员会出现在选择器吗？** | 不会。群成员不在 rcontact 表中，不需要额外过滤 |
| **性别数据在哪？** | `contact` 旧表的 `sex` 字段 (0=未设置 1=男 2=女) |
| **为什么大部分性别为空？** | 微信默认不设置性别，需要用户手动修改 |
| **怎么 JOIN contact 表？** | `LEFT JOIN contact c ON r.username = c.username` — 用 LEFT JOIN 因为 contact 表不一定有对应记录 |
| **搜索支持什么字段？** | 昵称/备注/微信号/拼音/wxid — 全部 LIKE 匹配 |
| **排序规则？** | 备注匹配 > 昵称匹配 > 微信号匹配，好友排在群聊前面 |
| **性能如何？** | 14018行数据，单次查询 < 50ms，分页后更快速 |

---

> **第十一章完** | 联系人选择器完整实现 | 含性别筛选、关键词搜索、分页、统计

---

# 第十二章：群成员 vs 群聊 vs 好友 — 彻底澄清与精准过滤

> 核心问题: "群成员数据是不是弄反了？过滤群成员不干净，有时候把好友都过滤掉了"
> 根因: 对 rcontact 表数据本质的误解 + type 位掩码陷阱

---

## 12.1 根本问题：你以为的 vs 实际存储的

### 你以为的 rcontact

```
rcontact 包含:
  ├── 好友 A
  ├── 好友 B
  ├── 群聊X 的成员张三     ← 你以为有这个
  ├── 群聊X 的成员李四     ← 你以为有这个
  ├── 群聊X 的成员王五     ← 你以为有这个
  └── 群聊X 本身           ← 你以为有这个
```

### ★ 实际的 rcontact (8.0.76 实测)

```
rcontact 包含:
  ├── 好友 A (type=4, username=wxid_aaa)
  ├── 好友 B (type=4, username=wxid_bbb)
  ├── 群聊X (type=3, username=22110637730@chatroom, nickname="化州车大炮群")
  ├── 群聊Y (type=3, username=34831702152@chatroom, nickname="水产海鲜群")
  └── 群聊Z (type=2, username=11021177242@chatroom, nickname="顺风车群")

  群成员张三/李四/王五 — ★ 不存在于 rcontact 表中 ★
```

### 关键结论

| 概念 | 在 rcontact 中？ | 标识 |
|---|---|---|
| **好友(个人)** | ✅ 存在 | type=4, username=wxid_xxx |
| **群聊(群本身)** | ✅ 存在 | type=3/2/2050/2051, username=数字@chatroom |
| **群成员(群里的人)** | ❌ 不存在 | 张三在群X中，但 rcontact 里只有张三的 type=4 好友记录 |
| **公众号** | ✅ 存在 | gh_ 前缀 |
| **系统号** | ✅ 存在 | type=33 |

---

## 12.2 为什么你"过滤群成员不干净"

### 常见错误1: 把 @chatroom 当成了"群成员"

```sql
-- ❌ 错误做法: 想排除群成员，结果排除了群聊本身
WHERE username NOT LIKE '%@chatroom'
```

这个过滤会把所有群聊（@chatroom 结尾的行）从结果中去掉。这些是群聊实体（群名称+群ID），不是群成员。

**后果**: 群聊从列表消失，但"群成员"根本没出现过，所以感觉"过滤不干净"。

### 常见错误2: 使用旧的位掩码过滤

```sql
-- ❌ 错误: 8.0.76 中 type & 64 永远为0
WHERE type & 64 != 0   -- 返回 0 行!

-- ❌ 错误: 8.0.76 中 type & 32 永远为0
WHERE type & 32 = 0    -- 什么也排除不了!

-- ❌ 错误: 以为 type=4 是"正常好友"，其他是"群成员"
WHERE type = 4         -- 这会排除所有群聊!
```

### 常见错误3: 误用 NOT LIKE 把好友也排除了

```sql
-- ❌ 危险: 如果有好友的 username 恰好包含 @chatroom 子串?
WHERE username NOT LIKE '%@chatroom'  -- 实际上不会误伤好友，但意图不对
```

实际上好友的 username 都是 `wxid_` 开头或自定义微信号，不会包含 `@chatroom`。但这个过滤表达的是"排除群聊"，不是"排除群成员"。

---

## 12.3 WeChat 内部如何过滤 (来自 j4.I() 源码)

微信自己的 `ContactStorage` 中，`I()` 方法使用以下内置分类:

```java
// @all.contact.android — 所有普通联系人 (排除群聊)
"where (type & 1!=0) and type & 32=0"

// @all.chatroom.contact — 所有群聊
"where (type & 1!=0) AND (username LIKE '@chatroom' OR '@im.chatroom') AND type & 32=0"

// @all.contact.without.chatroom — 排除群聊的联系人
// 用 t(false, false) + NOT username LIKE '@chatroom' 组合
```

**但注意**: 这些是旧版位掩码逻辑。在 8.0.76 中，`type` 列存储的是小整数 (1,2,3,4,33...)，**位掩码不再有效**。

8.0.76 实测:
```sql
-- 测试: 微信代码中的位掩码
SELECT count(*) FROM rcontact WHERE type & 32 != 0;  -- 结果: 0
SELECT count(*) FROM rcontact WHERE type & 64 != 0;  -- 结果: 0
SELECT count(*) FROM rcontact WHERE type & 1 != 0;   -- 结果: ? (type=1,3,5,7等)

-- 正确的方式 (8.0.76):
SELECT count(*) FROM rcontact WHERE type = 4;        -- 结果: 13950 (好友)
SELECT count(*) FROM rcontact WHERE username LIKE '%@chatroom'; -- 结果: 68 (群聊)
```

---

## 12.4 ★ 正确过滤方案

### 联系人选择器需要的数据

| 你要的 | SQL 条件 | 数量 |
|---|---|---|
| 好友 (个人联系人) | `type = 4` | 13950 |
| 群聊 (可发消息的目标) | `username LIKE '%@chatroom'` | 68 |

```sql
-- ★ 正确精确过滤 (一行搞定)
SELECT * FROM rcontact
WHERE deleteFlag = 0
  AND (type = 4 OR username LIKE '%@chatroom')
ORDER BY
  CASE WHEN type = 4 THEN 0 ELSE 1 END,
  CASE WHEN length(conRemarkPYFull) > 0
       THEN upper(conRemarkPYFull) ELSE upper(quanPin) END ASC
```

### 为什么不需要过滤"群成员"

```
群成员 = 群里的人 = 他也是某人的好友
         = 他在 rcontact 中的记录 type=4 (和他的群归属无关)

你不能通过 rcontact 判断"这个人是不是在某个群里"，
因为 rcontact 没有"群归属"字段。

如果你要显示"群X的成员列表"，
需要从 message 表或网络请求获取，不能用 rcontact 过滤。
```

### 完整的数据流

```
你的联系人选择器数据源:
  ┌──────────────────────────────────────┐
  │ rcontact WHERE type=4                │
  │ 13950 个好友                          │
  │ (其中包含了所有群里的所有成员)           │
  │ (但没有字段标记他们在哪个群)             │
  ├──────────────────────────────────────┤
  │ rcontact WHERE username LIKE         │
  │ '%@chatroom'                         │
  │ 68 个群聊 (可以发消息的群实体)          │
  └──────────────────────────────────────┘
  总计: 14018 个可选联系对象

  不在数据源中的:
  ❌ 群成员专属记录 — 不存在
  ❌ 公众号 — 已被 type=4 条件排除
  ❌ 系统号 — 已被 type=4 条件排除
  ❌ 陌生人 — 已被 type=4 条件排除
```

---

## 12.5 诊断: 检查你的过滤器哪里有问题

```sql
-- 步骤1: 看 rcontact 全部 type 分布
SELECT type, count(*)
FROM rcontact
WHERE deleteFlag = 0
GROUP BY type
ORDER BY count(*) DESC;

-- 步骤2: 看你当前的过滤 SQL 返回了多少行
-- 把你自己写的 SQL 放这里，看看实际返回什么
SELECT count(*) FROM rcontact WHERE deleteFlag=0 AND (你的条件);

-- 步骤3: 对比正确结果
SELECT count(*) FROM rcontact
WHERE deleteFlag = 0 AND (type = 4 OR username LIKE '%@chatroom');
-- 应该: ~14018 行

-- 步骤4: 抽样检查被排除的行
SELECT username, nickname, type
FROM rcontact WHERE deleteFlag = 0
  AND NOT (type = 4 OR username LIKE '%@chatroom')
LIMIT 30;
-- 这应该显示公众号、系统号等被正确排除的行
```

### 常见错误修正

| 你的代码可能是 | 问题 | 正确写法 |
|---|---|---|
| `type & 64 != 0` | 位掩码无效 | `type = 4` |
| `NOT LIKE '%@chatroom'` | 排除了群聊 | 不需要这行 |
| `type NOT IN (3,33)` | 排除了群聊 | 用正向条件 `type=4 OR @chatroom` |
| `type = 4 AND NOT LIKE '@chatroom'` | 无意义 | `type=4` 本身就不含 @chatroom |

---

## 12.6 最终正确 Java 方法

```java
/**
 * ★ 联系人选择器 — 正确过滤 (不依赖位掩码)
 *
 * 数据来源: rcontact
 * 过滤逻辑:
 *   - type=4 → 所有好友 (无论是否在群里)
 *   - username LIKE '%@chatroom' → 所有群聊实体
 *   - 自动排除: 公众号/系统号/陌生人/文件助手
 *
 * 不需要额外过滤"群成员" — 因为 rcontact 里根本没有群成员专属记录
 */
public List<ContactBean> getSelectableContacts(int limit) {
    return queryContacts(
        "SELECT username, nickname, alias, conRemark, type, " +
        "CASE WHEN type=4 THEN '好友' ELSE '群聊' END AS category " +
        "FROM rcontact " +
        "WHERE deleteFlag = 0 " +
        "  AND (type = 4 OR username LIKE '%@chatroom') " +
        "ORDER BY " +
        "  CASE WHEN type=4 THEN 0 ELSE 1 END, " +
        "  CASE WHEN length(conRemarkPYFull) > 0 " +
        "    THEN upper(conRemarkPYFull) " +
        "    ELSE upper(quanPin) END ASC " +
        "LIMIT " + limit
    );
}
```

---

## 12.7 总结

| 问题 | 答案 |
|---|---|
| **群成员在 rcontact 里吗？** | ❌ 不在。群成员就是普通好友(type=4)，没有群归属标记 |
| **@chatroom 行是什么？** | 群聊本身（群实体），不是群成员 |
| **怎么排除群成员？** | 不需要排除。根本不在 rcontact 里 |
| **type & 64 为什么无效？** | 8.0.76 的 type 是小整数，不是位掩码 |
| **为什么过滤"不干净"？** | 你可能把 @chatroom 群聊当成了群成员，或者用了无效的位掩码 |
| **正确过滤一行代码** | `WHERE (type = 4 OR username LIKE '%@chatroom')` |

---

> **第十二章完** | 群成员/群聊/好友概念澄清 | type位掩码陷阱 | 正确过滤方案


# 第十三章：type=4 不是"好友"！— 颠覆性修正

> ★ 重大修正: 之前说 type=4=好友 是**错误的**
> 正确结论来自 WeChat 源码 j4.t() 的位掩码逻辑

---

## 13.1 问题根源

用户反馈: "我实质是51个好友，怎么你分析出13950个？"

**答案: 因为 `type=4` 根本不是 WeChat 定义的好友。**

rcontact 表 13950 行 type=4 是微信存储的**所有见过的人**:
- 群里说过话的
- 附近的人/摇一摇
- 手机通讯录匹配但未添加的
- 暂时会话
- 朋友圈互动过的
- ...等等

---

## 13.2 WeChat 内部真正的"好友"定义

来自 `j4.t()` 方法 (已逐行 smali 验证):

```java
// ★ WeChat 内部好友过滤的核心代码
public static String t(boolean z, boolean z2) {
    e0 e0Var = s.Q2;
    String str = (" where (" +
        (z ? "type & 1!=0 or type & 16!=0" : "type & 1!=0") +
        ")") +
        " and type & 32=0 " +
        " and type & 8 =0 ";
    if (!z2) {
        r2 r2Var = y3.W2;
        str = str + " and verifyFlag & 8 =0 ";
    }
    return str + " and deleteFlag = 0";
}

// t(false, false) 的结果:
// where (type & 1 != 0)        ← ★ 核心: type 最低位必须是1
//   and type & 32 = 0           ← 不能有 bit5
//   and type & 8 = 0            ← 不能有 bit3
//   and verifyFlag & 8 = 0      ← 不能有 bit3
//   and deleteFlag = 0
```

### 关键解读

| 条件 | type=4 (100) | type=1 (001) | type=3 (011) | type=65 (1000001) |
|---|---|---|---|---|
| `type & 1 != 0` | ❌ 失败 (=0) | ✅ 通过 | ✅ 通过 | ✅ 通过 |
| `type & 32 = 0` | ✅ 通过 | ✅ 通过 | ✅ 通过 | ❌ 失败 |
| `type & 8 = 0` | ✅ 通过 | ✅ 通过 | ✅ 通过 | ✅ 通过 |
| **结果** | **不是好友** | 好友 | 好友 | 不是好友 |

**type=4 的二进制是 `100`，最低位是 0，所以 `type & 1 = 0`，直接被排除！**

---

## 13.3 type 值的真实含义 (位掩码解读)

WeChat 的 type 是**位掩码**，不同 bit 代表不同属性:

| bit | 值 | 含义 |
|---|---|---|
| 0 | 1 | **是联系人** (好友必须有) |
| 1 | 2 | 未知 |
| 2 | 4 | **非好友/临时联系人** (type=4 的含义!) |
| 3 | 8 | 可能是黑名单/屏蔽 |
| 4 | 16 | 可能是"包含好友"(t(true)时用) |
| 5 | 32 | 可能是"公众号/服务号" |
| 6 | 64 | 可能是另一个标志 |

### 你实际看到的 type 值

| type | 二进制 | 含义 |
|---|---|---|
| **你的好友** | bit0=1, bit5=0, bit3=0 | `&1!=0 && &32=0 && &8=0` |
| **type=1** | 001 | 最低位=1 → 好友候选 (陌生人) |
| **type=3** | 011 | 最低位=1, bit1=1 → 群聊中的联系人 |
| **type=4** | 100 | bit2=1 → **临时/非好友** ★ |
| **type=5** | 101 | bit0=1, bit2=1 → 既是好友又标记了什么 |
| **type=33** | 100001 | bit0=1, bit5=1 → 公众号(排除) |
| **type=65** | 1000001 | bit0=1, bit6=1 → 特殊标志 |

---

## 13.4 正确的好友查询

```sql
-- ★ 正确 (WeChat 原生逻辑)
SELECT username, nickname, alias, conRemark, type, createTime
FROM rcontact
WHERE (type & 1 != 0)           -- ★ 最低位是1 (是联系人)
  AND (type & 32 = 0)            -- 不是公众号
  AND (type & 8 = 0)             -- 不是黑名单
  AND (verifyFlag & 8 = 0)       -- 不是企业
  AND deleteFlag = 0
  AND username NOT LIKE '%@chatroom'  -- 排除群聊实体
ORDER BY nickname
```

这个查询的返回行数应该接近你的 51 个好友。

### 简单验证

```sql
-- 看 type 分布 (用位掩码视角)
SELECT 
  type,
  CASE WHEN (type & 1 != 0) THEN '√联系人' ELSE '×非联系人' END AS isContact,
  CASE WHEN (type & 32 != 0) THEN '√公众号' ELSE '×非公众号' END AS isBiz,
  count(*) AS cnt
FROM rcontact
WHERE deleteFlag = 0
GROUP BY type
ORDER BY cnt DESC
```

---

## 13.5 type=4 是什么？

```
type=4 (二进制 100, 十进制 4)

bit2=1 的含义: 可能是 "temporary contact" (临时联系人)

这些是你见过的但没加好友的人:
- 群聊中说过话但不是你好友的人
- 附近的人
- 摇一摇
- 手机通讯录匹配但未添加
- 短暂会话
```

---

## 13.6 联系人性别的正确获取

```sql
-- ★ 正确 (用 WeChat 原生好友条件 + JOIN contact 取性别)
SELECT r.username, r.nickname, r.conRemark, r.type,
  COALESCE(c.sex, 0) AS sex,
  CASE COALESCE(c.sex, 0)
    WHEN 1 THEN '男'
    WHEN 2 THEN '女'
    ELSE '未设置'
  END AS sexText
FROM rcontact r
LEFT JOIN contact c ON r.username = c.username
WHERE (r.type & 1 != 0)
  AND (r.type & 32 = 0)
  AND (r.type & 8 = 0)
  AND (r.verifyFlag & 8 = 0)
  AND r.deleteFlag = 0
  AND r.username NOT LIKE '%@chatroom'
  AND c.sex = 1                -- ★ 筛选男性
ORDER BY r.nickname
```

---

## 13.7 联系人选择器修正版

```java
// ★ 正确的好友过滤 (基于 WeChat 源码)
private static final String FRIEND_FILTER =
    "(type & 1 != 0) " +           // ★ 是联系人
    "AND (type & 32 = 0) " +        // 不是公众号
    "AND (type & 8 = 0) " +         // 不是黑名单
    "AND (verifyFlag & 8 = 0) " +   // 不是企业
    "AND deleteFlag = 0 " +         // 未删除
    "AND username NOT LIKE '%@chatroom'"; // 不是群聊实体

/** ★ 修正版: 获取好友列表 */
public List<ContactBean> getFriends(int limit) {
    return queryContacts(
        "SELECT r.username, r.nickname, r.alias, r.conRemark, r.type, " +
        "COALESCE(c.sex,0) AS sex, " +
        "CASE WHEN COALESCE(c.sex,0)=1 THEN '男' " +
        "     WHEN COALESCE(c.sex,0)=2 THEN '女' ELSE '未设置' END AS sexText " +
        "FROM rcontact r " +
        "LEFT JOIN contact c ON r.username = c.username " +
        "WHERE " + FRIEND_FILTER + " " +
        "ORDER BY r.conRemarkPYFull, r.quanPin " +
        "LIMIT " + limit
    );
}

/** ★ 修正版: 联系人选择器 (好友+群聊) */
public List<ContactBean> getSelectableContacts(int limit) {
    return queryContacts(
        "SELECT username, nickname, alias, conRemark, type, " +
        "CASE WHEN username LIKE '%@chatroom' THEN '群聊' ELSE '好友' END AS category " +
        "FROM rcontact WHERE deleteFlag = 0 AND (" +
        "  (username LIKE '%@chatroom') " +           // 群聊
        "  OR (" + FRIEND_FILTER + ")" +             // ★ 好友
        ") ORDER BY CASE WHEN username LIKE '%@chatroom' THEN 1 ELSE 0 END, nickname " +
        "LIMIT " + limit
    );
}

/** ★ 修正版: 按性别筛选好友 */
public List<ContactBean> getFriendsBySex(int sex, int limit) {
    String sexFilter = "";
    if (sex == 1) sexFilter = "AND c.sex = 1";
    else if (sex == 2) sexFilter = "AND c.sex = 2";

    return queryContacts(
        "SELECT r.username, r.nickname, r.alias, r.conRemark, " +
        "c.sex, " +
        "CASE WHEN c.sex=1 THEN '男' WHEN c.sex=2 THEN '女' ELSE '未设置' END AS sexText " +
        "FROM rcontact r " +
        "INNER JOIN contact c ON r.username = c.username " +  // ★ INNER JOIN
        "WHERE " + FRIEND_FILTER + " " +
        sexFilter + " " +
        "ORDER BY r.nickname LIMIT " + limit
    );
}
```

---

## 13.8 之前的错误总结

| 之前 (错误) | 现在 (正确) | 原因 |
|---|---|---|
| `type=4 是好友` | `type & 1 != 0 才是好友` | type=4 二进制 100, 最低位是0 |
| 13950个好友 | ~51个好友 + 13900临时联系人 | type=4被误认为是好友 |
| 按 sex 列筛选 | **contact**表 sex 字段 | rcontact没有sex字段 |
| 位掩码失效 | 位掩码有效 | 之前理解错了用法 |

---

## 13.9 快速自查

```sql
-- 第1步: 看有多少行 type&1=0 (非好友)
SELECT count(*) FROM rcontact WHERE deleteFlag=0 AND (type & 1 = 0);
-- 应该 ~13950 (这就是那些 type=4 的)

-- 第2步: 看真正的联系人有多少
SELECT count(*) FROM rcontact
WHERE deleteFlag=0
  AND (type & 1 != 0)
  AND (type & 32 = 0)
  AND (type & 8 = 0)
  AND username NOT LIKE '%@chatroom';
-- 应该 ~51 (你的真正好友数)

-- 第3步: 看真正的联系人的 type 分布
SELECT type, count(*) FROM rcontact
WHERE deleteFlag=0 AND (type & 1 != 0) AND (type & 32 = 0) AND (type & 8 = 0)
  AND username NOT LIKE '%@chatroom'
GROUP BY type ORDER BY count(*) DESC;
-- 这能告诉你好友实际是 type=几
```

---

> **第十三章完** | type=4 不是好友 | WeChat 位掩码真相 | 正确过滤+性别筛选
