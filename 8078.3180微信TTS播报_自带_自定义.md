分析完成。以下是微信 8.0.78 自带 TTS 完整链路 与 自定义接入方案 的深度逆向报告（所有类名/方法名均经 smali 验证）。
一、微信自带 TTS 完整链路（8.0.78）
1. 功能定位
微信自带的是 "聊天记录朗读"（ ChatRecordsTts ），需要手动触发（聊天界面悬浮球），按时间顺序连续朗读整个会话。它没有"来消息自动播报"的监听（仅有  revokeMsgListener / deleteMsgListener  处理朗读过程中的撤回/删除）。你的需求必须自定义 hook。
2. 完整调用链（混淆名已验证）
UI 触发（ChatRecordsTtsIndicatorComponent 悬浮球）
 └─> b1 (ChatRecordsTtsService 混淆)           [d1.get() 单例]
      ├─ lj(b1, Continuation)  processMessages  主循环：按时间顺序连续朗读队列消息
      ├─ pj(e9, q, e9, Boolean) generateReadableMessage  消息→可朗读文本（548行 switch 全类型）
      ├─ r1.a(Context, e9)     静态工具：纯文本提取 + XML标签正则清洗
      ├─ sj(talker, createTime) 从 DB 拉消息（"Select * From ... createTime > ?"）
      └─ qj() → fv4.z (TTS引擎, 接口 qv.f1 = TextToSpeechService)
             ├─ lj(text, Z, a, Continuation)  readTextAndWait  文本→在线合成
             ├─ mj(e9, Z, ...)  readVoiceMessageAndWait  语音消息→下载→播放
             ├─ jj(z, Context, path, ...)  playVoiceFile  播放本地音频
             ├─ kj() → fv4.p (TextToSpeechHelper)
             │     └─ F7(text, time, fv4.n回调, audioFocus, pc5.v34 scene, pc5.u34 language)
             │           └─ fv4.q0 TTS任务(在线合成) → x61.h 状态机 → v61.j1 MediaPlayer封装播放
             └─ stop()
3. 关键类/方法签名（smali 级验证）
 com.tencent.mm.feature.chatrecordstts.b1 
主服务
混淆= ChatRecordsTtsService ，单例  d1.get() 
朗读核心
〃
 lj 
 (Lb1;Lkotlin/coroutines/Continuation;)Object 
processMessages 主循环
〃
 pj 
 (Le9;Ld96/q;Le9;Ljava/lang/Boolean;)Lcom/tencent/mm/feature/chatrecordstts/m; 
消息→可朗读文本
〃
 qj 
 ()Lqv/f1; 
获取 TTS 引擎
〃
 sj 
 (Ljava/lang/String;J)Ljava/util/List; 
DB 拉取消息
 com.tencent.mm.feature.chatrecordstts.r1 
 a 
 (Landroid/content/Context;Le9;)Ljava/lang/String; 
纯文本提取（可复用）
 com.tencent.mm.feature.chatrecordstts.fv4.z 
TTS实现
 extends jp0.o implements qv.f1 
实际引擎
〃
 lj 
 (Ljava/lang/String;ZLr96/a;Lkotlin/coroutines/Continuation;)Object 
readTextAndWait 播放文本
〃
 jj 
静态  (Lfv4/z;Landroid/content/Context;Ljava/lang/String;Lkotlinx/coroutines/q;Ljava/util/concurrent/atomic/AtomicBoolean;Z)V 
playVoiceFile
 com.tencent.mm.feature.chatrecordstts.fv4.p 
 F7 
 (Ljava/lang/CharSequence;JLfv4/n;ZLpc5/v34;Lpc5/u34;)V 
播放管理
 ph5.n0 
 c 
 (Ljava/lang/Class;)Lph5/m; 
服务定位器（ calling getService ）
 com.tencent.mm.sdk.platformtools.a3 
 a 
静态字段  Landroid/content/Context; 
微信全局 Context
4. 关键结论（与自定义的取舍）
•  
微信 TTS 引擎是"在线合成"（腾讯云类云端 TTS）： fv4.q0  任务把文本发云端 → 合成音频 → 下载 →  v61.j1 (MediaPlayer 封装) 播放，缓存目录  imSpeech 。不是 Android 系统  TextToSpeech 。
•  
微信自带的文本生成逻辑（ b1.pj()  +  r1.a() ）非常完善，覆盖文本/图片/视频/语音/表情/位置/名片/链接/小程序/公众号/合并记录等，且可复用。
•  
微信自带 TTS 无法直接满足"来消息自动播报"（无新消息监听），必须自己 hook 消息入口。
二、自定义接入方式（两个方案）
方案 A（推荐）：系统 TextToSpeech（离线、稳定、可控）
唯一需要 hook 的入口：
java
XposedHelpers.findAndHookMethod("com.tencent.mm.storage.f9", cl, "Bb", 
    "com.tencent.mm.storage.e9", boolean.class, hook);
// 对应 MsgInfoStorage.insertMsgInfo(MsgInfo, boolean)
消息实体方法映射（已在第一节逐条 smali 验证）：
java
int    type     = (int)    callMethod(msg, "getType");     // field_type
int    isSend   = (int)    callMethod(msg, "z0");          // field_isSend, 0=收 1=发
long   svrId    = (long)   callMethod(msg, "F0");          // field_msgSvrId
long   create   = (long)   callMethod(msg, "getCreateTime");
long   msgId    = (long)   callMethod(msg, "getMsgId");
String talker   = (String) callMethod(msg, "N0");          // field_talker
String content  = (String) callMethod(msg, "j");           // getContent 文本
boolean isText  = (boolean) callMethod(msg, "Z2");         // type∈{1,11,21,31,36}
boolean isImage = (boolean) callMethod(msg, "J2");         // type∈{3,13,23,33,39}
boolean isVideo = (boolean) callMethod(msg, "isVideo");    // type==43
完整 Java Xposed 模块：
java
package com.your.module.tts;

import android.content.Context;
import android.os.Handler;
import android.os.Looper;
import android.speech.tts.TextToSpeech;
import android.text.TextUtils;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Locale;
import java.util.Set;
import java.util.regex.Pattern;
import de.robv.android.xposed.*;
import de.robv.android.xposed.callbacks.XC_LoadPackage;

public class WeChatTTS implements IXposedHookLoadPackage {

    // 8.0.78 混淆常量（已smali验证）
    static final String CLS_MSGINFO = "com.tencent.mm.storage.e9"; // MsgInfo
    static final String CLS_STORAGE = "com.tencent.mm.storage.f9"; // MsgInfoStorage
    static final String CLS_CTX     = "com.tencent.mm.sdk.platformtools.a3"; // MMApplicationContext

    private final Handler main = new Handler(Looper.getMainLooper());
    private TextToSpeech tts;
    private Context appCtx;
    private final Set<Long> recentSvrId = new HashSet<>();
    private static final Pattern XML_TAG = Pattern.compile("<[^>]*>");

    @Override
    public void handleLoadPackage(XC_LoadPackage.LoadPackageParam lp) {
        if (!"com.tencent.mm".equals(lp.packageName)) return;
        if (!"com.tencent.mm".equals(lp.processName)) return; // 仅主进程
        try {
            XposedHelpers.findAndHookMethod(CLS_STORAGE, lp.classLoader, "Bb",
                    CLS_MSGINFO, boolean.class, new XC_MethodHook() {
                @Override protected void beforeHookedMethod(MethodHookParam p) {
                    try { onNewMsg(p.args[0]); } catch (Throwable t) { XposedBridge.log(t); }
                }
            });
        } catch (Throwable t) { XposedBridge.log("[TTS] hook err " + t); }
    }

    void onNewMsg(Object msg) throws Throwable {
        int isSend = (int) XposedHelpers.callMethod(msg, "z0");
        if (isSend != 0) return;                            // 只播收到的

        long svrId = (long) XposedHelpers.callMethod(msg, "F0");
        long create = (long) XposedHelpers.callMethod(msg, "getCreateTime");
        if (Math.abs(System.currentTimeMillis() - create) > 10_000) return; // 排除历史同步
        if (!recentSvrId.add(svrId)) return;                // 去重

        int type = (int) XposedHelpers.callMethod(msg, "getType");
        String text;
        if (type == 1) {                                    // 文本
            text = (String) XposedHelpers.callMethod(msg, "j");
            text = XML_TAG.matcher(text == null ? "" : text).replaceAll("");
            if (TextUtils.isEmpty(text.trim())) return;
        } else if (type == 3) {                             // 图片
            text = "收到一张图片";
        } else if (type == 43) {                            // 视频
            text = "收到一条视频";
        } else {
            return;
        }
        speak(text);
    }

    void speak(final String text) {
        main.post(() -> {
            if (tts == null) initTts();
            if (tts != null) tts.speak(text, TextToSpeech.QUEUE_FLUSH, null, "mm_tts");
        });
    }

    void initTts() {
        try {
            appCtx = (Context) XposedHelpers.getStaticObjectField(CLS_CTX, "a");
        } catch (Throwable ignored) {}
        if (appCtx == null) return;
        tts = new TextToSpeech(appCtx, s -> {
            if (s == TextToSpeech.SUCCESS) tts.setLanguage(Locale.CHINESE);
        });
    }
}
方案 B：复用微信自带 TTS 引擎（在线合成、效果好，但有依赖）
两个复用层次，都在你 hook 到  e9  后调用：
(1) 复用微信"消息→朗读文本"逻辑（推荐： r1.a  是 public static）：
java
// 等价于微信朗读时的完整文本生成（含 XML 清洗）
Class<?> r1 = XposedHelpers.findClass("com.tencent.mm.feature.chatrecordstts.r1", cl);
String speechText = (String) XposedHelpers.callStaticMethod(r1, "a", appCtx, msg);
(2) 直接调用微信 TTS 引擎播放（ fv4.z.lj  = readTextAndWait，在线合成）：
java
// ph5.n0.c(qv.f1.class) → TextToSpeechService 实例
Class<?> n0 = XposedHelpers.findClass("ph5.n0", cl);
Object ttsService = XposedHelpers.callStaticMethod(n0, "c",
        XposedHelpers.findClass("qv.f1", cl));      // 返回 fv4.z 实例
// fv4.z.lj(String text, boolean z, r96.a callback, Continuation) — z=true 表示需要音频焦点
XposedHelpers.callMethod(ttsService, "lj", speechText, true, null, null);
方案 B 注意： ph5.n0.c  依赖微信服务定位器已初始化（主进程、登录后）； lj  是协程挂起函数，最后一个参数传 `` 会导致同步等待——建议  speechText  由主线程传入、TTS 合成在微信内部协程执行，实际表现为"调用后异步播放"，无阻塞风险；但若引擎未就绪会静默失败。稳定性依赖版本，仅作增强选项。
三、方案对比总结
依赖
Android 系统 TTS
仅 r1.a 静态方法
微信服务定位器+在线合成
网络
离线
离线
需网络
音质
取决于系统引擎
同左
腾讯云级，最佳
稳定性
高
高
中（依赖内部服务）
实现复杂度
低
低
中
推荐
✅ 主方案
✅ 叠加使用
可选增强
推荐组合：方案 A 为主（hook  f9.Bb  + 系统 TextToSpeech），文本生成可叠加复用微信  r1.a （记得用  z0()==0  +  createTime  近 10s +  msgSvrId  去重三重过滤，仅主进程 hook）。
需要我进一步：① 把  b1.pj() （generateReadableMessage）中 49 类 App 消息/引用消息 的完整类型分支提取成表格？② 分析微信 TTS 的音频焦点/打断策略（ x61.h  状态机）供你自定义播报队列参考？③ 验证  d1.get()  单例入口，让方案 B 更稳定？