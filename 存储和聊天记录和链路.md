链路全部确认完毕。下面直接给可用的答案。
一、读取群聊/联系人聊天记录（f9 查询方法）
通过  ((c4) j1.v(c4.class)).lj()  拿到 f9（MsgInfoStorage）后：
读某会话全部记录
 f9.G2(talker)  →  List<e9> 
 select * from <talker> ，全量
分页倒序读
 f9.H2(talker, createTimeBefore, limit) 
 createTime < 某值 Order By createTime Desc Limit N 
按时间范围读
 f9.D3(talker, createTimeFrom, createTimeTo) 
 createTime>from AND <=to Desc 
单条（按 svrId）
 f9.O3(talker, svrId) 
接收防重也用这个
单条（按 msgId）
 f9.N3(talker, msgId)  /  f9.P8(msgId) 

单条（按 createTime）
 f9.J3(talker, createTime) 

最后一条 createTime
 f9.I9(talker)  →  int 

读出来的  e9  用字段 getter 拿内容： N0() =talker、 j() =content、 getType() 、 z0() =isSend、 F0() =svrId、 getCreateTime() 。
二、文字消息存储链路
接收（别人发来的文字）：
a2.b (MessageSyncExtension.dkAddMsg) → b41.k.k → aa.y(a) → f9.yb(e9) → f9.Bb(e9, false)
（ b41.aa.y  里自动建会话， f9.yb  失败上报， Bb(false)  普通 INSERT。）
发送（自己发出的文字）：
构造 e9(type=1) → f9.Bb(e9, true) 或 f9.yb(e9) [本地插入/回显]
→ new v51.r0(...) (NetSceneSendMsg) → r1.h(scene, 0) 入队
→ GET newsendmsg CGI (/cgi-bin/micromsg-bin/newsendmsg, cmd=522)
三、发送文字的标准调用（照着写就能用）
java
// 1. 构造文本消息
e9 msg = new e9();
msg.u1(talker);                              // 目标会话（群/联系人）
msg.b1(content);                             // 文本内容 → e9.j()
msg.setType(1);                              // 文本类型
msg.e1(System.currentTimeMillis());          // createTime（毫秒）
msg.k1(1);                                   // ★ isSend = 1（自己发出）
msg.t1(1);                                   // ★ status = 1（发送中）
msg.r3("");                                  // msgSource 可空

// 2. 插入本地消息表（回显，发送/重发用 true=replace）
f9 lj = ((c4) j1.v(c4.class)).lj();
long msgId = lj.Bb(msg, true);               // 或 lj.yb(msg)（false 分支）

// 3. 走 NetSceneSendMsg 发送
v51.r0 scene = new v51.r0(msgId, talker);    // resend 构造：读 msgId 并发出去
com.tencent.mm.modelbase.r1 queue = j1.q().b; // NetSceneQueue = gp0.y.b
queue.h(scene, 0);                           // 或 queue.g(scene)
（若想一步到位不手工插库，用新消息构造： new v51.r0(talker, content, 1, 0, 0, msgSource) ，它内部会建 e9 +  Bb(e9,true) 。）
四、"语音能回、文字不回"—— 最可能的 4 个坑（按概率）
① e9 缺  isSend （k1(1)）—— 最大嫌疑 v51.r0.doScene()  里只发  status==1 && isSend==1  的消息：
java
if (arrayList.size() == 0) { Log.w("no sending message"); return -2; }   // ★ 全被跳过，本地有记录但没发出
...
for (...) { if (e9Var2.z0() == 1) { /* 才组包发送 */ } }
如果你的文字 e9 没设  k1(1) （isSend=0），插入后  doScene  一个都不发，通知不弹、对方收不到、本地还躺着一条记录。
② type / content 字段
•  
type 必须是  1 （ TYPE_TEXT  常量检查一下你的值）
•  
文本内容写  b1(content) ；语音写文件路径/时长。若 sendText 误用了语音 e9 模板（type 填 34 / content 字段写成路径），NetScene 会按错误类型组包。
③ 只插库、没入队 NetScene aa.y / Bb  只是本地入库，微信不会"看到数据库有 isSend=1 就自动发"。必须  new r0(msgId, talker)  +  r1.h(scene,0)  入队触发 CGI。语音能发说明你应该已经走了这套，文字就检查是不是少了入队这步。
④ resend 读表失败 new r0(msgId, talker)  内部  k0.yi(talker, msgId)  反查消息。若  Bb(true)  插入的表名与实际 talker 表不一致（特殊字符/群后缀  @chatroom ），读不到 → doScene 返回 -2 →  onSceneEnd(0,0)  静默失败。建议发送前  lj.N3(talker, msgId)  自检一下能读到。
五、一句话定位法
在你模块  sendText  入口和  v51.r0.doScene  里各加一行日志：
[sendText] talker=... content=... isSend=1 type=1 msgId=...
[doScene] queue.size=... first.isSend=... first.type=...
对比语音：同一链路下，文字若是 isSend=0 / type≠1 / 队列空（-2），就是上面①/②/③；队列有消息、也发了，但对方还是收不到，再去查 CGI 组包和 status。

第二次审查核对:
我重新核对了这一长串对话里的每一条关键结论，发现 1 处需要修正、1 处需要降级置信度，其余实锤保真。审查结果如下。
二次审查结果（针对后台捕获 + v1038 + 发送方案全链路）
🔴 修正 1： m.t()  不是"前后台"标志，我之前的表述有误
重新反编译了  gp0.m.t() ：
java
public static boolean t() {
    ...
    Log.w("MMKernel.CoreAccount", "account holded :%s init:%b", ...);
    return t;   // 静态字段 = 账号是否被"hold"（会话持有状态）
}
 m.t()  =  CoreAccount.isHold() （账号被持有），与前后台/锁屏无关。
这修正了什么：
•  
我之前说"后台时  !m.t()  走 y51.c 立即处理、前台走 z0" —— 这个前后台判读是错的。 v51.d2.g  的  z = !m.t() && j1.a()  只判断"未 hold 且已登录"，与前后台无关。
•  
真实的前后台/锁屏判定在别处： v51.z0.onGYNetEnd  用  PowerManager.isScreenOn （仅 idkey 统计）、 a2.b  用  AppExplicitBackgroundOwner.isBackground() （仅影响"消息未处理"的上报，不参与插库分支）。
•  
修正后的结论：接收链路没有独立的"后台不插库"分支。后台捕获不到 = 消息没进 App（断连/被杀/hold 丢弃）或 hook 没接住（进程/时序），不存在"后台专门走另一条不调 f9.Bb 的路"。
🟡 置信度降级：":push 进程不插库 → hook 空转"是强推断，尚未进程级实锤
•  
证据强度： gp0.y.b = modelbase.r1 （NetSceneQueue）、 v51.r0  构造/doScene、接收链路全链源码 —— 这些都实锤。
•  
但 "f9.Bb 只在主进程被调用、push 进程装了 hook 也不触发" 这一条，我无法读到 manifest 的  process  属性（ run_command  取不到 APK，无权限）来确认  NotifyService  挂在哪进程，只能降级为"高置信假设"。
•  
验证方法（不依赖我来做）：push 进程日志里 grep  收到文本 / 过滤: / afterHookedMethod 异常  三行——
•  
若三者都无 → push 进程确实从不触发（支持"空转"假设）；
•  
若有"过滤/异常" → push 进程其实在处理，是过滤链或反射字段的问题，方向完全不同。
✅ 实锤保真（源码级，二次复核通过）
Bb 仅 6 个直接调用点； f9.yb  包  Bb(e9,false) 
find_caller + yb 源码 line 2945
boolean：false=INSERT（接收/业务）、true=REPLACE（发送/重发）
Bb 源码 line 235-257
接收链  a2.b → b41.k.k → aa.y → f9.yb → Bb(e9,false) 
a2.b / b41.k.k line 113-118 / aa.y line 792-803，find_caller 交叉
发送链  构造e9 → Bb(e9,true) → v51.r0 → newsendmsg(522) 
v51.r0 构造 line 118 + doScene line 289-358
doScene 只发  z0()==1 （isSend=1） ← 文字发送最大坑
doScene line 325  if (e9Var2.z0() == 1) 
NetSceneQueue= j1.q().b = modelbase.r1 ，入队  h/g 
gp0.j1.q→gp0.y→字段 b=Lcom/tencent/mm/modelbase/r1 ✅
e9 字段  u1=talker b1=content e1=createTime k1=isSend t1=status 
v51.r0 构造源码逐行对应
读记录  G2(talker) 全量 /  H2(talker,ct,limit) 分页 /  D3(talker,from,to) 时间范围
f9 源码 line 828/859/370
 gp0.j1 =MMKernel、 al5.g =FilePreviewHelper、 Db/Hb =备份恢复
源码头部+调用方日志
📌 最终修正版判断（可以直接用）
1.  
后台捕获不到：不是"后台走别的链路"，而是 ① 主进程 hook 是否真装上（DexKit 时序）② push 进程是否参与插库 ③ 消息是否到达。→ 先确认主进程日志里有没有  已 hook f9.Bb/yb ，再 grep push 进程的  过滤/异常  行。
2.  
文字能发语音不能：优先检查你的 sendText 里 e9 是否  k1(1) （isSend=1）—— 这是 doScene 组包发送的硬条件；其次 type=1、content 走  b1() 、最后确认  r1.h(scene,0)  真的入队了。