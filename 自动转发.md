# 微信消息自动转发（自动群发）完整逆向分析报告
分析工具：LSPilot AI 助手（宿主内逆向）
目标包：com.tencent.mm（微信 Android 8.0.x 混淆结构，storage.e9 消息实体体系，推断 8.0.40+）
用途：独立 Xposed Java 模块（非 LSPilot BSH 插件）实现"收到指定联系人图文/语音 → 自动转发给设定群聊/联系人"

════════════════════════════════════════════════════════════
一、消息实体（MsgInfo） — 所有操作的对象
════════════════════════════════════════════════════════════

继承链（class_hierarchy 实测）：
  com.tencent.mm.storage.e9 (最终消息实体)
    └─ dx0.p3
        └─ im.c8 (消息基类, autoDB, 字段带 field_ 前缀)
            └─ qf5.f0 (DB 基类)

im.c8 / e9 关键字段访问（全部 smali 实测验证）：

  field_content        j()→String        b1(String)→void   消息内容：文本=纯文本, 多媒体=XML
  field_talker         N0()→String       u1(String)→void   会话ID（对端 wxid / 群id / 公众号id）★核心
  field_type           getType()→int     setType(int)      消息类型
  field_msgId          getMsgId()→long   setMsgId(long)    本地（DB）消息ID
  field_msgSvrId       F0()→long         q1(long)          服务器消息ID
  field_msgSeq         D0()→long         n1(long)          消息 Seq
  field_createTime     getCreateTime()→long e1(long)       创建时间(毫秒)
  field_status         M0()→int          t1(int)   状态(2=发送成功 5=失败, 存储层实测)
  field_isSend         z0()→int          —   是否自己发送(0=收, 1=发)★过滤用
  field_imgPath        x0()→String       j1(String)        图片/语音/视频本地路径
  field_reserved       G0()→String       r3(String)        保留字段(引用/魔术内容)
  field_lvbuffer       —                 l1(byte[])        protobuf 原文
  field_bizChatUserId  p0()→String       —                 企业微信用户ID
  field_bizClientMsgId q0()→String       —                 客户端消息ID

典型类型判断方法（e9 内实测 smali）：
  Z2() : type ∈ {1,11,21,31,36,0x42000031}  → 文本族
  C2() : type == 47         → 文件消息
  k2() : (type&0xffff)==49 或 type==0x42000031 → AppMsg 卡片族
  t2() : type==0x11000031   → AppMsg 特殊变体(小程序等)
  isVideo(): type==43       → 视频
  type==3  → 图片, type==34 → 语音, type==10000 → 系统消息

════════════════════════════════════════════════════════════
二、消息存储层（读写 DB）
════════════════════════════════════════════════════════════

1) MsgInfoStorage（消息 DAO）：
   类：com.tencent.mm.storage.f9   implements vn3.m0
   构造：(qf5.k0, d8, k8)
   关键方法（decompile_class_methods_only 实测）：
     yb(e9)→long            插入一条消息（返回本地 msgId）
     Bb(e9,boolean)→long    插入
     Hb(e9)→long / Db(e9)→long  插入(带锁/不带)
     Qc(long,e9,boolean)→int  更新
     Ic(long,e9)→int          更新
     O3(String talker,long svrId)→e9  按 talker+svrId 查
     N3(String,long)→e9 / J3→e9      按 talker+msgId 查
     G7(String)→e9          会话最后一条
     P8(String)→e9          最新一条

2) 存储逻辑工具类：
   com.tencent.mm.storage.b41.aa (MicroMsg.MsgInfoStorageLogic, 全静态)
     y(e9)→long            插入并自动创建会话对象
     o(e9, p0)             接收消息修复(补 flag/createTime)

3) 服务定位（两种都可用，模块内常用反射方式调用）：
   tn3.c4 / com.tencent.mm.storage.c4（服务接口）
     lj() → 消息存储(vn3.m0, 实际= f9)
     cj() → 会话存储(y3)
   获取：gp0.j1.v(tn3.c4.class) 或 ph5.n0.c(tn3.c4.class)
   b41.h9（MessageStorage 服务容器）
     b()→b41.e，e.v()→vn3.m0(f9)

4) 快捷存取工具 ex0.j0（= ex0.k0.F0 静态单例，已实测字段类型）：
     r(sp3.g)→long         插入 protobuf 消息封装并返回 msgId
                            sp3.g 字段偏移：+2 createTime, +3 talker, +4 isSend,
                            +7 type, +9 status, +12 imgPath, +13 content
     k(String talker,long msgId)→e9  取消息
     s(String talker,long msgId,e9)  更新消息
     l/n(String talker,long svrId)→e9 按svrId取
     m(MsgIdTalker)→e9             按 前向引用 取

════════════════════════════════════════════════════════════
三、消息接收链路（自动转发的触发点）
════════════════════════════════════════════════════════════

链路（反编译实测）：
  长连接收到的 AddMsg（增量同步, syncType==5 批量）/ 单条推送
    → com.tencent.mm.plugin.messenger.foundation.a2
      (MicroMsg.MessageSyncExtension, implements tn3.b5)
      a2.a(Object obj, j60, int, byte[], boolean, p35, e5)  批量同步
      a2.b(p0, p35, e5)                                      单条 AddMsg 同步 ★hook点
    → 解析 AddMsg: pc5.k4（位于 p0.a 字段，字段实测）
        r=svrMsgId, e=q16(from), f=q16(to), g=type, o=createTime(秒),
        s=msgSeq, h=q16(content), n=p16(msgSource), p/q=扩展串, i=imgStatus, t=push
    → tn3.g5.b(5, k4) 分组分发观察者；tn3.k5.H0(k4) 观察者回调
    → s0.a(type).k(p0) 按类型构造消息实体 e9
    → 存入 f9（MsgInfoStorage.yb）
    → tn3.e5.a(e9, k4) 通知新消息（tn3.e5 接口: void a(e9,k4); void b()）

最佳 Hook 点（择一，推荐 D→C→B 稳定性递减）：
  D) f9.yb(e9)          —— 存储层 100% 必经（也含自己发出的消息，用 field_isSend 过滤）
  C) b41.aa.y(e9)       —— 同上，带自动建会话
  B) tn3.e5.a(e9,k4)    —— 直接拿到 e9 实体 + AddMsg 参数，最方便鉴权
  A) a2.b(p0,p35,e5)    —— 所有同步单条入口，拿到 k4 后再查/组 e9

════════════════════════════════════════════════════════════
四、消息发送链路（自动转发的执行点）
════════════════════════════════════════════════════════════

【A. 纯文本发送（无 UI，最简，适合群发文本）】
  入口：hy.a（MicroMsg.BypMsgInfoService, extends ph5.w implements iy.d0）
        hy.a.ej(String from, String text, String sessionId, long createTime) → void
        hy.a.gj(String to, String chatName, long msgId, String text)         → boolean
  内部（反编译实测）：
    A) 构造 up3.o BypSendParams(fromUserName=当前用户, toUserName=目标, sessionId=目标,
        bizType=0, msgSource, cliMsgId=目标+'T'+createTime, fileName=null, extXml=null)
    B) 填充 sp3.g 消息封装（talker/content/type=1/status=1...）
    C) ex0.j0.r(gVar) 入库（返回 msgId）
    D) 协程 up3.l → up3.n.a(o, 1, filler) 构造 cs 网络请求(from/to/type=1/content) 发送
    E) 回调更新 DB status（t1(2)成功 / t1(5)失败）—— up3.l.invokeSuspend 实测
  等效链式发送器：v51.r1（builder）
    r1.e(String text).h(String talker).i(int type).a() → n1，n1.a()→boolean 发送

【B. AppMsg / 图片 / 文件 / 卡片 / 链接 发送（核心）】
  类：com.tencent.mm.pluginsdk.model.app.k0（MicroMsg.AppMsgLogic，全静态）
     M(r appInfo, String appId, String talker, String source, String attachPath,
       byte[] data, String thumbPath, String reserved, String innerXml,
       MsgIdTalker, boolean, String, String) → Pair(code, msgId)
    r = dx0.r（AppMessage, extends dx0.i）：i=子类型, q=attach路径, n()=title, g=desc,
        d=appId, F=authKey, U=aesKey, B=cdnUrl, m=长度, w2=Pair, x2=MsgQuoteItem
    解析：dx0.r.v(contentXml) → r；序列化：dx0.r.u(r, null, null) → contentXml
    M() 流程（反编译实测）：
      构造 e9：b1(contentXml) / t1(1) / u1(目标talker) / e1(0) / k1(1) / setType(p(r))
      图片压缩/缩略 (b1.ej() 系列: r5/v5/q5/y1)
      入库 (h9.b().v().yb(e9) 或 ex0.j0.r)
      发送队列：v5.fj() + d4(NetSceneSendAppMsg)（CGI /cgi-bin/micromsg-bin/sendappmsg, type=222）
      CDN 上传在 d4.doScene 内处理

【C. 官方"转发单条"入口（复用最接近"转发"语义）】
  com.tencent.mm.ui.chatting.a4.h(Context, String talker, e9 msg,
                                  boolean, boolean, String) → void
    (MicroMsg.ChattingEditModeRetransmitMsg.retransmitSingleMsg)
    内部按类型分派：文本→q3.E；图片→复制 attach；AppMsg→q3.G；名片→u1.a().nj()...
  简化封装：
  q3.G(Context, String talker, String appmsgXml, int type, boolean flag) → void
    构新 e9 设 content=appmsgXml, type, 调 q3.E 发送
  q3.E(Context, String talker, e9 msg, boolean, boolean) → void   AppMsg 通用发送

【D. 发送确认】
  发送完成微信写库 status=2；失败走 SendMsgFailEvent；可用轮询 f9 或 hook tn3.e5.a 确认。

════════════════════════════════════════════════════════════
五、自动转发实现方案（可执行骨架）
════════════════════════════════════════════════════════════

核心思想：
  1) Hook 接收（推荐 f9.yb 或 e5.a）拿到消息 e9
  2) 过滤：e9.N0() ∈ 指定来源 && 类型 ∈ {文本/图片/AppMsg} && !isSend
  3) 转发：按类型分派；目标列表循环 = 自动群发

Java(Xposed) 伪代码骨架：

  // ── ① 接收：hook 消息同步回调（tn3.e5.a 或 a2.b）──
  XposedHelpers.findAndHookMethod(CLS_MSG_SYNC_EXT, "b",
      Cls_p0, Cls_p35, Cls_e5, new XC_MethodHook() {
      protected void afterHookedMethod(MethodHookParam p) {
          Object k4 = XposedHelpers.getObjectField(p.arg(0), "a"); // pc5.k4 = AddMsg
          boolean isSend = 当前用户.equals(q16(k4.f).d);           // from==我
          if (isSend) return;
          String talker = q16(k4.f).d;                              // 会话
          int type = getInt(k4, "g");
          String content = q16(k4.h).d;                             // 原文/XML
          long svrId = getLong(k4, "r");
          if (!rules.match(talker, type)) return;
          Object e9 = ex0.j0.k(talker, svrId);                      // 取入库消息实体
          forwardToTargets(e9, type, content);
      }
  });

  // ── ② 转发分派 ──
  void forwardToTargets(e9 msg, int type, String content) {
      for (String target : TARGETS) {           // 群发目标列表
          switch (type) {
            case 1: case 11: case 21: case 31:  // 文本族
                hy.a.ej(myId, content, target, System.currentTimeMillis());
                break;
            case 3:  // 图片：取本地文件→AppMsg 图片
                String path = msg.x0();          // 原图路径
                byte[] img = readFile(path);
                dx0.r r = newR(3, title, content, attachPath);
                k0.M(r, appId, target, source, attachPath, img, thumb, null, null,
                     new MsgIdTalker(0, null), false, null, null);
                break;
            case 49: // AppMsg(卡片/链接/小程序/文件) 原样转发
                q3.G(appContext, target, content, 49, false);
                break;
            case 47: // 文件
                k0.M(构造文件r(attachPath=本地文件), ..., target, ..., path, null, ...);
                break;
            case 34: // 语音：微信 UI 禁止直接转发语音 → 降级二选一
                // 方式1 把 msg.x0() 语音文件作为"文件"消息发送
                // 方式2 有转写则发转写文本
                break;
          }
      }
  }

  关键服务获取：
    Object svc = callStatic(Cls_ph5_n0, "c", Cls_服务接口.class);
    // 或   gp0.j1.v(Class)
  当前用户 wxid：b41.y1.u()
════════════════════════════════════════════════════════════
六、DexKit 动态适配方案（免硬编码/跨小版本）
════════════════════════════════════════════════════════════

策略：每个混淆类用"稳定字符串锚点 + 结构特征"定位，版本升级后初始化时重定位并缓存。

1) 消息同步扩展(接收)：使用字符串 "MicroMsg.MessageSyncExtension"
    类 com.tencent.mm.plugin.messenger.foundation.a2
    方法 b:(p0,p35,e5)→q0；a:(6参)
2) AppMsg 发送核心：使用字符串 "MicroMsg.AppMsgLogic"
    类 com.tencent.mm.pluginsdk.model.app.k0，方法 M 参数含 dx0.r+talker+byte[]+MsgIdTalker
3) NetSceneSendAppMsg：使用字符串 "MicroMsg.NetSceneSendAppMsg"
    类 com.tencent.mm.pluginsdk.model.app.d4，构造 (String,long,String,String)
4) 文本发送(Byp)：使用字符串 "MicroMsg.BypSendHelper" → up3.l/up3.m；
    BypSendParams=up3.o 字段 (fromUserName,toUserName,sessionId,bizType,msgSource,
    cliMsgId,fileName,extCommonInfoXml) —— toString 已含字段名可作特征
5) 消息实体：类 hasField "field_talker" && "field_content" && method "getType()I" → im.c8；
    取 subclass 且 hasField "field_imgPath" → e9；兜底：继承 qf5.f0 && Cloneable
6) 消息 DAO：类 hasField(构造含 qf5.k0) && 方法 "yb" 返回 long、"O3" 返回 e9 → f9
7) 快捷存取 ex0.j0：字段 F0 类型 ex0.j0，类 hasMethod "r(sp3.g)J" / "k(Ljava/lang/String;J)e9"

DexKit Java 示例：
    DexKitBridge.create(cx, (bridge) -> {
        List<ClassEntity> cls = bridge.findClassUsingStrings(
            Arrays.asList("MicroMsg.MessageSyncExtension"), false, 1);
        String msgSync = cls.get(0).getClassName();
        bridge.close();
    });

适配建议：
  - 冷启动在 initZygote/afterHookEnv 后异步跑一次 DexKit 定位并缓存
  - 微信版本变化重新定位（com.tencent.mm.app.BuildInfo 或 package 信息）
  - 方法参数类型用 DexKit findMethod + getMethodParamTypes 动态获取

════════════════════════════════════════════════════════════
七、版本适配 & 风险提示
════════════════════════════════════════════════════════════
- 本报告基于当前宿主微信实测反编译（storage.e9 体系，8.0.40+ 特征）。
- 首次上线建议先做"监听验证"（只 hook a2.b 打印 newMsg 日志），确认体系一致后再放开转发。
- 文本=hy.a.ej 直发；图片/卡片=q3.G 或 k0.M；文件=type47 k0.M；语音无官方转发 → 降级。
- 高频群发有封号/被骚扰风险：加频率限制、白名单、人工确认。
- 微信风控：sendappmsg 有 CDN/频率检查，异常触发安全验证（f51.b NetSceneSendMsgFake 场景）。
- 所有 hook 在 UI 线程外收窄、try/catch 兜底，避免拖垮微信主进程。
