我已经把 8.0.78(3180) 的语音消息整条链路拆完了。这类模块类名全部混淆，核心链路集中在  v61.* （语音业务层）、 yl.* （录音层）、 pv/wb0 （路径与逻辑服务）里。下面是地毯式分析结果。
一、整条链路总览（发送 + 接收）
【发送】
ChatFooter(输入栏) ─► com.tencent.mm.ui.chatting.k4 (ChattingFooterEventImpl.u)
      │
      ▼
yl.x0 (SceneVoice.Recorder) ─► 开始录音 e(talker, quoteMsg, extra)
      │
      ├─ 默认: v61.d1.h(talker, "amr_")   → 在 voiceinfo 表注册记录，生成 baseName
      │        v61.l1 (VoiceRecorder=Android MediaRecorder, AMR_NB)  写文件
      └─ Opus: v61.y (OpusVoiceRecorder, 16kHz/64kbps) + v61.c(v61.w) Opus writer
      │
      ▼
yl.x0.stop() ─► v61.d1.u(baseName, durationMs, 0, quoteMsg, quoteXml)
      │
      ▼
v61.d1.u()：构造 type=34 消息(e9) → b41.aa( MsgInfoStorageLogic ).y() 入库 → x(c1) 回写voiceinfo
      │
      ▼
x51.b0 (SendMsgService, /cgi-bin/micromsg-bin/newsendmsg) → CDN 上传

【接收】
v61.k (NetSceneDownloadVoice, /cgi-bin/micromsg-bin/downloadvoice, CDN)
      │
      ▼
v61.d1.v/updateRecv + v61.d1.c(checkAndUpdateVoiceFormat) → 写 voice2 文件
      │
      ▼
pv.p0 (VoiceLogicService) / AsyncMediaPlayerWrapper → 播放(chatting/viewitems/bq 渲染)
二、发送侧关键类逐个拆解
1. UI 入口
•  
 com.tencent.mm.pluginsdk.ui.chat.ChatFooter  — 输入栏（492 个字符串，含大语音相关逻辑）
•  
 com.tencent.mm.ui.chatting.k4 （ChattingFooterEventImpl）
•  
 u(String fileName, int durationMs, e9 quoteMsg)  — 录音结束后的发送出口：
•  
普通聊天 →  v61.d1.u(fileName, durationMs, 0, quoteMsg, K0) （AMR 经典管线，走  e.g().c(RepairerConfigVoiceScenesOpus)!=1  时）
•  
开启 Opus 或企业/群聊特殊场景 →  xi0.d  +  yi0.f.pj(...) （新管线，受  RepairerConfigNewSendVoice  控制）
2. 录音层  yl.* （SceneVoice.Recorder）
•  
 yl.x0  字段： a:yl/d （录音器接口）、 e:String (baseName)、 h:e9 (引用消息)、 i:String (引用XML)
•  
 e(talker, e9 quote, str2)  开始：决定管线（Opus/AMR/SPX/漂流瓶）， amr  分支调用  v61.d1.h(str, "amr_")  注册记录并拿 baseName
•  
 stop() ：长度 <800ms 取消；否则  v61.d1.u(this.e, this.n, 0, this.h, this.i)  发送，然后  v0.dj().e()  清理
•  
 yl.d  = 录音器接口（ startRecord(fileName)  /  stopRecord() ）
•  
两个实现：
•  
 v61.l1 （MicroMsg VoiceRecorder）→ Android  MediaRecorder ：MIC、AMR_NB(encoder=1)、maxDuration 70000ms
•  
 v61.y （MicroMsg.Audio.OpusVoiceRecorder）→ PCM(16k) + Opus(64kbps) writer，文件头  #!OPUS_RAW_V1 
3. 语音逻辑  v61.d1  = VoiceLogic（核心！）
 h(talker, prefix) 
在  v61.m1/v0 （voiceinfo 表）注册记录，返回 baseName（ prefix + 时间名 ）
 u(baseName, durationMs, flag, quote, quoteXml) 
⭐ 发送语音消息的唯一入口（msg type=34 入库+触发发送）
 s(talker, fileName, duration) 
⭐ 复制已有语音文件 → 注册 → 发送（转发/发送给朋友用同款）
 k(fileName) 
读 voiceinfo 记录
 b(fileName)  /  t(fileName) 
取消录音（删记录）
 c(c1, path) 
checkAndUpdateVoiceFormat：按文件头魔数修正 voiceformat
 x(c1) 
回写 voiceinfo（msgId 关联）
 v(...) 
updateRecv（接收写入）
三、⭐ 两条可直接调用的发送 API（对你"音频转语音发送"最有用）
入口 A： v61.d1.s(String talker, String fileName, int duration)  — 文件已存在就发
反编译逻辑（这就是微信"转发/发送给朋友"同款路径）：
java
public static String s(String talker, String fileName, int duration) {
    String newName = h(talker, b1.c(fileName));   // b1.c: 按前缀判断格式(amr_/silk_/spx_/opus_)
    // 把源文件 voice2/...复制到新名 (VFSFileOp.copy, 失败<0 就放弃)
    if (z6.d(getVoiceFullPath(fileName), getVoiceFullPath(newName, true), false) < 0) return null;
    u(newName, duration, 1, null, null);          // 发送
    return newName;
}
调用方式：源文件必须已经放在正确的语音目录，且文件名以  amr_ / silk_ / spx_ / opus_  开头（否则按 amr 算）。
入口 B： v61.d1.u(String baseName, int durationMs, int flag, e9 quote, String quoteXml)  — 直接发送
java
// 1) 注册 voiceinfo，拿 baseName
String base = d1.h(talker, "amr_");            // → "amr_xxxx"
// 2) 把 AMR 数据写到语音目录的完整路径
String fullPath = pv.p0.ej(x, base, true);     // 或 wb0.b.Ej(x, base, true)
writeAmrData(fullPath, pcmOrAmrBytes);
// 3) 发送（会自己敲 duration、构造 type34 msg、入库、触发上传）
boolean ok = d1.u(base, durationMs, 0, null, null);
四、语音文件的格式与路径（决定性细节）
文件格式判定（ v61.b1 ，读文件头魔数）
0
 #!AMR\n （前6字节）
AMR_NB，经典录音默认
2
spx 头
老版 SPX
3
 #!OPUS_RAW_V1 （前13字节）
Opus（64kbps/16k）
1
其它
兜底
•  
文件名前缀决定记录格式： amr_ / spx_ / silk_ / opus_ ，默认  amr_ （ v61.b1.c ）
•  
baseName =  前缀 + b41.x1.a(y1.u(), now)  =  前缀 + "ssHHmmMMddyy" + md5[0..7] + hex + 100~106 
•  
发送时会  d1.c  按文件头真实魔数校正 voiceformat（amr→"0"、spx→"4"、其它→"1"），所以你文件名  .amr  但内容为 opus 也能被识别校正
文件存放路径（ pv.p0.ej  = getAmrFullPath）
完整路径 = <账号目录>/voice2/<md5[0:2]>/<md5[2:4]>/msg_<baseName>.amr
•  
 k1.d(voice2路径, "msg_", baseName, ".amr", mode=2, beCreate)  → mode 2 = md5 两级 hash 子目录（ k1.b(k.g(baseName)) ）
•  
beCreate=true 会主动建目录
•  
老文件会从  voice2/msg_xxx.amr 、 voice/xxx.amr  迁移（不推荐手工拼路径，统一用  pv.p0.ej(x, baseName, true)  解析）
voiceinfo 表（ v61.m1/v0 ）
•  
方法： n1(fileName)  查记录、 y1(c1)  插入、 K1(fileName,c1)  更新、 u1(msgSvrId)  按服务端ID查、 t1/x1  兜底
•  
记录字段( v61.c1 )： b =fileName、 c =talker、 l =时长ms、 p =voiceformat、 i =状态、 a =flags、 m =localMsgId、 x =msgSvrId、 w =cdn url/aeskey、 e =svrId
五、接收/播放链路（简要）
•  
 v61.k  = NetSceneDownloadVoice（ /cgi-bin/micromsg-bin/downloadvoice  CDN）， doScene  里  startCdnVoiceDownload 
•  
 v61.f1  = VoiceMsgExtension：消息管线里解析 voice XML：
xml
<msg><voicemsg length="" clientmsgid="" endflag="1" cancelflag="0" voicelength=""
fromusername="" voiceformat="" bufid="" voiceurl="" aeskey=""/></msg>
•  
 pv.p0.gj  = AsyncMediaPlayerWrapper /  VoiceComponent_playUploadVoiceSound  播放
•  
渲染： com.tencent.mm.ui.chatting.viewitems.bq （语音气泡）
六、给你的通用 Xposed 模块落地建议
最稳路线 = 入口 B（d1.h + pv.p0.ej + d1.u），格式选 AMR 或 Opus：
1.  
转码：你的音频（mp3/wav）→ PCM → AMR_NB(8k/单声道) 或 Opus(16k/64kbps+ #!OPUS_RAW_V1  头)
•  
模块内置 ffmpeg 转，或直接调微信自带的  com.tencent.mm.modelvoice.MediaRecorder.native_pcm2amr(pcm, len, PByteArray, flag) （但 native 需在主 dex ClassLoader 上下文，推荐前者更可控）
2.  
Hook 点（找当前会话 / 固定会话发）：
java
// 1) 注册 voiceinfo
String base = (String) XposedHelpers.callStaticMethod(
    clazz("v61.d1"), "h", talker, "amr_");
// 2) 解析并写入文件
String full = (String) XposedHelpers.callStaticMethod(
    clazz("pv.p0"), "ej", xInstance, base, true);
writeFile(full, amrBytes);
// 3) 发送
boolean ok = (Boolean) XposedHelpers.callStaticMethod(
    clazz("v61.d1"), "u", base, durationMs, 0, null, null);
3.  
兜底 Hook：拦  yl.x0.stop()  /  k4.u(...)  在真实录音发送前替换  this.e  指向的文件内容（把录音文件替换成你的音频），这样完全走微信原生路径，最不容易被风控。
几个坑提醒：
•  
 d1.u  内部会  b1.g  从文件算时长，文件不存在/魔数不对 → 返回 false 并删记录，所以必须先写文件再 u
•  
引用消息  e9  传 null 即可；群聊/企业场景可能被强制走 Opus 新管线（ xi0.d + yi0.f ），如果开的是  RepairerConfigVoiceScenesOpus  建议你统一用 Opus 格式
•  
AMR 必须带  #!AMR\n  头（Android MediaRecorder 输出自带）；Opus 必须带  #!OPUS_RAW_V1  13字节头
•  
发送是异步的（SendMsgService）， u  返回 true 只代表入库成功
