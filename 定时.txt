基于我提供的 ScheduleBroadcast.java 源码，在我现有的 com.leshao.v3 Xposed 微信模块内部集成这套定时群发功能，不输出、不导出任何独立文件，全部逻辑内嵌在现有模块工程中，不拆分独立子模块，严格遵守以下规则：
 
1. 全程不修改、不删除、不重构项目原有任意代码、原有Hook、原有UI、原有工具类 ContextManager / LogWriter，只新增代码、新增内部调用逻辑、新增页面入口，新旧功能完全兼容无冲突。
​
2. 所有定时群发相关代码全部作为模块内置功能，整合进现有包路径 com.leshao.v3.hook，只新增内部类与方法，不拆分独立工程、不单独打包、不生成外部依赖文件。
​
3. 完整实现源码内标注的55项定时群发全部功能，微信8.0.76对应的底层存储API、消息构造、定时调度、群查询、模板变量、文件发送、filehelper指令交互逻辑1:1保留，不简化删减任何功能。
​
4. 修复源码自带全部隐患：数据库无锁/未关闭、主线程ANR、Android12+闹钟权限失效、PendingIntent覆盖、静态上下文泄漏、硬编码明文类名逆向风险、休眠杀任务、多账号uin切换错乱、SP多线程丢失数据、循环sleep无法紧急停止、媒体文件校验缺失等问题；所有优化仅新增代码，不动原有业务。
​
5. 配套内置配套逻辑：
​
- 在现有Xposed入口类追加初始化定时模块、注册filehelper监听的少量代码；
​
- 在现有设置界面增加定时功能跳转入口，新增内置定时管理页面作为模块内置页面；
​
- 内置权限引导逻辑、前台保活、定时广播接收器，全部内嵌项目，不拆分独立组件；
​
- 补充群分组、场景模板、草稿箱、发送统计、敏感词自定义全套内置逻辑。
​
6. 安全混淆适配：仅在现有proguard规则里追加本功能所需keep片段，不改动原有混淆配置；所有微信类名、表名、反射字符串内置异或加密逻辑，SP存储任务数据简易加密，Release移除本地SD卡日志。
​
7. 输出要求：
① 列出仅需要新增的代码片段/内部类，全部内嵌现有项目，无需新建独立文件；
② 给出两处原有类仅需追加的少量代码（入口Hook、主设置页面）；
③ 给出 build.gradle、AndroidManifest、混淆文件需要追加的片段，不覆盖原有内容；
④ 全部代码写完后自检全部功能，确认和原有模块共存无闪退、Hook不失效、定时任务正常触发；
⑤ 所有新增代码直接粘贴进现有项目即可运行，不需要导出任何独立文件、不需要拆分子模块。