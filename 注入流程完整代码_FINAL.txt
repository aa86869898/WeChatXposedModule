================================================================================
     WeKit 聊天窗口长按菜单注入「转发[K]」按钮 —— 最终完整分析 (v3)
================================================================================

分析目标: dev.ujhhgtg.wekit (WeKit Xposed 模块)
APK 内类总数: 128 个 wekit 相关类

================================================================================
目录
================================================================================
  第一章  架构总览
  第二章  数据结构详解 (tq9/vq9/wq9/sq9/yq9/u83/y83)
  第三章  WeChatMessageContextMenuApi 完整源码 (10个方法)
  第四章  in9.B() 四个运行时 Hook 回调 (case 21/22/23/24)
  第五章  "转发"菜单提供者 d43 完整源码
  第六章  onClick 回调 q90(6)/q90(7) 完整源码
  第七章  条件过滤器 sw2(25)/sw2(26)
  第八章  全局开关 ra5.i.g
  第九章  DexKit 方法定位机制
  第十章  完整时序流程 (12步)
  第十一章  所有遗漏点总结


================================================================================
第一章  架构总览
================================================================================

WeKit 使用三层架构向微信聊天上下文菜单注入自定义按钮:

┌─────────────────────────────────────────────────────────────┐
│                      WeChat App (微信)                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  DexKit 自动搜索定位以下微信方法:                      │   │
│  │  ① addMenuItem(int, CharSequence, Drawable)          │   │
│  │  ② onContextItemSelected(MenuItem)                   │   │
│  │  ③ 多选菜单创建方法                                   │   │
│  │  ④ 多选菜单项选择方法                                 │   │
│  └──────────────┬──────────────────────────────────────┘   │
│                 │ XposedBridge.hookMethod()                  │
│  ┌──────────────▼──────────────────────────────────────┐   │
│  │  WeChatMessageContextMenuApi  (核心注入器)            │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ <clinit>: 4个 pl6 DexKit委托 → su6 Method引用  │  │   │
│  │  │ D(): 安装4个Xposed Hook (in9 21/22/23/24)      │  │   │
│  │  │ K()/N(): 注册/注销菜单提供者                   │  │   │
│  │  ├────────────────────────────────────────────────┤  │   │
│  │  │ ★ 运行时回调 (in9.B() switch case 21-24):      │  │   │
│  │  │ case 21 → 长按消息 → 注入菜单项到微信原生菜单   │  │   │
│  │  │ case 22 → 点击菜单项 → 分发到 onClick 回调      │  │   │
│  │  │ case 23 → 多选模式菜单创建                      │  │   │
│  │  │ case 24 → 多选模式点击 → P()注入子菜单          │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  └──────────────┬──────────────────────────────────────┘   │
│                 │ K()/N() 注册/注销                          │
│  ┌──────────────▼──────────────────────────────────────┐   │
│  │  12个 sq9 实现类 (菜单项提供者)                       │   │
│  │  d43→"转发"  f43→"转圈"  r90→"撤回"  rf5→"修改"    │   │
│  │  y17→"复读"  ys6→"编辑"  jb2/lb2→"下载"            │   │
│  │  bd7→"存本地(语音)"  wc7→"存本地(表情)"             │   │
│  │  q92→"查看详情"  zc7→"保存到面板"                   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘


================================================================================
第二章  数据结构详解
================================================================================

2.1  sq9 接口 —— 菜单提供者接口
────────────────────────────────
public interface sq9 {
    List a();       // 返回 List<tq9> 菜单项定义列表
}

2.2  tq9 类 —— 完整菜单项定义 (7个字段!)
────────────────────────────────
public final class tq9 {
    public final int a;           // 菜单项 ID (如 777010)
    public final String b;        // 显示文本 (如 "转发")
    public final Drawable c;      // 图标 Drawable
    public final aq3 d;           // Compose 矢量图标 (imageVector)
    public final u83 e;           // isSupported: 每条消息的条件检查
    public final yq9 f;           // multiSelect: 全局条件 (vq9/wq9/xq9)
    public final y83 g;           // onClick: 点击回调 y83.z(View,rq9,msg)

    public tq9(int id, String text, Drawable drawable, aq3 imageVector,
               u83 isSupported, yq9 multiSelect, y83 onClick) { ... }
}

// toString() 验证: "MenuItem(id=..., text=..., drawable=..., 
//                           imageVector=..., isSupported=..., 
//                           multiSelect=..., onClick=...)"

2.3  yq9 及其子类 —— 多选条件接口
────────────────────────────────
public interface yq9 { }

// vq9: 单条消息条件 (用于判断消息类型是否支持该操作)
public final class vq9 implements yq9 {
    public final u83 a;           // 条件函数 (如 sw2(26))
    public final y83 b;           // 多选时的回调 (如 q90.i0 = q90(6))
    
    public vq9(u83 condition, y83 multiCallback) {
        this.a = condition;
        this.b = multiCallback;
    }
}

// wq9: 多条消息条件
// xq9: 不区分单条/多条

2.4  u83 接口 —— 条件函数
────────────────────────────────
public interface u83 {
    Object B(Object obj);     // 单参数条件判断, 返回 Boolean 或其他
}

2.5  y83 接口 —— 点击回调
────────────────────────────────
public interface y83 {
    Object z(Object view, Object msgData, Object msgOrList);
    // view = View (聊天 View)
    // msgData = rq9 (包装原始消息对象)
    // msgOrList = jb5 (单条) 或 List<jb5> (多条)
}


================================================================================
第三章  WeChatMessageContextMenuApi 完整源码
================================================================================

类路径: dev.ujhhgtg.wekit.features.api.ui.WeChatMessageContextMenuApi
父类:   x50 (BaseFeature 抽象基类)

静态字段:
  g: WeChatMessageContextMenuApi  单例
  h: ec4[4]                       DexKit 方法委托数组
  i: LinkedHashMap<String,List>   菜单注册表 (Key=提供者类名, Value=菜单项列表)
  j: su6                          methodCreateMenu 的 Method 引用
  k: su6                          methodSelectMenuItem 的 Method 引用
  l: su6                          methodMultiCreateMenu 的 Method 引用
  m: su6                          methodMultiSelectMenuItem 的 Method 引用

实例字段:
  n: View                         缓存当前消息的聊天 View

================================================================================
3.1  <clinit> — 静态初始化 (300行 Smali)
================================================================================

.method static constructor <clinit>()V
    # === 创建4个 DexKit 方法委托 (pl6) ===
    # pl6(宿主Class, Kotlin属性名, JVM签名descriptor, flags)

    # [0] methodCreateMenu
    new-instance v0, Lpl6;
    invoke-direct {v0, v1, "methodCreateMenu",
        "getMethodCreateMenu()Ldev/ujhhgtg/wekit/dexkit/dsl/DexMethodDelegate;", 0}

    # [1] methodSelectMenuItem
    new-instance v2, Lpl6;
    invoke-direct {v2, v1, "methodSelectMenuItem",
        "getMethodSelectMenuItem()L...DexMethodDelegate;", 0}

    # [2] methodMultiCreateMenu
    new-instance v3, Lpl6;
    invoke-direct {v3, v1, "methodMultiCreateMenu",
        "getMethodMultiCreateMenu()L...DexMethodDelegate;", 0}

    # [3] methodMultiSelectMenuItem
    new-instance v5, Lpl6;
    invoke-direct {v5, v1, "methodMultiSelectMenuItem",
        "getMethodMultiSelectMenuItem()L...DexMethodDelegate;", 0}

    # 存入静态数组 h[4]
    sput-object v6, ...WeChatMessageContextMenuApi;->h:[Lec4;

    # 创建单例 + 创建 LinkedHashMap 注册表
    sput-object v5, ...WeChatMessageContextMenuApi;->g:L...;
    sput-object v7, ...WeChatMessageContextMenuApi;->i:Ljava/util/LinkedHashMap;

    # === 解析4个委托为 Method 引用 ===
    # 通过 pq9(3) → q35.O(7, pq9) → h52.a() → su6 j
    # 通过 pq9(4) → ... → su6 k
    # 通过 pq9(5) → ... → su6 l
    # 通过 pq9(6) → ... → su6 m
    sput-object v4, ...;->j:Lsu6;
    sput-object v0, ...;->k:Lsu6;
    sput-object v0, ...;->l:Lsu6;
    sput-object v0, ...;->m:Lsu6;
    return-void
.end method

================================================================================
3.2  D() — 安装 Xposed Hook (174行 Smali)
================================================================================

.method public final D()V
    # Hook 1: h[0]=methodCreateMenu → in9(21) 回调
    x50.x(this, j.resolve(h[0]), new in9(0x15))

    # Hook 2: h[1]=methodSelectMenuItem → in9(22) 回调
    x50.x(this, k.resolve(h[1]), new in9(0x16))

    # Hook 3: h[2]=methodMultiCreateMenu → in9(23) 回调
    x50.x(this, l.resolve(h[2]), new in9(0x17))

    # Hook 4: h[3]=methodMultiSelectMenuItem → in9(24) 回调
    x50.x(this, m.resolve(h[3]), new in9(0x18))
.end method

# x50.x() → x50.w(executable, 50ms, callback) → XposedBridge.hookMethod()
# 包装为 w50 (XC_MethodHook子类), beforeHookedMethod → in9.B(param)

================================================================================
3.3  K() — 注册菜单项提供者 (44行 Smali)
================================================================================

.method public static K(Lsq9 p0)V
    # Key = p0.getClass().getName()  (如 "d43")
    # Value = p0.a() → List<tq9>    (如 [tq9(777010, "转发", ...)])
    LinkedHashMap i.put(className, menuItemList)
.end method

================================================================================
3.4  N() — 注销菜单项提供者 (36行 Smali)
================================================================================

.method public static N(Lsq9 p0)V
    LinkedHashMap i.remove(p0.getClass().getName())
.end method

================================================================================
3.5  L() — 反射获取消息列表 (Java)
================================================================================

public static ArrayList L(Object obj) {
    // 反射查找 thisObject 中类型为 List 的字段
    // 获取其值并包装为 ArrayList<jb5>
    t43 t43Var = new t43(obj.getClass());
    iv2 condition = new iv2();
    condition.Z = List.class;
    sz6 field = t43Var.I(condition);
    if (field == null) return null;
    
    List raw = (List) mc9.c(obj, field.a);
    ArrayList result = new ArrayList();
    for (Object item : raw) {
        result.add(new jb5(item));   // jb5 是消息包装类
    }
    return result;
}

================================================================================
3.6  M() — 反射获取聊天 View (Java)
================================================================================

public static View M(Object obj) {
    // 反射查找 thisObject 中的 View 类型字段 (递归父类)
    t43 t43Var = new t43(obj.getClass());
    iv2 condition = new iv2();
    condition.h0 = new pq9(1);   // View 子类过滤器
    condition.X = true;          // 递归
    sz6 field = t43Var.I(condition);
    return field != null ? (View) mc9.c(obj, field.a) : null;
}

================================================================================
3.7  O() — BFS 遍历对象图查找 ChattingContext (Java)
================================================================================

public static Object O(Object obj) {
    Class chattingContextClass = WeMessageApi.g.X().e();
    Set visited = Collections.newSetFromMap(new IdentityHashMap());
    Deque queue = new ArrayDeque();
    queue.addLast(obj);
    visited.add(obj);
    
    // BFS 最多 8 层
    for (int depth = 0; !queue.isEmpty() && depth < 8; depth++) {
        for (int i = 0; i < queue.size(); i++) {
            Object current = queue.removeFirst();
            // 遍历整个继承链的所有字段
            for (Class<?> c = current.getClass();
                 c != null && c != Object.class;
                 c = c.getSuperclass()) {
                for (Field f : c.getDeclaredFields()) {
                    if (f.getType().isPrimitive() || f.getType().isArray())
                        continue;
                    String name = f.getType().getName();
                    // 跳过 java.*, android.*, kotlin.*
                    if (name.startsWith("java.") || name.startsWith("android.")
                        || name.startsWith("kotlin.")) continue;
                    f.setAccessible(true);
                    Object value = f.get(current);
                    if (value == null) continue;
                    if (chattingContextClass.isInstance(value))
                        return value;     // 找到了!
                    if (visited.add(value))
                        queue.addLast(value);  // 继续 BFS
                }
            }
        }
    }
    return null;
}

================================================================================
3.8  P() — Compose 子菜单注入 (498行 Smali)
================================================================================

# P(View view, Object chattingContext, ArrayList<jb5> msgList)

Step 1: 从 LinkedHashMap i.values() 扁平化获取所有 tq9 → v6
Step 2: 初始化 v7=单条菜单列表, v8=多条菜单列表
Step 3: 第一轮遍历 → vq9 类型:
         vq9.a.B(msgList) → Boolean.true → 从 tq9 构造 uq9 → 加入 v7
Step 4: 第二轮遍历 → wq9 类型:
         逐条 tq9.e.B(msg) → 全满足 → 从 tq9 构造 uq9 → 加入 v8
Step 5: v7+v8 都为空 → Toast "没有可用于所选消息的 WeKit 菜单项"
Step 6: dg1.W(context, true, callback) → 显示 Compose 菜单对话框

该方法主要用于 case 24 (多选模式) 和 case 22 的合并模式子菜单。

================================================================================
3.9  I() 和 J() — Compose UI 辅助方法
================================================================================

这两个方法是 Compose UI 辅助函数，用于渲染 WeKit 菜单对话框中的
各个菜单选项的 UI 组件（图标、文字、布局）。它们不参与注入逻辑本身。

I(uq9, j83, qe1, int): 渲染单个菜单项的 UI (图标+文字行)
J(String, qe1, int):   渲染纯文本行


================================================================================
第四章  in9.B() 四个运行时 Hook 回调 (case 21-24)
================================================================================

in9.B() 是一个 3656 行的巨型 switch，packed-switch 从 case 0 到 case 28。
case 21-24 是 WeChatMessageContextMenuApi 的 4 个 Hook 回调。

packed-switch 映射:
  case 21 → :pswitch_2bd  ★ 菜单创建Hook
  case 22 → :pswitch_17c  ★ 菜单点击Hook
  case 23 → :pswitch_10c  ★ 多选菜单创建Hook
  case 24 → :pswitch_bf   ★ 多选菜单点击Hook

================================================================================
4.1  case 21 — 菜单创建Hook (最核心!)
================================================================================

此回调在用户长按消息、微信准备显示上下文菜单时触发。

完整流程:

Step 1: 获取参数
  args[0] = 微信菜单对象 (ContextMenu / MMMenu)
  args[1] = 被长按的 View

Step 2: 从 View 获取消息
  view.getTag() → 原始消息对象
  WeMessageApi.f0(rawMsg) → 转换后的消息对象
  new jb5(msgObj) → 包装为 jb5
  缓存: WeChatMessageContextMenuApi.n = view

Step 3: 反射查找微信菜单的 addMenuItem 方法
  t43.A(in9(29)) → 查找方法 (int, CharSequence, Drawable) → MenuItem

Step 4: 遍历注册表, 用 tq9.e 过滤匹配的菜单项
  for each tq9 in LinkedHashMap i.values():
    if tq9.e.B(jb5) == true:  → 加入 v4 (匹配列表)

Step 5: ★ 根据 ra5.i.g 选择注入模式 ★

──────────────────────────────────────────────────────────
模式 A: 合并模式 (ra5.i.g = true)
──────────────────────────────────────────────────────────
  只向微信菜单添加 1 个 "WeKit" 入口项:
    addMenuItem(0xbdb28, "WeKit", dr2.c)
  
  用户点击 "WeKit" → case 22 检测到 id=0xbdb28 
  → 显示 Compose 子菜单列出所有匹配项

──────────────────────────────────────────────────────────
模式 B: 独立模式 (ra5.i.g = false) ★ 最常用 ★
──────────────────────────────────────────────────────────
  向微信菜单逐个添加匹配的菜单项:
    for each tq9 in v4:
      text = tq9.b + " [K]"          ★ "[K]" 后缀在此硬编码!
      addMenuItem(tq9.a, text, tq9.c)
  
  如 d43 的菜单项:
    tq9.b = "转发" + " [K]" = "转发 [K]"
    addMenuItem(777010, "转发 [K]", drawable)
  
  用户点击 "转发 [K]" → case 22 检测到 id=777010
  → 找到 tq9.a==777010 → 调用 tq9.g.z(view, rq9, jb5)

──────────────────────────────────────────────────────────

关键 Smali (独立模式分支, 行 1760-1838):

:cond_35d
    invoke-virtual {v4}, Ljava/util/ArrayList;->iterator()...;
:goto_361
    check-cast v3, Ltq9;
    
    # 获取菜单 ID
    iget v4, v3, Ltq9;->a:I          # 777010
    invoke-static {v4}, Ljava/lang/Integer;->valueOf(I)...
    
    # ★ 拼接 " [K]" 后缀
    iget-object v5, v3, Ltq9;->b:Ljava/lang/String;   # "转发"
    new-instance v6, Ljava/lang/StringBuilder;
    invoke-direct {v6}, Ljava/lang/StringBuilder;-><init>()V
    invoke-virtual {v6, v5}, ...append("转发")...
    const-string v5, " [K]"                    # ★★★
    invoke-virtual {v6, v5}, ...append(" [K]")...
    invoke-virtual {v6}, ...toString()         # → "转发 [K]"
    
    # 获取图标
    iget-object v3, v3, Ltq9;->c:Landroid/graphics/drawable/Drawable;
    
    # 调用微信的 addMenuItem
    filled-new-array {id, "转发 [K]", icon}, [Ljava/lang/Object;
    invoke-virtual {v0, v1, args}, ...Method.invoke(...)
    # → 微信菜单中出现 "转发 [K]"!

================================================================================
4.2  case 22 — 菜单点击Hook (双模式分发)
================================================================================

此回调在用户点击菜单项时触发。有两个分支:

──────────────────────────────────────────────────────────
分支 A: 合并模式 — 点击 "WeKit" (id=0xbdb28)
──────────────────────────────────────────────────────────
  if menuItem.getItemId() == 0xbdb28:
    1. 从 LinkedHashMap i 获取所有 tq9
    2. 用 tq9.e.B(jb5) 过滤匹配项 → v8
    3. 创建 iq4 回调 (v8, view, jb5, param)
    4. dg1.W(context, true, callback) → 显示 Compose 子菜单
    5. setResult(null) → 消费事件

──────────────────────────────────────────────────────────
分支 B: 独立模式 — 点击 "转发 [K]" (id=777010 等)
──────────────────────────────────────────────────────────
  遍历所有注册的 tq9:
    if tq9.a == menuItem.getItemId():    # 777010 匹配!
      # 调用 onClick 回调
      tq9.g.z(view, new rq9(rawMsg), jb5)
      # = q90(7).z(view, rq9, jb5)
      # → d43.doForward(singleMessage)
      setResult(null) → 消费事件
      break

──────────────────────────────────────────────────────────

关键 Smali (独立模式分支, 行 1243-1328):

:cond_278
    check-cast v2, Ltq9;
    iget v3, v2, Ltq9;->a:I              # 777010
    invoke-interface {v4}, ...getItemId()I # MenuItem.getItemId()
    move-result v5
    if-ne v3, v5, :cond_278              # ID 不匹配 → 继续
    
    # ★ ID 匹配! 调用 onClick
    iget-object v1, v2, Ltq9;->g:Ly83;    # q90(7)
    new-instance v2, Lrq9;
    invoke-direct {v2, v10}, Lrq9;-><init>(rawMsgObj)V
    invoke-interface {v1, v9, v2, v11}, Ly83;->z(
        Ljava/lang/Object;   # view
        Ljava/lang/Object;   # rq9(rawMsg)
        Ljava/lang/Object;   # jb5
    )Ljava/lang/Object;
    # → q90(7).z(view, rq9, jb5) → d43.doForward()

================================================================================
4.3  case 23 — 多选菜单创建Hook
================================================================================

类似 case 21，但在多选消息场景下触发。
检查是否有非 xq9 类型的 tq9。
如果有 → addMenuItem(0xbdb28, "WeKit", icon)

================================================================================
4.4  case 24 — 多选菜单点击Hook
================================================================================

类似 case 22，但在多选消息场景下。
检测到 id==0xbdb28 → 调用 L(), O(), M(), P()
  L(thisObject) → ArrayList<jb5> (消息列表)
  O(thisObject) → ChattingContext (BFS 查找)
  M(thisObject) → View (聊天 View)
  P(view, ctx, msgList) → 显示 Compose 子菜单


================================================================================
第五章  "转发"菜单项提供者 d43 完整源码
================================================================================

public class d43 implements sq9 {

    // D(): 功能启用时注册
    public final void D() {
        WeChatMessageContextMenuApi.K(this);
    }

    // C(): 功能禁用时注销
    public final void C() {
        WeChatMessageContextMenuApi.N(this);
    }

    // a(): 返回菜单项定义 (sq9 接口方法)
    public final List a() {
        // 构建矢量图标 (缓存, 仅首次创建)
        a43 a43Var = a43.c;
        aq3 aq3Var = u5.E;
        if (aq3Var == null) {
            zp3 zp3Var = new zp3("forward", 24f, 24f, 960f, 960f, ...);
            // ... 大量矢量路径绘制 (省略) ...
            aq3Var = zp3Var.c();
            u5.E = aq3Var;
        }

        return hu.O(new tq9(
            777010,            // a: 菜单ID
            "转发",            // b: 文本 (会被 + " [K]")
            a43Var,            // c: Drawable 图标
            aq3Var,            // d: Compose 矢量图标
            new sw2(25),       // e: isSupported 条件
            new vq9(           // f: multiSelect 条件
                new sw2(26),   //    vq9.a = 单消息条件
                q90.i0         //    vq9.b = 多消息回调 = q90(6)
            ),
            q90.j0             // g: onClick 回调 = q90(7)
        ));
    }
}

字段对应关系:
  tq9.a = 777010        → 菜单项 ID
  tq9.b = "转发"        → 显示文本
  tq9.c = a43Var        → Drawable 图标
  tq9.d = aq3Var        → Compose 矢量图标
  tq9.e = sw2(25)       → 每消息条件 (isSupported)
  tq9.f = vq9(sw2(26), q90(6)) → 多选条件
  tq9.g = q90(7)        → 点击回调 (单消息转发)


================================================================================
第六章  onClick 回调 q90(6)/q90(7) 完整源码
================================================================================

q90 类有 21 个静态单例 (Y 到 w0), 实现了 y83 接口:

  q90.i0 = new q90(6)   ← vq9.b 多消息转发回调
  q90.j0 = new q90(7)   ← tq9.g 单消息转发回调

================================================================================
6.1  q90(7) — 单消息转发 onClick
================================================================================

public final Object z(Object view, Object msgData, Object msgOrList) {
    // case 7:
    View v = (View) view;
    Object rawMsg = ((rq9) msgData).a;
    jb5 msg = (jb5) msgOrList;
    
    d43 d43Instance = d43.i;
    nd2 nd2Var = new nd2(6, msg);     // 包装为 nd2 任务
    // 异步执行转发
    sx1 executor = y82.a;
    dh1.B(is0.c(bx1.Z), null, null,
          new ry1(v, nd2Var, null, 7), 3);
}

================================================================================
6.2  q90(6) — 多消息转发 onClick (vq9.b)
================================================================================

public final Object z(Object view, Object msgData, Object msgOrList) {
    // case 6:
    View v = (View) view;
    Object rawMsg = ((rq9) msgData).a;
    List<jb5> msgList = (List) msgOrList;
    
    d43 d43Instance = d43.i;
    w6 w6Var = new w6(4, msgList);    // 包装为 w6 任务
    // 异步执行批量转发
    sx1 executor = y82.a;
    dh1.B(is0.c(bx1.Z), null, null,
          new ry1(v, w6Var, null, 7), 3);
}


================================================================================
第七章  条件过滤器 sw2(25) / sw2(26)
================================================================================

sw2 是一个巨大的 u83 工厂类, switch 从 case 0 到 case 28。

sw2(25) = tq9.e (isSupported): 
  参数: jb5 (单条消息)
  返回: Boolean (该消息是否支持转发)
  逻辑: 检查消息类型 (通过 jb5.l() 获取类型), 
        判断是否为可转发的卡片/文本/图片等类型

sw2(26) = vq9.a (单消息条件):
  参数: ArrayList<jb5> (消息列表)
  返回: Boolean (整组消息是否满足条件)
  逻辑: 对消息列表执行条件判断


================================================================================
第八章  全局开关 ra5.i.g
================================================================================

ra5 extends jc8 (SwitchFeature 基类):
  ra5.i 是单例
  ra5.i.g 是 boolean，控制合并/独立模式

jc8.H() 方法:
  从 SharedPreferences 读取开关值:
    this.g = prefs.getBoolean(featureName, defaultJ());
  
  如果 g=true → 调用 p() → 调用子类 D()
  如果 g=false → 不启用

在运行时:
  case 21 中读取 ra5.i.g:
    若 true → 合并模式 (只添加 1 个 "WeKit" 入口)
    若 false → 独立模式 (逐个添加 "转发 [K]" 等)

这个开关对应 WeKit 设置中的某个功能开关,
默认值由 jc8.J() 返回 (当前实现返回 false → 独立模式)。


================================================================================
第九章  DexKit 方法定位机制
================================================================================

9.1  pl6 — Kotlin 属性委托
────────────────────────────────
pl6 extends ql6 implements dc4:
  pl6(WeChatMessageContextMenuApi.class, "methodCreateMenu", 
       "getMethodCreateMenu()L...DexMethodDelegate;", 0)
  
  get(obj) → m0c().x(new Object[]{obj})
  m0c() → g0().c()  → DexKit DSL 的构建方法

9.2  q35.O(priority, pq9) — 解析委托
────────────────────────────────
q35.O(7, pq9(3)):
  优先级=7, 回调=pq9(3)
  内部调用 DexKit DSL 搜索微信 DEX, 定位方法

9.3  h52.a() — 获取 Method 引用
────────────────────────────────
将 DexKit 搜索结果包装为 su6 (Method 引用)

9.4  x50.x() → XposedBridge.hookMethod()
────────────────────────────────
su6.resolve(pl6) → x52 (Method引用)
x52.e() → Executable (Method/Constructor)
XposedBridge.hookMethod(executable, w50(in9))


================================================================================
第十章  完整时序流程 (12步)
================================================================================

 1. APP 启动
    └ WeChatMessageContextMenuApi.<clinit>()
      ├ 创建 4 个 pl6 DexKit 委托
      ├ 创建 LinkedHashMap i (注册表)
      └ q35.O + h52.a → 解析委托为 su6 (j,k,l,m)

 2. WeKit 设置加载
    └ ra5.i.H()
      └ ra5.i.g = SharedPreferences.getBoolean(...)
      └ if true → p() → 子类 D()

 3. 功能模块加载
    └ d43.D() → K(this) → i["d43"] = [tq9(777010, "转发", ...)]

 4. BaseFeature.p() 触发
    └ WeChatMessageContextMenuApi.D()
      ├ Xposed Hook → 微信 menuCreateMethod       → in9(21)
      ├ Xposed Hook → 微信 menuSelectMethod        → in9(22)
      ├ Xposed Hook → 微信 multiCreateMethod       → in9(23)
      └ Xposed Hook → 微信 multiSelectMethod       → in9(24)

 5. 用户长按消息
    └ 微信调用 menuCreateMethod

 6. in9(21) (case 21) 触发
    ├ args[0] = 微信菜单对象
    ├ args[1] = View → getTag() → 消息 → jb5
    ├ 反射查找 addMenuItem(int, CharSequence, Drawable)
    └ 遍历 i 的 tq9 → tq9.e.B(jb5) 过滤

 7. ★ 注入菜单项 (根据 ra5.i.g)
    ├ 合并模式: addMenuItem(0xbdb28, "WeKit", icon)
    └ 独立模式: addMenuItem(777010, "转发 [K]", icon)  ← "[K]"!!
                 addMenuItem(其他ID, "其他文本 [K]", icon)

 8. 用户看到菜单: "转发 [K]" 等项

 9. 用户点击 "转发 [K]"
    └ 微信调用 menuSelectMethod

10. in9(22) (case 22) 触发
    ├ 获取 MenuItem.getItemId() = 777010
    ├ 判断: 777010 ≠ 0xbdb28 → 跳过合并模式分支
    └ 遍历 tq9 → tq9.a == 777010 → 匹配!

11. ★ 执行 onClick
    └ tq9.g.z(view, rq9(rawMsg), jb5)
    └ = q90(7).z(view, rq9, jb5)
    └ = d43.i.doForward(singleMsg)

12. ★ 转发完成
    └ dh1.B 异步线程执行实际转发逻辑
    └ c43 转发引擎处理消息


================================================================================
第十一章  遗漏点总结 (相比上一版分析)
================================================================================

本次分析修正/补充的内容:

1. ★ tq9 有 7 个字段，不是 5 个
   a(id), b(text), c(drawable), d(imageVector/aq3),
   e(isSupported/u83), f(multiSelect/yq9), g(onClick/y83)

2. ★ vq9 有两个字段: a:u83 (条件), b:y83 (多选回调)

3. ★ in9.B() 的 packed-switch 标签名 ≠ case 值
   标签 :pswitch_7b 对应 case 27 (不是 case 21!)
   case 21 是 :pswitch_2bd

4. ★ case 21 有两种注入模式:
   合并模式 (ra5.i.g=true): 添加 1 个"WeKit"入口
   独立模式 (ra5.i.g=false): 逐个添加，且拼接 " [K]" 后缀

5. ★ case 22 有双重分发:
   分支A: id=0xbdb28 → Compose 子菜单 (合并模式)
   分支B: id=tq9.a → 直接调用 tq9.g.onClick (独立模式)

6. ★ "[K]" 后缀硬编码在 case 21 独立模式分支中

7. ★ ra5.i.g 来自 SharedPreferences，控制模式切换

8. ★ q90(6) 和 q90(7) 是实际执行转发的 onClick
   q90(7)=单消息转发, q90(6)=多消息转发

9. ★ sw2(25) 和 sw2(26) 是消息类型条件检查

10. ★ in9(23) 和 in9(24) 处理多选消息的菜单注入

11. ★ I() 和 J() 是 Compose UI 渲染辅助方法，非注入逻辑

12. ★ wekit 模块有完整的 128 个类，菜单注入只是其功能之一


================================================================================
                         —— 全文完 (v3 Final) ——
================================================================================
